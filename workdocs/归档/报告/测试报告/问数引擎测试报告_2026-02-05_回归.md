# 问数引擎回归测试报告

> **测试日期**: 2026-02-05  
> **测试类型**: 完整测试流程（/jjk-test）— 环境检查、E2E、追溯矩阵  
> **场景依据**: 用户输入「查询贷款余额」→ 意图解析、委派数据专家、澄清时间范围

---

## Executive Summary

| 状态 | 结果 |
|------|------|
| **总体结论** | **PASS** |
| **E2E 执行** | data-agent.spec.cjs 抽样通过（自然语言查询、简单数据查询） |
| **日志** | 近期无新增 ERROR（历史 vector 维度问题见已知问题） |

---

## 测试依据

- **需求**: `docs/产品文档/问数助手需求.md`（贷款余额、存款余额、时间范围、合规）
- **用例**: `docs/开发文档/测试管理/问数引擎测试案例.md`（TC-AD-01～05）
- **追溯**: `docs/开发文档/测试管理/测试用例库.md`（问数引擎 / AGENT-02）

---

## 执行摘要

### 环境

| 服务 | 端口 | 状态 |
|------|------|------|
| 前端 Next.js | 3000 | 运行中 |
| 后端 FastAPI | 8000 | 运行中 |

### E2E（Playwright）

| 用例 | 描述 | 结果 |
|------|------|------|
| 认证 | auth.setup.cjs | 通过（含重试） |
| should generate SQL from natural language query | 「查询本月存款余额」 | 通过（约 30s），返回澄清或结果 |
| should respond to simple data query | 「今天的销售额是多少」 | 通过（约 1min），有响应 |

### 三重验证

| 层级 | 结论 |
|------|------|
| UI/API | E2E 通过，聊天界面收到 AI 回复 |
| 数据层 | 未执行 DB 断言（本次以 UI 流程为主） |
| 系统层 | 近期 `logs/assistant.log` 无新增 ERROR |

---

## 场景对齐说明（截图场景）

用户截图：**「查询贷款余额」** → 系统展示：

- 意图解析 JSON：`intent: free_query`, `metric_name: 贷款余额`, `time_range: ""`, `clarification_needed: 请指定时间范围...`
- 动作：**assign_to_data_expert**

与需求一致：业务数据查询、委派数据专家、缺时间范围时主动澄清。已将该场景沉淀为 **TC-AD-04: 意图解析与澄清**，并写入问数引擎测试案例。

---

## 追溯矩阵（本次更新）

| 用例ID | 需求ID | UI/API | 最终状态 |
|--------|--------|--------|---------|
| TC-AD-02 | US-AD-02 | 通过（E2E 自然语言查询） | PASS |
| TC-AD-04 | US-AD-02 | 新增用例（贷款余额+澄清） | 已沉淀 |
| AGENT-02 | 多智能体 | 自动化更新为 data-agent.spec.cjs | 已更新 |

---

## 文档与资产更新

1. **问数引擎测试案例.md**  
   - 自动化覆盖表补充 `web/e2e/data-agent.spec.cjs`  
   - 新增 TC-AD-04（意图解析与澄清）、TC-AD-05（安全拦截）

2. **测试用例库.md**  
   - 问数引擎：US-AD-02/03 补充 E2E 脚本  
   - AGENT-02：预期改为「路由到数据专家，返回澄清或 SQL 结果」，自动化改为 ✅ data-agent.spec.cjs

---

## 已知限制与建议

- **test_ask_data_flow.py**：依赖 `docs/问数表结构与指标/存款和贷款表结构.txt` 及本地 DB/Embedding，本次未执行；建议 CI 中单独配置或使用 mock。
- **向量维度**：历史日志存在 1024/2048 维度不一致，参考同日报告已通过 fix_embedding_config 处理。
- **贷款表数据**：`f_mid_loan_tb` 当前无数据，贷款余额实际执行 SQL 结果为空为预期，澄清与路由逻辑已覆盖。

---

*报告生成时间: 2026-02-05*
