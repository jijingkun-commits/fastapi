# C08 P2 后台查询与治理能力补齐

> WS 编号: WS-C08  
> 类型: parallel  
> 对应 feature_id: P1-08

## 0. 关联与来源

- 对应 `task_key`: `PP-20260304-USER-MEMORY-LLM-ASYNC`
- 来源主计划：`workdocs/归档/实施计划/用户个性化永久记忆与管理能力_implementation_plan.md`
- 来源并行计划：`workdocs/任务拆解/2026-03-04_用户个性化永久记忆与管理能力/parallel_plan.md`

## 1. 目标

- 本包目标：后台提供列表详情调试归档删除重建
- 完成定义（DoD）：后台提供列表详情调试归档删除重建; 统一记忆语义字段与审计行为

### 1.1 功能机制

- 触发条件：当前卡被 `card_order` 激活。
- 输入：上游依赖卡状态与本卡代码锚点。
- 输出：本卡 `acceptance_checks` 全部通过并回填证据。
- 状态流转：Backlog -> Doing -> Review -> Done。

### 1.2 代码锚点与样例

- 代码锚点：
  - `app/api/v1/endpoints/memory_admin_api.py::list_memories`
  - `app/services/memory_admin_service.py::archive_memory`

```python
# 伪代码：执行本卡机制并输出可验证结果
result = execute_c08()
assert result.ok
```

## 2. 文件边界

### 可修改（白名单）
- `app/api/v1/endpoints/memory_admin_api.py`
- `app/services/memory_admin_service.py`

### 禁止修改（黑名单）
- 不在本卡代码锚点中的跨卡文件

## 3. 状态与契约

- 依赖卡：C06
- task_mode：`implementation-card`
- merge_required：`true`
- 外部契约：`planning_contract` 与 `vk_cards` 一致

## 4. 实施步骤

1. 拉取并确认依赖卡状态。
2. 完成机制点并补测试。
3. 执行验收命令并回填证据。

## 5. 测试与验收

- 验收命令：
- `venv/bin/python -m pytest tests/api/test_memory_admin_api.py -q`

- 对应 implementation plan `done_gate`：`workdocs/归档/实施计划/用户个性化永久记忆与管理能力_implementation_plan.md` 中 `C08` 段。
- 证据回填位置：`workdocs/归档/实施计划/用户个性化永久记忆与管理能力_implementation_plan.md`

## 6. 风险与回滚

- 主要风险：跨卡依赖未满足导致误推进。
- 回滚点：
- memory.intent_admin_enabled=false

## 7. card_export（机读）

```yaml
card_export:
  id: WS-C08
  card_id: C08
  feature_ids: ["P1-08"]
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  hard_depends_on: ["C06"]
  acceptance_checks:
    - venv/bin/python -m pytest tests/api/test_memory_admin_api.py -q
  rollback_anchors:
    - memory.intent_admin_enabled=false
  evidence_entry: workdocs/归档/实施计划/用户个性化永久记忆与管理能力_implementation_plan.md
  pr_id: PR-08
  pr_branch: codex/user-memory-async-pr-08
  pr_depends_on: ["PR-06"]
  pr_subject: "后台查询治理能力补齐"
```
