# 工作包说明：WS-C03 C03 S2 候选去重与证据卡片

> WS 编号: WS-C03  
> 名称: C03 S2 候选去重与证据卡片  
> 类型: parallel  
> 对应 `feature_id`: P2-01, P2-02

## 0. 关联与来源

- 对应 `task_key`: PP-20260301-KB-RETRIEVAL-P2
- 对应 `task_id`: T-03, T-04
- 来源主计划: `docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md`
- 来源并行计划: `workdocs/任务拆解/2026-03-01_知识库检索P2分阶段治理/parallel_plan.md`

## 1. 目标

- 本包目标:
- 同文档候选限额与重复片段去重
- 结果组装改为证据卡片，降低上下文噪声
- 保持 KB 图片占位符协议兼容

## 2. 文件边界

### 可修改（白名单）
- app/ai/tools/ragflow_tool.py
- tests/unit/test_ragflow_tool.py

### 禁止修改（黑名单）
- 白名单外文件

## 3. 状态与契约

- 可写字段: 当前卡片 `feature_ids` 对应实现字段
- 只读字段: 上一阶段已冻结契约与下游未解锁字段
- 外部契约: `depends_on=['C02']`

## 4. 实施步骤

1. 按 `mechanism_summary` 完成核心改造。
2. 绑定并执行 `acceptance_checks`。
3. 回填证据到 `evidence_entry`。

### 4.1 串行门禁

- 前置卡: C02
- 解锁条件: 当前卡 `acceptance_checks` 全部通过
- 本 WS 不得推进条件: 前置卡未通过或 Gate 阻断

## 5. 测试与验收

- 最小测试集:
  - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "dedup or evidence"

### 5.0 验收门禁映射

- 对应 implementation plan `done_gate`:
- 候选去重与文档限额生效
- 证据回填位置: `docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md`

## 6. 风险与回滚

- 主要风险: 契约偏移或回归失败导致串行阻塞
- 回滚点:
- 关闭去重与文档限额开关
- 回退到旧 result_text 拼接逻辑

## 7. card_export（机读）

```yaml
card_export:
  id: WS-C03
  feature_id: P2-01
  card_key: PP-20260301-KB-RETRIEVAL-P2::WS-C03
  title: C03 S2 候选去重与证据卡片
  type: parallel
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  hard_depends_on: ['C02']
  soft_depends_on: []
  depends_on: ['C02']
  file_whitelist:
    - app/ai/tools/ragflow_tool.py
    - tests/unit/test_ragflow_tool.py
  mechanism_summary:
    - 同文档候选限额与重复片段去重
    - 结果组装改为证据卡片，降低上下文噪声
    - 保持 KB 图片占位符协议兼容
  code_anchor_refs:
    - app/ai/tools/ragflow_tool.py::_dedup_and_cap_candidates
    - app/ai/tools/ragflow_tool.py::_format_retrieval_results
  example_refs:
    - docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md#L1
  acceptance_checks:
    - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "dedup or evidence"
  rollback_anchors:
    - 关闭去重与文档限额开关
    - 回退到旧 result_text 拼接逻辑
  evidence_entry: docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md#8
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "dedup or evidence"
  done_gate:
    - 候选去重与文档限额生效
  pr_id: PR-02
  pr_branch: codex/kb-retrieval-p2-pr-02
  pr_depends_on: ['PR-01']
  pr_subject: S2 候选去重与文档限额
```
