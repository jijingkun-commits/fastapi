# 工作包说明：WS-C05 C05 S3 查询改写与融合重排

> WS 编号: WS-C05  
> 名称: C05 S3 查询改写与融合重排  
> 类型: parallel  
> 对应 `feature_id`: P3-01, P3-02

## 0. 关联与来源

- 对应 `task_key`: PP-20260301-KB-RETRIEVAL-P2
- 对应 `task_id`: T-06, T-07
- 来源主计划: `workdocs/归档/正文/实施计划/知识库检索P2分阶段治理_implementation_plan.md`
- 来源并行计划: `workdocs/任务拆解/2026-03-01_知识库检索P2分阶段治理/parallel_plan.md`

## 1. 目标

- 本包目标:
- 原问主路 + 扩展路并行召回
- 多路候选融合排序并保留可解释分数
- 改写失败自动回退主路

## 2. 文件边界

### 可修改（白名单）
- app/ai/tools/ragflow_tool.py
- tests/unit/test_ragflow_tool.py

### 禁止修改（黑名单）
- 白名单外文件

## 3. 状态与契约

- 可写字段: 当前卡片 `feature_ids` 对应实现字段
- 只读字段: 上一阶段已冻结契约与下游未解锁字段
- 外部契约: `depends_on=['C04']`

## 4. 实施步骤

1. 按 `mechanism_summary` 完成核心改造。
2. 绑定并执行 `acceptance_checks`。
3. 回填证据到 `evidence_entry`。

### 4.1 串行门禁

- 前置卡: C04
- 解锁条件: 当前卡 `acceptance_checks` 全部通过
- 本 WS 不得推进条件: 前置卡未通过或 Gate 阻断

## 5. 测试与验收

- 最小测试集:
  - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "rewrite or rerank"

### 5.0 验收门禁映射

- 对应 implementation plan `done_gate`:
- 改写与多路融合可开关控制
- 证据回填位置: `workdocs/归档/正文/实施计划/知识库检索P2分阶段治理_implementation_plan.md`

## 6. 风险与回滚

- 主要风险: 契约偏移或回归失败导致串行阻塞
- 回滚点:
- 关闭 query rewrite 开关，仅保留原问主路
- 回退到单路 similarity 排序

## 7. card_export（机读）

```yaml
card_export:
  id: WS-C05
  feature_id: P3-01
  card_key: PP-20260301-KB-RETRIEVAL-P2::WS-C05
  title: C05 S3 查询改写与融合重排
  type: parallel
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  hard_depends_on: ['C04']
  soft_depends_on: []
  depends_on: ['C04']
  file_whitelist:
    - app/ai/tools/ragflow_tool.py
    - tests/unit/test_ragflow_tool.py
  mechanism_summary:
    - 原问主路 + 扩展路并行召回
    - 多路候选融合排序并保留可解释分数
    - 改写失败自动回退主路
  code_anchor_refs:
    - app/ai/tools/ragflow_tool.py::_build_retrieval_queries
    - app/ai/tools/ragflow_tool.py::_merge_and_rerank_candidates
  example_refs:
    - workdocs/归档/正文/实施计划/知识库检索P2分阶段治理_implementation_plan.md#L1
  acceptance_checks:
    - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "rewrite or rerank"
  rollback_anchors:
    - 关闭 query rewrite 开关，仅保留原问主路
    - 回退到单路 similarity 排序
  evidence_entry: workdocs/归档/正文/实施计划/知识库检索P2分阶段治理_implementation_plan.md#8
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "rewrite or rerank"
  done_gate:
    - 改写与多路融合可开关控制
  pr_id: PR-04
  pr_branch: codex/kb-retrieval-p2-pr-04
  pr_depends_on: ['PR-03']
  pr_subject: S3 查询改写与多路召回融合
```
