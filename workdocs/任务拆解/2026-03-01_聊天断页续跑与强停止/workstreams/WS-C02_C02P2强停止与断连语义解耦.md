# WS-C02 C02 P2 强停止与断连语义解耦

> WS 编号: WS-C02  
> 对应卡片: `C02`  
> 类型: `parallel`  
> 对应 `feature_id`: `P1-02,P1-03`

## 0. 关联与来源

- 对应 `task_key`: `PP-20260301-CHAT-RUN-STOP`
- 来源主计划: `workdocs/归档/实施计划/聊天断页续跑与强停止_implementation_plan.md`
- 来源并行计划: `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/parallel_plan.md`
- card_key: `PP-20260301-CHAT-RUN-STOP::WS-C02`
- PR 归属: `PR-02` / `codex/chat-run-stop-pr-02`
- PR 依赖: `['PR-01']`

## 1. 目标

- 停止按钮改为强停止：cancel run + 本地 abort 双阶段收口
- 服务端区分 disconnect 与 cancel，断页不断任务、点击停止强取消
- 补齐取消失败重试与前端提示，避免“假停止”体验

### 1.1 功能机制（必填）

- 触发条件: 前置依赖 `C01` 完成
- 输入: 上游卡片 done gate 证据 + 当前 feature 配置
- 输出: 卡片验收证据、回滚锚点、状态更新
- 状态流转（含异常分支）: `Backlog -> Doing -> Review -> Gate -> Done`，失败则回滚并标记 blocked
- 与上/下游 WS 的契约关系: 仅在 `depends_on` 满足后推进

### 1.2 代码锚点与样例（必填）

- 代码锚点（函数/类级）:
  - `web/src/hooks/useSSEStream.ts::stop`
  - `app/services/chat_service.py::ChatService.stream`
  - `app/api/v1/endpoints/chat_api.py::cancel_run`
- 最小样例（可伪代码）:

```python
if checks_passed:
    mark_done(card_id)
else:
    rollback_to_anchor()
```

## 2. 文件边界

### 可修改（白名单）
- `web/src/hooks/useSSEStream.ts`
- `web/src/components/chat/ChatInput.tsx`
- `app/services/chat_service.py`
- `app/api/v1/endpoints/chat_api.py`
- `tests/unit/test_chat_service_disconnect_continue.py`

### 禁止修改（黑名单）
- `workdocs/归档/实施计划/openclaw迁移重建基线_implementation_plan.md`

## 3. 状态与契约

- 可写字段: `card.status`、`evidence_entry`、`gate_result`
- 只读字段: `card_id`、`feature_ids`、`depends_on`
- 外部契约: `planning_contract` 与 `vk_cards.json` 一致

## 4. 实施步骤

1. 拉取前置卡完成状态并执行本卡前置检查。
2. 运行 `acceptance_checks` 并记录输出。
3. 回填证据与回滚锚点结果。

### 4.1 串行门禁（serial 模式必填）

- 前置卡: `C01`
- 解锁条件: `depends_on` 对应卡片全部通过
- 本 WS 不得推进条件: 任一 `acceptance_checks` 失败

## 5. 测试与验收

- 最小测试集:
- `cd web && pnpm exec playwright test e2e/test_feedback_stop.spec.cjs --project=chromium`
- `venv/bin/python -m pytest tests/unit/test_chat_service_disconnect_continue.py -q`
- 验收标准:
- 停止按钮改为强停止
- 断连继续语义通过单测

### 5.0 验收门禁映射（必填）

- 对应 implementation plan `done_gate`: `停止按钮改为强停止; 断连继续语义通过单测`
- 本 WS 负责的门禁子项: `C02:P1-02,P1-03`
- 证据回填位置（文档节）: `workdocs/归档/实施计划/聊天断页续跑与强停止_implementation_plan.md#7`

## 6. 风险与回滚

- 主要风险: 前置卡未完成、字段契约漂移、测试结果不稳定
- 回滚点:
- 前端 stop 回退到仅 AbortController.abort
- 后端断连语义回退到旧 streaming 处理路径
- 回滚开关/策略: 仅在本卡范围回滚，禁止跨卡改动

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表: 见白名单
- 是否修改了白名单外文件（是/否）: 否
- 测试命令与结果: 见 acceptance_checks
- 已知风险点: 串行链路断裂导致阻塞
- 回滚建议: 先回滚本卡，再评估下游
- 证据绑定检查（target_task_id == evidence_task_id）: 必填

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-C02
  feature_id: P1-02,P1-03
  card_key: PP-20260301-CHAT-RUN-STOP::WS-C02
  title: C02 P2 强停止与断连语义解耦
  type: parallel
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  lane: lane-runtime-control
  hard_depends_on: ['C01']
  soft_depends_on: []
  depends_on: ['C01']
  file_whitelist:
    - web/src/hooks/useSSEStream.ts
    - web/src/components/chat/ChatInput.tsx
    - app/services/chat_service.py
    - app/api/v1/endpoints/chat_api.py
    - tests/unit/test_chat_service_disconnect_continue.py
  mechanism_summary:
    - 停止按钮改为强停止：cancel run + 本地 abort 双阶段收口
    - 服务端区分 disconnect 与 cancel，断页不断任务、点击停止强取消
    - 补齐取消失败重试与前端提示，避免“假停止”体验
  code_anchor_refs:
    - web/src/hooks/useSSEStream.ts::stop
    - app/services/chat_service.py::ChatService.stream
    - app/api/v1/endpoints/chat_api.py::cancel_run
  acceptance_checks:
    - cd web && pnpm exec playwright test e2e/test_feedback_stop.spec.cjs --project=chromium
    - venv/bin/python -m pytest tests/unit/test_chat_service_disconnect_continue.py -q
  rollback_anchors:
    - 前端 stop 回退到仅 AbortController.abort
    - 后端断连语义回退到旧 streaming 处理路径
  evidence_entry: workdocs/归档/实施计划/聊天断页续跑与强停止_implementation_plan.md#7
  check_cmd:
    - cd web && pnpm exec playwright test e2e/test_feedback_stop.spec.cjs --project=chromium
    - venv/bin/python -m pytest tests/unit/test_chat_service_disconnect_continue.py -q
  done_gate:
    - 停止按钮改为强停止
    - 断连继续语义通过单测
  source_ws_file: workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/workstreams/WS-C02_C02P2强停止与断连语义解耦.md
```
