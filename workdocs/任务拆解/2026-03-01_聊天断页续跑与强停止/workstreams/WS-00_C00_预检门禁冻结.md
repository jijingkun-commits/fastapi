# WS-00 C00 预检门禁冻结

> WS 编号: WS-00  
> 对应卡片: `C00`  
> 类型: `foundation`  
> 对应 `feature_id`: `C00-01`

## 0. 关联与来源

- 对应 `task_key`: `PP-20260301-CHAT-RUN-STOP`
- 来源主计划: `workdocs/归档/正文/实施计划/聊天断页续跑与强停止_implementation_plan.md`
- 来源并行计划: `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/parallel_plan.md`

## 1. 目标

- 校验 planning_contract 与 vk_cards 一致性
- 冻结 task_key 与执行模式，禁止执行期改名
- 完成 active_task 绑定前禁止进入 C01

### 1.1 功能机制（必填）

- 触发条件: 前置依赖 `无` 完成
- 输入: implementation plan 与 vk_cards 契约内容
- 输出: preflight_status 与 active_task 对齐证据
- 状态流转（含异常分支）: `Backlog -> Doing -> Review -> Gate -> Done`，失败则阻断下游

### 1.2 代码锚点与样例（必填）

- 代码锚点（函数/类级）:
  - `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/contracts/vk_cards.json::card_order`
  - `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/contracts/_active_task.json::task_key`
  - `scripts/set_active_task.py::main`
- 最小样例（可伪代码）:

```python
if preflight_pass:
    allow_enter('C01')
else:
    block_pipeline('C00')
```

## 2. 文件边界

### 可修改（白名单）
- `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/parallel_plan.md`
- `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/contracts/vk_cards.json`
- `workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/reports/preflight_status.json`

### 禁止修改（黑名单）
- `workdocs/归档/正文/实施计划/openclaw迁移重建基线_implementation_plan.md`

## 3. 状态与契约

- 可写字段: `preflight_status`, `active_task`
- 只读字段: `task_key`, `card_order`, `gate_contract`
- 外部契约: `planning_contract` 与 `vk_cards.json` 一致

## 4. 实施步骤

1. 对照 implementation_plan 校验 card_order/depends_on/gate_contract。
2. 校验 feature/card/pr 双向映射完整性。
3. 写入 preflight_status 并执行 active_task 绑定。

## 5. 测试与验收

- 最小测试集:
- `python3 scripts/docs_guard.py --strict`
- 验收标准:
- planning_contract 与 vk_cards 一致
- active_task 作用域字段完整

## 6. 风险与回滚

- 主要风险: 契约字段遗漏、task_key 漂移、active_task 绑定错误
- 回滚点:
- 停止 /jjk-vktodo，回退拆解产物
- 回滚开关/策略: 仅回滚当前拆解目录，禁止跨专题回退

## 7. card_export（机读，必填）

```yaml
card_export:
  id: WS-00
  feature_id: C00-01
  card_key: PP-20260301-CHAT-RUN-STOP::WS-00
  title: C00 预检门禁冻结
  type: foundation
  task_mode: inspection-card
  merge_required: false
  execution_mode: serial
  lane: lane-foundation
  hard_depends_on: []
  soft_depends_on: []
  depends_on: []
  file_whitelist:
    - workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/parallel_plan.md
    - workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/contracts/vk_cards.json
    - workdocs/任务拆解/2026-03-01_聊天断页续跑与强停止/reports/preflight_status.json
  acceptance_checks:
    - python3 scripts/docs_guard.py --strict
  rollback_anchors:
    - 停止 /jjk-vktodo，回退拆解产物
```
