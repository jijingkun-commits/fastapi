# 批判性审查再评估报告（v2）：自动化大型任务开发设计方案

> 审查人：critic agent
> 审查日期：2026-02-27
> 审查对象：`docs/内部参考/迭代需求/自动化大型任务开发设计方案.md`（~2312 行，已应用 15 项修正）
> 对照文档：
> - `.omc/research/eval-critique.md`（上一轮批判性审查，7.5/10）
> - `.omc/research/eval-code-verified.md`（上一轮代码实证评估，7.2/10）
> 评估方法：逐项复查上一轮全部发现，验证修正效果，检查新引入问题

---

## 总体可行性评分：8.6 / 10（上一轮 7.5/10，+1.1）

**评价**：15 项修正整体质量高，两个 CRITICAL 级问题（端口错误 + 认证缺失）已彻底修复，文件清单从 5 个扩展到 16+7 个，payload 迁移新增 31 条逐条对照 checklist。方案从"方向正确但核心前提悬空"提升为"核心前提已验证、可直接进入 P0 实施"。剩余扣分项集中在两处遗留不一致（Ch 7.5 的 `eval` 残留、`consecutiveErrors` 数据源未明确）和系统级 crontab 兜底仍缺具体配置。

---

## 一、上一轮批判性审查（eval-critique.md）逐项复查

### CRITICAL 级发现

#### C1. OpenClaw Gateway 端口与认证方式完全未验证 → ✅ 已修复（质量：优秀）

**修复位置与内容**：
- Ch 4.2 L342-343：`hooks_gateway_url` 默认值更正为 `http://localhost:18789`，新增 `hooks_token` 字段，含完整代码证据引用（`hooks.ts:158-175`、`server-http.ts:253-291`）
- Ch 5.4 L606：`hooks_gateway` 参数统一为 `os.getenv("OPENCLAW_GATEWAY", "http://localhost:18789")`
- Ch 9.2 L1437-1438：`OPENCLAW_GATEWAY` 和 `OPENCLAW_HOOKS_TOKEN` 均从环境变量读取
- Ch 14.1 Q1：状态更新为"已确认"，含完整代码证据
- Ch 14.2 L1894-1935：前置验证脚本新增 `verify_hooks_token()` 步骤，所有 HTTP 调用携带 Bearer Token
- Ch 14.3 ADR-008：新增端口和认证方式确认的决策记录
- Ch 12.1 R12：新增 token 配置错误风险项

**验证**：全文搜索 `42800` 仅出现在审查记录的历史引用中（ADR-008 说明原值为错误值），不再出现在任何代码示例或配置中。`18789` 出现 10 处，覆盖所有关键位置。`Bearer`/`OPENCLAW_HOOKS_TOKEN` 出现 20 处，认证逻辑完整贯穿。

**评价**：修复彻底。不仅更正了端口值，还建立了完整的认证链路（环境变量 → header 构造 → 前置验证 → 风险矩阵 → ADR 记录），形成闭环。

#### C2. 3000 字符 Payload 迁移缺乏逐条对照验证机制 → ✅ 已修复（质量：优秀）

**修复位置与内容**：
- 附录 B.4（L2036-2104）：新增完整的迁移对照 checklist，共 31 条：
  - B.4.1 固定真理源路径声明：P-01 ~ P-05（5 条）
  - B.4.2 硬顺序执行协议：E-01 ~ E-06（6 条）
  - B.4.3 分支决策矩阵：D-01 ~ D-07（7 条）
  - B.4.4 硬约束清单：C-01 ~ C-10（10 条）
  - B.4.5 输出格式模板：O-01 ~ O-03（3 条）
- B.4.6 迁移验收标准：4 条判定条件（全部标记已迁移 + 验证方式通过 + payload 不超 500 字符 + A/B 对比测试）

**评价**：修复质量超出预期。每条 checklist 包含"原约束摘要 → 迁移目标文件 → 迁移后位置 → 验证方式 → 状态"五列，可直接作为 P2 阶段的执行清单。验收标准中的"至少 3 个典型场景 A/B 对比测试"为行为等价性提供了可操作的验证手段。

### HIGH 级发现

#### H1. 仓内文件改造清单严重不完整（5 个 vs 21 个） → ✅ 已修复（质量：良好）

**修复位置**：Ch 10.1（L1540-1559）

**修复内容**：仓内修改文件从 5 个扩展为 16 个，覆盖 VK 报告 3.4.2 节核心文件，每个文件标注 Phase 归属。新增文件包括 `.cursor/commands/jjk-vkplan.md`、`.cursor/commands/jjk-vktodo.md`、`CLAUDE.md`、`.cursor/rules/mcp-routing.mdc` 等。

**评价**：覆盖率从 24%（5/21）提升到 76%（16/21）。Ch 10.1 注释说明"其余 5 个为 docs 内部参考文档的小幅引用清理，可在 P3 阶段批量处理"，这是合理的优先级划分。但建议在 P3 阶段补充这 5 个文件的具体清单，避免遗漏。

#### H2. 可删除文件清单不完整（1 个 vs 7 个） → ✅ 已修复（质量：良好）

**修复位置**：Ch 10.4（L1585-1593）

**修复内容**：可删除文件从 1 个扩展为 7 个，覆盖 VK 报告 3.4.4 节全部文件（4 个 `vk_*.sh` 脚本 + `jjk-vksync.md` + `.mcp.json` VK 条目 + `coder4_cron_state.json`），标注为 P3 阶段执行。

**评价**：完整覆盖，修复到位。

#### H3. wt-flow.sh verify 子命令存在命令注入风险 → ⚠️ 部分修复

**修复位置**：Ch 6.4（L849-895）

**修复内容**：
- 新增 `ALLOWED_PREFIXES` 白名单（`pytest`、`ruff`、`grep`、`python`、`bash`、`cat`、`jq`、`wc`、`test`、`diff`）
- 命令前缀校验：不在白名单中的命令被 BLOCKED
- `eval "$check"` 替换为 `bash -c "set -euo pipefail; $check"`

**剩余缺陷**：Ch 7.5（L1249）的 `verify_in_worktree()` 函数仍使用 `eval "$check"` 且无白名单校验。Ch 6.4 和 Ch 7.5 描述的是同一功能（在 worktree 中执行 acceptance_checks），但安全措施不一致。Ch 6.4 是修复后的版本，Ch 7.5 是修复前的残留。

**确定性**：确定存在。L1249 明确写着 `eval "$check"`，无白名单保护。

**建议**：Ch 7.5 的 `verify_in_worktree()` 应与 Ch 6.4 的 `verify()` 保持一致，使用 `bash -c` + 白名单校验，或标注 Ch 7.5 为"简化示意，完整实现见 Ch 6.4"。

#### H4. 并行模式设计深度不足 → ✅ 已修复（质量：优秀）

**修复位置**：Ch 6.7.1（L985-1025）、Ch 6.7.2（L1027-1054）、Ch 6.7.3（L1055-1063）

**修复内容**：
1. **并发写入保护**（6.7.1）：bash 侧使用 `flock` 文件锁，Python 侧使用 `fcntl.flock`，含完整代码示例和超时处理
2. **parallel-merge 冲突处理**（6.7.2）：完整 mermaid 流程图 + 4 条冲突处理规则（已合并不回滚、冲突卡标记 blocked、级联阻塞、无依赖卡继续）
3. **hooks 触发策略**（6.7.3）：4 种事件的触发方式表（单卡完成不触发、全部完成触发一次、冲突触发 agent 通知、all_done 触发一次）

**评价**：三个子问题均有完整设计，从 20 行描述扩展为约 80 行含代码和流程图的详细设计。质量显著提升。

#### H5. 单点故障从 VK 转移到 OpenClaw Gateway → ⚠️ 部分修复

**修复位置**：Ch 12.1 R1（L1712）

**修复内容**：R1 缓解措施提到"`--local-mode` 可通过系统 crontab 独立调度"。

**剩余缺陷**：仍缺少系统级 crontab 的具体配置（crontab 表达式、触发脚本路径、与 OpenClaw cron 的互斥机制）。R1 仅一句话提及，无实施细节。

**确定性**：确定缺失。全文搜索 `crontab` 仅在 R1 出现一次，无具体配置。

**建议**：在 Ch 9 或 Ch 12 中补充系统 crontab 兜底的具体配置示例，至少包含：(1) crontab 表达式（如 `*/30 * * * *`）；(2) 触发脚本（`python3 scripts/coder4_bootstrap_kernel.py --local-mode`）；(3) 与 OpenClaw cron 的互斥说明（两者可共存，系统 crontab 仅在 OpenClaw 不可用时实际执行有效操作）。

### MEDIUM 级发现

#### M1. Ch 5.4 与 Ch 9.2 的 Gateway 默认值来源不一致 → ✅ 已修复

**修复位置**：Ch 5.4 L606、Ch 9.2 L1437

**修复内容**：全文统一为 `os.getenv("OPENCLAW_GATEWAY", "http://localhost:18789")`。

**评价**：修复彻底，两处代码示例完全一致。

#### M2. Ch 6.4 verify 子命令的 glob 路径在 bash 中不可用 → ✅ 已修复

**修复位置**：Ch 6.4 L831-833

**修复内容**：改为从 `_active_task.json` 读取 `task_key` 构造确定性路径：`local task_key=$(jq -r '.task_key' docs/内部参考/任务拆解/_active_task.json)` + `local cards_file="docs/内部参考/任务拆解/${task_key}/vk_cards.json"`。

**评价**：修复正确。路径构造逻辑清晰，且增加了文件存在性校验。

#### M3. Telegram ID 硬编码在代码示例中 → ✅ 已修复

**修复位置**：Ch 9.3 L1473

**修复内容**：`TELEGRAM_TO = os.getenv("CODER4_TELEGRAM_TO", "<TELEGRAM_USER_ID>")`，默认值改为占位符。

**评价**：修复到位，PII 已移除。与 ADR-005 决策一致。

#### M4. 工时估算差异未充分解释 → ✅ 已修复（质量：良好）

**修复位置**：Ch 13.3 L1868-1874

**修复内容**：新增差异分解表，列出 3 项差异来源：
1. P2 仓外文件重写（4-5 天 vs ~2-3 天）：评审报告未充分计入 WORKFLOW_AUTO（460 行）和 VK_AGENT_PROMPTS（628 行）的逐条重写工作量
2. 端到端验收（1-2 天 vs 未单独估算）：本方案新增 A/B 对比验证环节
3. P1 wt-flow.sh 扩展（1.5 天 vs ~0.5 天）：代码实证确认从"小改"变为"中等改造"

**评价**：差异分解清晰，每项都有具体数据支撑。第 3 项引用了代码实证报告 H6，体现了跨报告的修正闭环。

#### M5. Schema 版本升级策略未定义 → ✅ 已修复（质量：良好）

**修复位置**：Ch 5.2.1（L504-526）

**修复内容**：新增完整的版本升级策略表：
- Patch（`1.0.x`）：新增可选字段，向后兼容，自动加载
- Minor（`1.x.0`）：新增必填字段，同 major 内兼容，自动迁移
- Major（`x.0.0`）：不兼容，提供迁移脚本

含 `load_local_card_status()` 代码示例，展示 major 版本校验和 minor 版本自动迁移逻辑。

**评价**：设计合理，覆盖了三种版本变更类型。`_auto_migrate_minor()` 函数虽未展开实现，但接口定义清晰，P1 阶段实现时有足够指引。

#### M6. attempt 文件的清理策略缺失 → ✅ 已修复（质量：良好）

**修复位置**：Ch 7.1.1（L1116-1135）

**修复内容**：
1. 按 `task_key` 隔离：`.omc/state/attempts/<task_key>/<card_id>/`
2. 保留最近 20 个 attempt 文件，超出自动清理
3. 任务完成后归档到 `.omc/archive/`
4. 磁盘空间监控：超过 100MB 时 Telegram 告警
5. 含 `cleanup_old_attempts()` 代码示例

**评价**：四项清理规则覆盖了上一轮提出的三个子问题（何时清理、磁盘监控、跨任务隔离）。设计完整。

### LOW 级发现

#### L1. Ch 6.3 next 子命令的 jq 原子写入缺少空输出校验 → ✅ 已修复

**修复位置**：Ch 6.3 L811-818

**修复内容**：新增 `[[ -s "$state_file.tmp" ]]` 非空校验，jq 输出为空时不执行 mv，输出错误信息并清理临时文件。

**评价**：修复到位。

#### L2. `/hooks/wake` 幂等性描述需补充条件 → ✅ 已修复

**修复位置**：Ch 3.2 L234

**修复内容**：幂等性描述更新为"是（重复调用安全，250ms coalesce 窗口）"，明确了 coalesce 机制。

**评价**：语义清晰，说明了重复调用在 250ms 窗口内会被合并。

#### L3. `consecutiveErrors >= 3` 阈值来源不明 → ⚠️ 未修复

**位置**：Ch 9.3 L1469

**问题**：`consecutiveErrors >= 3` 仍作为异常恢复触发条件，但 `task-runner-state.json` Schema（Ch 4.2）中没有 `consecutiveErrors` 字段。该字段的数据来源仍未明确——是从 OpenClaw cron state 读取？还是需要新增到 Schema 中？

**确定性**：确定未修复。Ch 4.2 字段说明表中无此字段。

**影响**：LOW。该字段仅用于异常恢复的特殊场景（`/hooks/agent` 精准控制路径），不影响主流程。但实施时需要明确数据来源。

#### L4. 附录 A.3 VK API 调用统计口径不一致 → ✅ 已修复

**修复位置**：附录 A.3 L1982

**修复内容**：统计注释更新为"~50 处（仅统计 prompt/规则中的间接引用；VK 报告 3.1 节统计全类型引用约 156 处，评审报告修正为约 203 处）"，明确标注了统计口径差异。

**评价**：修复到位，口径差异说明清晰。

#### L5. Ch 14.1 Q6 状态未关闭 → ✅ 已修复

**修复位置**：Ch 14.1 L1889

**修复内容**：Q6 状态更新为"已解决（见 Ch 7.3-7.4）"。

**评价**：修复到位。

---

## 二、上一轮代码实证报告（eval-code-verified.md）逐项复查

### CRITICAL 级发现

#### C1. Gateway 默认端口错误（42800 → 18789） → ✅ 已修复

与 eval-critique C1 同源。全文 `42800` 已替换为 `18789`，10 处引用全部更正。详见上文 C1 复查。

#### C2. 认证方式缺失（Bearer Token 必填） → ✅ 已修复

与 eval-critique C1 同源。所有 `httpx.post()`/`requests.post()` 调用已增加 `headers=_hooks_headers()`，前置验证脚本新增 `verify_hooks_token()` 步骤。详见上文 C1 复查。

#### C3. `/hooks/wake` 的 mode 参数语义不完整 → ✅ 已修复

**修复位置**：Ch 3.2 L228

**修复内容**：触发延迟描述更新为"零（`mode: "now"`）或下一次 heartbeat（`mode: "next-heartbeat"`）"，适用场景补充了 `"next-heartbeat"` 用于低优先级通知（如 VK 只读同步）。

**评价**：修复到位。`"next-heartbeat"` 模式的适用场景说明合理。

### HIGH 级发现

#### H1. coder4_task_ledger.jsonl 为空文件，证据链断裂 → ❌ 未修复（非方案缺陷）

**说明**：这是当前运行时状态的事实发现，不是设计方案的缺陷。设计方案的 attempt 系统（Ch 7）和台账设计（Ch 10.3 #13）是对未来的规划，不需要修改历史数据。G01 验收的证据问题属于当前任务收口范畴，不在本设计方案的修正范围内。

**评价**：不计入修正评估。

#### H2. coder4 主执行 cron job 不在当前 jobs.json 中 → ✅ 已修复

**修复位置**：Ch 8.4 L1341

**修复内容**：新增路径确认说明，列出三个可能路径（`~/.openclaw/cron-store.json`、`~/.openclaw/cron/jobs.json`、`~/.openclaw-dev/cron/jobs.json`），并给出排查指引（检查 `openclaw.json` 的 `cron.store` 配置字段或查看启动日志）。明确标注"P0 阶段第一步应确认实际路径"。

**评价**：修复合理。虽未给出确定路径（需运行时确认），但排查指引清晰，P0 阶段可操作。

#### H3. heartbeat.prompt 是配置字段而非文件 → ✅ 已修复（质量：优秀）

**修复位置**：Ch 8.4.1（L1369-1385，新增章节）

**修复内容**：新增完整的优先级链表（4 级：EXEC_EVENT_PROMPT > buildCronEventPrompt > heartbeat.prompt 配置字段 > HEARTBEAT.md 文件），明确 3000 字符约束应写入 `heartbeat.prompt` 配置字段（优先级 3），附录 B.2 迁移策略同步修正。

**评价**：新增章节质量高，优先级链清晰，对设计方案的影响分析到位。

#### H4. /hooks/agent 返回 202 异步执行 → ✅ 已修复

**修复位置**：Ch 9.3 L1476-1481、Ch 14.2 L1920-1929

**修复内容**：
- `trigger_agent_turn()` 函数注释明确标注"返回 202 异步执行，无同步等待 API"
- 给出结果获取策略："调用方应通过轮询 task-runner-state.json 或等待下一次 heartbeat 获取结果"
- 前置验证脚本 `verify_hooks_agent()` 检查 202 状态码和 `runId` 字段

**评价**：修复到位。异步语义已明确，结果获取策略虽简略但可行（heartbeat 自然轮询）。

#### H5. Transcript pruning 会话重置策略不够具体 → ✅ 已修复（质量：良好）

**修复位置**：Ch 12.1 R5（L1716）

**修复内容**：R5 缓解措施大幅扩展，包含：
1. 代码实证引用（`heartbeat-runner.ts:386-403`）
2. 具体重置策略：每完成 3 张卡片后切换会话键（`coder4-batch-{N}`）
3. 重置时保留 `task-runner-state.json` 和 `_active_task.json` 作为上下文恢复源
4. 重置前将关键决策记录追加到 `coder4_task_ledger.jsonl`

**评价**：从"使用隔离会话 + 定期重置"的笼统描述，提升为含具体频率、保留策略和恢复机制的完整方案。

#### H6. wt-flow.sh 新增子命令工作量被低估 → ✅ 已修复

**修复位置**：Ch 13.3 L1874

**修复内容**：工时差异分解表中明确标注"P1 wt-flow.sh 扩展（1.5 天 vs ~0.5 天）：代码实证确认从'小改'变为'中等改造'"。

**评价**：工时估算已调整，引用了代码实证报告作为依据。

### MEDIUM 级发现

#### M1. bootstrap_kernel.py 的 VK API 调用统计偏少 → ✅ 已修复

**修复位置**：附录 A.1（L1957-1963）

**修复内容**：VK API 调用从 3 处扩展为 4 处（新增 L410+ dispatch 的 PATCH 调用），附录 A.3 统计同步更新为"写入（POST/PATCH）3 处（L382 seed + L396 activate + L410 dispatch）"。

**评价**：修复到位。注意 A.3 统计仍写"3 处"是因为 L231 的 GET 单独计为读取 1 处，写入确实是 3 处 POST/PATCH，总计 4 处 HTTP 调用。数据自洽。

#### M2. _active_task.json ALL_DONE 边界条件 → ✅ 已修复（质量：良好）

**修复位置**：Ch 5.2.2（L528-562，新增章节）

**修复内容**：
1. 定义了三种状态组合及预期行为（ALL_DONE + 无未完成 → SCOPE_COMPLETE；ALL_DONE + Gate 未完成 → 过渡状态；非 ALL_DONE → 正常执行）
2. 含 `scope_guard_check()` 代码示例，区分 `pending_gates` 和 `pending_impl`
3. 过渡状态返回 `GATE_PHASE`，允许 Gate 卡进入执行流

**评价**：新增章节直接回应了代码实证报告 M2 的发现，设计合理。

#### M3. OpenClaw 配置使用 YAML/JSON 双格式 → ⚠️ 部分修复

**修复位置**：Ch 8.4 L1341

**修复内容**：提到"检查 `~/.openclaw/openclaw.json`（或 `config.yaml`）中的 `cron.store` 配置字段"，承认了双格式的存在。

**剩余缺陷**：仅在 cron 路径确认处提及，其他引用 OpenClaw 配置的位置（如 Ch 8.4.1 的 `heartbeat.prompt` 配置修改路径）未统一说明格式。

**影响**：LOW。实际操作时检查文件扩展名即可确定格式。

#### M4. cron 存储路径与设计方案假设不一致 → ✅ 已修复

与 H2 同源。Ch 8.4 已补充多路径排查指引。

---

## 三、新发现（修正过程中引入的问题）

### N1. Ch 7.5 verify_in_worktree() 与 Ch 6.4 verify() 安全措施不一致（MEDIUM）

**位置**：Ch 7.5 L1249 vs Ch 6.4 L849-895

**问题**：Ch 6.4 的 `verify()` 已修复为 `bash -c` + 白名单校验，但 Ch 7.5 的 `verify_in_worktree()` 仍使用 `eval "$check"` 且无白名单保护。两处描述的是同一功能（在 worktree 中执行 acceptance_checks），但安全措施不一致。

**影响**：MEDIUM。如果实施者参考 Ch 7.5 而非 Ch 6.4，将引入命令注入风险。

**建议**：在 Ch 7.5 代码块前增加注释"简化示意，完整安全实现见 Ch 6.4 verify()"，或将 Ch 7.5 的代码同步更新为 `bash -c` + 白名单。

### N2. Ch 9.3 consecutiveErrors 字段数据源仍未明确（LOW）

**位置**：Ch 9.3 L1469

**问题**：上一轮 L3 发现的延续。`consecutiveErrors >= 3` 作为异常恢复触发条件，但 `task-runner-state.json` Schema 中无此字段。修正过程未处理此问题。

**影响**：LOW。仅影响异常恢复的特殊路径。

**建议**：在 Ch 4.2 Schema 中新增 `consecutive_errors` 字段（integer, 可选, 默认 0），或在 Ch 9.3 中标注"此字段从 OpenClaw cron state 读取，非本地 Schema 字段"。

### N3. Ch 10.5 改造量统计表与 Ch 10.1-10.4 不完全一致（LOW）

**位置**：Ch 10.5 L1611

**问题**：统计表显示"修改文件数 合计 9"，但 Ch 10.1 列出 16 个仓内修改文件 + Ch 10.2 列出 5 个仓外修改文件 = 21 个。统计表可能仅计入了 P1 阶段的核心文件，但未标注统计口径。

**影响**：LOW。不影响实施，但可能造成工作量误判。

**建议**：更新统计表或标注"此处仅统计核心代码文件，不含文档小改"。

### N4. 审查 3 修正记录的自评分可能偏乐观（观察项）

**位置**：审查 3 L2307-2312

**问题**：修正后自评分预估为"代码实证 7.2→8.5、批判性审查 7.5→8.5"。本次独立复查认为批判性审查维度的提升幅度合理（7.5→8.6），但代码实证维度的 H1（ledger 为空）和 H2（cron 路径不确定）属于运行时事实，设计方案只能"标注"而非"修复"，8.5 分略偏乐观，8.0-8.3 更为客观。

**影响**：无实际影响，仅为评分校准。

---

## 四、假设验证表更新

| # | 隐含假设 | 上一轮状态 | 本轮状态 | 变更说明 |
|---|---------|-----------|---------|---------|
| A1 | Gateway 在 `localhost:18789` 监听 | 未验证 | ✅ 已验证 | 代码实证确认 `types.gateway.ts:280`，设计方案全文已更正 |
| A2 | `/hooks/wake` 和 `/hooks/agent` 需要 Bearer Token | 未验证 | ✅ 已验证 | 代码实证确认 `hooks.ts:158-175`，设计方案已增加完整认证链路 |
| A3 | `heartbeat.prompt` 支持 3000 字符注入 | 未验证 | ⚠️ 已澄清路径 | Ch 8.4.1 明确了优先级链，但字段长度限制仍待 P0 实测（Ch 14.1 Q3） |
| A4 | OpenClaw Codex 执行超时后状态可恢复 | 未验证 | ✅ 成立 | 代码实证确认 `timeoutSeconds` 参数支持 |
| A5 | `os.rename()` 在 macOS APFS 上是原子操作 | 成立 | ✅ 成立 | 无变化 |
| A6 | `jq` 在所有目标环境中可用 | 需确认 | ⚠️ 需确认 | 无变化。建议 P0 阶段前置检查 `which jq` |
| A7 | 单一开发者使用，无并发写入 | 成立 | ✅ 成立 | 无变化。Ch 6.7.1 额外增加了 flock 保护作为防御性设计 |

---

## 五、边界条件覆盖更新

| # | 边界条件 | 上一轮状态 | 本轮状态 | 变更说明 |
|---|---------|-----------|---------|---------|
| B1 | `task-runner-state.json` 写入中断 | 覆盖 | ✅ 覆盖 | 无变化，Ch 4.6 原子写入设计充分 |
| B2 | `vk_cards.json` 格式错误 | 覆盖 | ✅ 覆盖 | 无变化 |
| B3 | `_active_task.json` 字段缺失 | 覆盖 | ✅ 覆盖 | 无变化 |
| B4 | hooks 调用网络超时 | 覆盖 | ✅ 覆盖 | 无变化，Ch 11.4 降级链完整 |
| B5 | 磁盘空间不足 | **未覆盖** | ✅ 覆盖 | Ch 7.1.1 新增 attempt 清理策略 + 100MB 告警 |
| B6 | 并发写入 `task-runner-state.json` | **未覆盖** | ✅ 覆盖 | Ch 6.7.1 新增 flock/fcntl 文件锁保护 |
| B7 | worktree 创建失败 | 部分覆盖 | 部分覆盖 | 无变化，Ch 11.1 可重试但无具体恢复步骤 |
| B8 | OpenClaw 进程崩溃 | **未覆盖** | ⚠️ 部分覆盖 | R1 提及系统 crontab 兜底但缺具体配置 |
| B9 | `card_order` 为空数组 | **未覆盖** | **未覆盖** | 仍未处理。`decide_action()` 对空 card_order 的行为未定义 |
| B10 | `schema_version` 不兼容 | 部分覆盖 | ✅ 覆盖 | Ch 5.2.1 新增完整版本升级策略（Patch/Minor/Major） |
| B11 | `_active_task` ALL_DONE + Gate 未完成 | **未覆盖**（新增） | ✅ 覆盖 | Ch 5.2.2 新增过渡状态处理 |

**覆盖率变化**：上一轮 5/10 覆盖（50%） → 本轮 8/11 覆盖（73%），提升显著。

---

## 六、修正效果汇总与新评分

### 修正效果统计

| 严重程度 | 上一轮数量 | 已修复 | 部分修复 | 未修复 | 修复率 |
|---------|----------|--------|---------|--------|--------|
| CRITICAL（critique） | 2 | 2 | 0 | 0 | 100% |
| CRITICAL（code-verified） | 3 | 3 | 0 | 0 | 100% |
| HIGH（critique） | 5 | 3 | 2 | 0 | 60%（含部分修复 100%） |
| HIGH（code-verified） | 6 | 5 | 0 | 1（非方案缺陷） | 83% |
| MEDIUM（critique） | 6 | 6 | 0 | 0 | 100% |
| MEDIUM（code-verified） | 4 | 3 | 1 | 0 | 75% |
| LOW（critique） | 5 | 4 | 0 | 1 | 80% |

### 新引入问题

| # | 严重程度 | 问题 | 影响 |
|---|---------|------|------|
| N1 | MEDIUM | Ch 7.5 eval 残留与 Ch 6.4 安全措施不一致 | 实施时可能参考错误版本 |
| N2 | LOW | consecutiveErrors 数据源未明确 | 仅影响异常恢复路径 |
| N3 | LOW | Ch 10.5 统计表与实际文件数不一致 | 不影响实施 |
| N4 | 观察项 | 自评分略偏乐观 | 无实际影响 |

### 评分对比

| 维度 | 上一轮（eval-critique） | 本轮 | 变化 | 说明 |
|------|----------------------|------|------|------|
| 核心前提可靠性 | 5/10（端口/认证未验证） | 9/10 | +4 | 两个 CRITICAL 彻底修复，ADR-008 记录决策 |
| 文件清单完整性 | 5/10（5/21 文件） | 8/10 | +3 | 16+7 文件覆盖，Phase 归属清晰 |
| Payload 迁移可验证性 | 4/10（无 checklist） | 9/10 | +5 | 31 条逐条对照 + 4 条验收标准 |
| 并行模式设计深度 | 4/10（20 行描述） | 8/10 | +4 | flock + 冲突流程图 + hooks 触发策略 |
| 边界条件覆盖 | 5/10（50%） | 7/10 | +2 | 73% 覆盖，B9 仍缺 |
| 降级路径完整性 | 6/10（缺系统 crontab） | 7/10 | +1 | R1 提及但缺具体配置 |
| 数据自洽性 | 8/10 | 9/10 | +1 | 工时差异分解、统计口径标注 |
| 前置文档吸收度 | 8/10 | 9/10 | +1 | 代码实证报告的发现也已充分吸收 |

### 总体评分

**8.6 / 10**（上一轮 7.5/10，+1.1）

**评分依据**：
- 两个 CRITICAL 级问题（端口 + 认证）彻底修复，hooks-first 架构的核心前提从"悬空"变为"已验证"，这是最大的质量提升
- 31 条 payload 迁移 checklist 使 P2 阶段从"凭经验迁移"变为"逐条核对"
- 并行模式从"概念描述"提升为"含代码和流程图的完整设计"
- 扣分项：Ch 7.5 eval 残留（-0.3）、系统 crontab 兜底缺具体配置（-0.3）、B9 card_order 空数组未处理（-0.2）、consecutiveErrors 数据源不明（-0.1）、Ch 10.5 统计不一致（-0.1）

**与上一轮的关键差异**：上一轮的核心风险是"方案核心前提（端口/认证）未验证，可能导致 hooks-first 架构完全不可行"。本轮该风险已消除。剩余问题均为实施细节层面（代码示例不一致、兜底配置缺失、边界条件遗漏），不影响架构方向的正确性。方案已具备进入 P0 实施的条件。

---

## 七、剩余改进建议（优先级排序）

| 优先级 | 建议 | 对应发现 | 工作量 |
|--------|------|---------|--------|
| P0 前 | Ch 7.5 `verify_in_worktree()` 同步 Ch 6.4 安全措施或标注"简化示意" | N1 | 10 分钟 |
| P0 前 | Ch 9.3 明确 `consecutiveErrors` 数据源（新增到 Schema 或标注来源） | N2/L3 | 10 分钟 |
| P0 中 | Ch 12.1 R1 补充系统 crontab 兜底的具体配置 | H5 | 30 分钟 |
| P1 前 | Ch 5.2 补充 `card_order` 为空数组时 `decide_action()` 的行为定义 | B9 | 15 分钟 |
| P1 中 | Ch 10.5 统计表与 Ch 10.1-10.4 对齐 | N3 | 10 分钟 |

**总工作量**：约 1.5 小时，均为文档层面的补充，不涉及架构调整。

---

*再评估完成。总体评分从 7.5 提升至 8.6（+1.1），15 项修正中 13 项完全修复、2 项部分修复，新引入 1 个 MEDIUM 级问题（Ch 7.5 eval 残留）。方案已具备进入 P0 实施的条件，剩余 5 项改进建议可在实施过程中同步完成。*
