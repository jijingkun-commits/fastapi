# 问数图表“10 条只画 9 根柱”修复计划

## 1. 问题摘要

- 问题描述：Top10 客户查询返回 `10` 条明细，但图表只显示 `9` 根柱。
- 根本原因：
  1. 图表 X 轴使用“客户名称”；当客户名称为空时，后端会统一序列化为 `"未知"`（`app/ai/workflow/data_graph.py`）。
  2. 前端 Vega-Lite 柱状图按 X 轴类目聚合/叠合，同名类目会合并为同一柱（`web/src/components/chat/messages/sql-result-chart.tsx`）。
  3. 本次数据中至少两条记录客户名称为空（表格显示为 `-`），因此被压缩成同一个“未知”类目。
- 影响范围：
  - 问数 `sql_result.chart` 柱状图/折线图（X 轴维度存在重复或空值时）。
  - TopN 排名类查询的可视化可信度（图与表不一致）。
- 严重程度评级：P1（结果可读性和业务信任受损，核心数据未丢失）。

## 2. 修复方案

### 方案概述

在后端构建图表载荷时增加“维度唯一性兜底”，保证每行结果在图上都可被唯一表达；同时优化轴字段判定，避免“客户编号”这类标识列被误判为纯数值指标而无法作为兜底维度。

### 涉及文件

#### `app/ai/workflow/data_graph.py`
- [ ] 修改点 1：新增“标识列识别”能力（如 `id/no/编号/客户号/ecif_cust_no`），避免把标识列一律当作指标列。
- [ ] 修改点 2：在 `_pick_chart_axes` 中允许标识列作为候选维度（用于冲突兜底）。
- [ ] 修改点 3：在 `_build_sql_result_chart_payload` 中检测 X 轴重复类目；若重复，优先拼接唯一标识（如 `客户名称（客户编号）`），确保 10 条 -> 10 个图元。
- [ ] 修改点 4：仅对冲突项做后缀补齐，避免正常类目视觉噪音。

#### `app/tests/test_data_graph_visualization.py`
- [ ] 修改点 1：新增“Top10 含重复/空客户名称”用例，断言 `chart.data` 中维度键唯一且条目数与明细一致。
- [ ] 修改点 2：新增“标识列为数字字符串”用例，断言轴选择仍可稳定输出“维度 + 指标”。

#### `web/src/components/chat/messages/sql-result-chart.tsx`
- [ ] 修改点 1：确认前端不会再次把后端已补齐的维度值归并为同一文案（必要时对占位值策略做最小调整）。
- [ ] 修改点 2：补充注释/保护逻辑，说明图表依赖后端传入的唯一维度键。

### 数据库变更

- 本次无需 DDL/DML。

### 配置变更

- 本次无需新增环境变量或系统配置。

## 3. 风险评估

- 可能引入的新问题：
  - 维度标签加后缀后，X 轴文字变长，移动端可读性下降。
  - 过度触发“唯一化”会让本应合并展示的场景被拆分。
- 需关注的边界：
  - 全部客户名称为空；
  - 多个维度列均可用时的优先级；
  - 饼图场景下是否应保持合并语义。
- 回滚方案：
  - 保留改动点在后端单函数（`_build_sql_result_chart_payload`）可快速回退；
  - 出现视觉噪音时先降级为“仅冲突项编号后缀”。

## 4. 验证计划

- 单元测试：
  - `pytest app/tests/test_data_graph_visualization.py -k "duplicate or top10"`
- 集成测试：
  - 复现会话：`查询 2025-06-30 贷款余额前10名客户 -> 生成图表`；验证图表柱数=10，表格行数=10。
- 手工验证：
  1. 构造 2 条客户名称为空但客户编号不同的数据；
  2. 观察图上是否各自占一根柱；
  3. 核对 tooltip 中客户名与编号可追溯。
- 性能验证：
  - Top50 图表载荷构建耗时无显著上升（目标 < 5ms 级别函数开销）。

## 5. 预防措施

- 补测试覆盖：将“维度重复冲突”加入固定回归用例。
- 补日志：在图表构建时记录“维度冲突数/是否启用兜底”。
- 规范改进：明确“展示维度键必须可唯一定位记录”的契约。
- 架构建议：将“图表轴选择+唯一化策略”抽离为独立 helper，避免散落在节点内。

## 6. 实施建议

- 修复优先级：高（建议本迭代立即处理）。
- 建议顺序：
  1. 后端轴选择与唯一化；
  2. 单元测试补齐；
  3. 前端最小兼容调整；
  4. 回归验证。
- 预计工作量：0.5 ~ 1 人日。
- 是否分阶段：可单阶段完成。

## 7. 文档关联检查

- [ ] 需求文档：`docs/产品文档/问数助手需求.md`
- [ ] 测试案例：`docs/开发文档/测试管理/问数引擎测试案例.md`
- [ ] API 文档：`docs/API文档/接口文档.md`（本次无接口结构变更）
- [ ] 数据库设计：`docs/开发文档/架构设计/数据库设计.md`（本次无库结构变更）
- [ ] 架构文档：`docs/开发文档/架构设计/AI模块设计.md`、`docs/开发文档/架构设计/前端架构.md`
