# 常用代码模式

> **用途**: 提供项目中常用的代码模式模板，确保 AI 生成的代码风格一致。

---

## 命令速查表

> 变更 `.cursor/commands/` 时必须同步更新此表。
> 触发方式差异：Claude Code / Cursor 使用 `/jjk-xxx`；Codex 使用 `/prompts:jjk-xxx`（由 `~/.codex/prompts/*.md` 提供）。

### 核心链路

| 命令 | 用途 | 典型上游 | 典型下游 |
|------|------|---------|---------|
| `/jjk-clarify` | 快速澄清需求 | 用户提问 | `/jjk-plan` |
| `/jjk-plan` | 正式规划（需求 + 技术方案） | `/jjk-clarify` | `/jjk-imp`、`/jjk-vkplan` |
| `/jjk-vkplan` | 并行拆解与看板落卡准备 | `/jjk-plan parallel` | `/jjk-vktodo` |
| `/jjk-vktodo` | 批量建卡到看板 | `/jjk-vkplan` | `/jjk-imp-ws` |
| `/jjk-imp` | 按计划编码 | `/jjk-plan`、`/jjk-pc` | `/jjk-review`、`/jjk-test` |
| `/jjk-imp-ws` | 按 WS 子任务编码 | `/jjk-vktodo` | `/jjk-review` |
| `/jjk-feature` | 一站式全流程 | 用户需求 | 自包含 |

### 问题排查链路

| 命令 | 用途 | 典型上游 | 典型下游 |
|------|------|---------|---------|
| `/jjk-pc`（别名 `/jjk-diagnose`） | 仅诊断，产出 `fix_plan_<topic>.md` | 问题报告 | `/jjk-imp` |
| `/jjk-debug` | 诊断 + 直接修复 | 问题报告 | `/jjk-review` |

### 质量保障

| 命令 | 用途 |
|------|------|
| `/jjk-verify` | 一站式验证（审查 + 测试 + 交互式 UAT） |
| `/jjk-review` | 代码审查 |
| `/jjk-test` | 完整测试流程 |
| `/jjk-lint` | 代码规范检查 |
| `/jjk-security-audit` | 安全审计 |
| `/jjk-deslop` | 清理 AI 冗余代码 |

### 辅助工具

| 命令 | 用途 |
|------|------|
| `/jjk-quick` | 小改动快速模式（<= 3 文件，跳过完整流程） |
| `/jjk-git-commit` | 规范化提交 |
| `/jjk-create-pr` | 创建 PR |
| `/jjk-refactor` | 代码重构 |
| `/jjk-api-docs` | 生成 API 文档 |
| `/jjk-doc-check` | 文档同步检查 |
| `/jjk-vksync` | VK 基线同步 |
| `/jjk-wtimp` | Worktree 隔离编码 |

---

## 🛠️ 后端模式

### 1. 添加新 Tool

**参考**: `app/ai/tools/todo_tools.py`

```python
"""工具模块（中文注释）。"""
from typing import Optional, List
from pydantic import BaseModel, Field
from langchain.tools import tool
from langchain_core.runnables.config import RunnableConfig


# ==================== Pydantic Schemas ====================

class MyToolInput(BaseModel):
    """工具输入参数。"""
    param1: str = Field(description="参数1说明")
    param2: Optional[int] = Field(default=None, description="可选参数2")


# ==================== Helper Functions ====================

def _get_user_id(config: RunnableConfig) -> Optional[int]:
    """从 config 中提取 user_id。"""
    return config.get("configurable", {}).get("user_id")


# ==================== Tools ====================

@tool(args_schema=MyToolInput)
def my_tool(
    param1: str,
    param2: int = None,
    config: RunnableConfig = None
) -> str:
    """工具描述（会显示给 LLM）。
    
    详细说明：什么场景使用此工具。
    """
    user_id = _get_user_id(config)
    
    # ... 业务逻辑 ...
    
    # 返回字符串格式，供 LLM 理解
    return "✅ 操作成功：xxx"
```

### 2. 添加新 Agent

**参考**: `app/ai/agents/todo_agent.py`

```python
"""Agent 模块（中文注释）。"""
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnableConfig


AGENT_PROMPT = ChatPromptTemplate.from_messages([
    ("system", """你是一个专业的 XXX 助手。

## 职责
- 职责1
- 职责2

## 工具使用规范
- 在操作前先用 list_xxx 了解当前状态
- 任务完成后用简洁的语言总结结果
"""),
    ("placeholder", "{messages}"),
])


def create_xxx_agent(llm):
    """创建 XXX Agent。"""
    from app.ai.tools.xxx_tools import tool1, tool2
    
    tools = [tool1, tool2]
    agent = AGENT_PROMPT | llm.bind_tools(tools)
    return agent
```

### 3. 注册新 Agent 到 Supervisor

**文件**: `app/ai/workflow/multi_agent_graph.py`

```python
# 1. 在 AgentType 枚举中添加
class AgentType(str, Enum):
    DATA = "data_expert"
    KNOWLEDGE = "knowledge_expert"
    TODO = "todo_expert"
    NEW_AGENT = "new_expert"  # ⬅️ 新增

# 2. 在 AGENT_DESCRIPTIONS 中添加描述
AGENT_DESCRIPTIONS = {
    # ...
    AgentType.NEW_AGENT: "将 XXX 任务分配给新专家",
}

# 3. 在 create_multi_agent_graph() 中注册节点
def create_multi_agent_graph(...):
    # 创建 Agent
    new_agent = create_xxx_agent(llm)
    
    # 添加节点
    graph.add_node("new_expert", _create_streaming_agent_wrapper(new_agent, "new_expert"))
    
    # 添加边 (Supervisor 已自动通过 handoff tools 处理)
```

### 4. 添加新 SSE 事件类型

**后端** (`app/ai/events.py`):

```python
# 1. 在 EventType 中添加
EventType = Literal[
    # ...
    "my_event",  # ⬅️ 新增
]

# 2. 添加发送函数
def emit_my_event(writer: StreamWriter, data: dict, node: str = "") -> None:
    """发送自定义事件。"""
    writer({
        "type": "my_event",
        "data": data,
        "node": node
    })
```

**前端** (`web/src/hooks/useSSEStream.ts`):

```typescript
// 1. 在 StreamCallbacks 接口中添加
onMyEvent?: (data: MyEventData) => void;

// 2. 在事件处理 switch 中添加
case "my_event":
  callbacks.onMyEvent?.(parsed.data);
  break;
```

### 5. 添加新 API 端点

**文件**: `app/api/v1/endpoints/xxx_api.py`

> **示例占位**：`xxx_api.py`、`xxx_schema.py`、`xxx_repository.py`、`/xxx` 仅用于模板演示；落地时请替换为真实业务名称与路由。

```python
"""XXX API 端点（中文注释）。"""
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.postgres_session import get_async_db
from app.schemas.xxx_schema import XxxCreate, XxxResponse
from app.repositories.xxx_repository import XxxRepository

router = APIRouter(prefix="/xxx", tags=["XXX"])


@router.post("", response_model=XxxResponse)
async def create_xxx(
    data: XxxCreate,
    db: AsyncSession = Depends(get_async_db)
):
    """创建 XXX。"""
    repo = XxxRepository()
    result = await repo.create(db, data)
    return result


@router.get("/{item_id}", response_model=XxxResponse)
async def get_xxx(
    item_id: int,
    db: AsyncSession = Depends(get_async_db)
):
    """获取 XXX。"""
    repo = XxxRepository()
    result = await repo.get_by_id(db, item_id)
    if not result:
        raise HTTPException(status_code=404, detail="未找到")
    return result
```

**注册路由** (`app/api/v1/router.py`):

```python
from app.api.v1.endpoints import xxx_api

api_router.include_router(xxx_api.router)
```

---

## 🎨 前端模式

### 1. 处理新 SSE 事件

**文件**: `web/src/hooks/useSSEStream.ts`

```typescript
// 在 startStream 函数的 switch 中添加
case "my_event":
  const myData = parsed.data as MyEventData;
  // 处理逻辑，例如更新消息状态
  updateMessage(msg => ({
    ...msg,
    myField: myData.value
  }));
  break;
```

### 2. 添加新聊天组件

**文件**: `web/src/components/chat/MyComponent.tsx`

> **示例占位**：`MyComponent.tsx`、`SomeData` 仅为模板占位，请按实际组件名与类型替换。

```tsx
"use client";

import { cn } from "@/lib/utils";

interface MyComponentProps {
  data: SomeData;
  className?: string;
}

export function MyComponent({ data, className }: MyComponentProps) {
  return (
    <div className={cn("p-4 rounded-lg bg-muted", className)}>
      {/* 组件内容 */}
    </div>
  );
}
```

### 3. 添加新 API 调用

**文件**: `web/src/lib/backend.ts`

```typescript
/**
 * 获取 XXX 数据
 */
export async function getXxx(id: number): Promise<XxxData> {
  const res = await apiFetch(`/api/v1/xxx/${id}`);
  if (!res.ok) {
    throw new Error("获取失败");
  }
  return res.json();
}

/**
 * 创建 XXX
 */
export async function createXxx(data: XxxCreate): Promise<XxxData> {
  const res = await apiFetch("/api/v1/xxx", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(data),
  });
  if (!res.ok) {
    throw new Error("创建失败");
  }
  return res.json();
}
```

---

## 🎯 高级模式

### 6. 双路径一致性（图片流式传输）

**场景**: 确保实时流和历史数据中的资源 URL 完全一致。

**模式**: 同一数据源，双路径分发 + 兜底修复。

```python
# 工具生成资源
@tool
def fig_inter(...):
    # 1. 生成唯一 URL
    proxy_url = asset_service.get_proxy_url(object_key)
    
    # 2. 路径 1: 实时流式发送
    try:
        writer = get_stream_writer()
        emit_result(writer, "image", {"url": proxy_url})
    except:
        pass  # 非流式上下文忽略
    
    # 3. 路径 2: 返回值（用于数据库持久化）
    return json.dumps({
        "status": "success",
        "image_url": proxy_url  # ⬅️ 同一个 URL
    })
```

```python
# 后端兜底修复（postprocess 节点）
def _postprocess(state):
    messages = state["messages"]
    
    # 从 ToolMessage 提取 image_url
    # 检查 AIMessage 是否包含
    # 如果缺失，自动追加
    fixed = fix_missing_image_links(messages)
    
    save_to_database(fixed)
```

```typescript
// 前端去重
const appendImageToAiMessage = (aiId: string, url: string) => {
    const displayedImages = message.additional_kwargs?.displayedImages || [];
    
    // 防止重复显示
    if (displayedImages.includes(url)) return;
    if (content.includes(url)) return;  // LLM 已输出
    
    // 追加并记录
    message.content += `\n\n![Image](${url})`;
    message.additional_kwargs.displayedImages = [...displayedImages, url];
};
```

**关键点**:
- ✅ URL 来源唯一（同一次函数调用）
- ✅ 实时流立即显示
- ✅ 数据库兜底保证
- ✅ 前端三重去重

---

## 🔀 Worktree 隔离开发模式

### 概述

通过 git worktree 实现每次代码修改的分支隔离，完成后自动合并回主分支。保证每次改动可追溯、可原子回滚。

### 工具

- 脚本: `scripts/wt-flow.sh`
- 命令: `/jjk-wtimp`

### 生命周期

```
master (干净) -> 创建 feature/<slug> 分支 + worktree
                    -> 在 worktree 中编码、测试、提交
                        -> rebase master (处理前进)
                            -> merge --no-ff 回 master
                                -> 清理 worktree + 分支
```

### 脚本用法

```bash
# 创建隔离环境
bash scripts/wt-flow.sh create <slug> [base-branch]
# 例: bash scripts/wt-flow.sh create 20260226-add-export-api

# 查看当前会话
bash scripts/wt-flow.sh status

# 合并回主分支并清理
bash scripts/wt-flow.sh merge

# 合并但保留 worktree
bash scripts/wt-flow.sh merge --no-cleanup

# 仅清理（不合并）
bash scripts/wt-flow.sh cleanup

# 检查是否在主分支上（用于 hook 安全网）
bash scripts/wt-flow.sh guard
```

### 分支命名规范

- 格式: `feature/<YYYYMMDD>-<关键词>`
- 例: `feature/20260226-add-export-api`

### 状态文件

会话状态保存在 `.omc/state/wt-flow-state.json`，包含分支名、worktree 路径、基准分支、创建时间。

### 与现有流程的关系

| 命令 | 说明 |
|------|------|
| `/jjk-wtimp` | worktree 隔离 + 编码（自动检测是否需要 team 并行） + 自动合并 |
| `/jjk-imp` | 直接在当前分支编码（不隔离） |
| `/jjk-imp-ws` | vibe-kanban worktree 子任务（独立机制） |
| `/jjk-feature` | 全流程一站式（不隔离） |

### Team 并行模式

当 `/jjk-wtimp` 检测到任务涉及多文件（>5）或多模块（>=2）时，自动建议切换到 team 并行模式：

- worktree 作为外层隔离，team 在 worktree 内并行编码
- teammates 不使用 `isolation: "worktree"`，避免嵌套
- teammates 按文件/模块划分职责，避免同文件冲突

### 异常处理

| 场景 | 行为 |
|------|------|
| master 有未提交变更 | 拒绝创建，提示先 commit 或 stash |
| 编码期间 master 前进 | merge 前自动 rebase，冲突时暂停并提示 |
| merge 冲突 | 自动 abort，保留 worktree，提示手动解决 |
| worktree 内有未提交变更 | 拒绝合并，提示先 commit |

---

## ⚠️ 禁止的反模式

### 1. 直接在 API 层操作数据库
```python
# ❌ 错误
@router.get("/users/{id}")
async def get_user(id: int, db: AsyncSession = Depends(get_async_db)):
    result = await db.execute(select(User).where(User.id == id))
    return result.scalar_one()

# ✅ 正确：通过 Repository 层
@router.get("/users/{id}")
async def get_user(id: int, db: AsyncSession = Depends(get_async_db)):
    repo = UserRepository()
    return await repo.get_by_id(db, id)
```

### 2. 在 Tool 中硬编码用户 ID
```python
# ❌ 错误
@tool
def my_tool():
    user_id = 1  # 硬编码

# ✅ 正确：从 config 获取
@tool
def my_tool(config: RunnableConfig = None):
    user_id = config.get("configurable", {}).get("user_id")
```

### 3. 前端使用 any 类型
```typescript
// ❌ 错误
const handleData = (data: any) => { ... }

// ✅ 正确：定义明确类型
interface MyData {
  id: number;
  name: string;
}
const handleData = (data: MyData) => { ... }
```

---

## 🆕 借鉴的开源设计模式

> 以下模式借鉴自 OpenAI Agents SDK、Flock、Anthropic Skills 等开源项目

### 1. Guardrails 护栏模式

**来源**: OpenAI Agents SDK `input_guardrails.py`

**文件**: `app/ai/guardrails.py`

```python
from app.ai.guardrails import guardrail_runner

# 在 preprocess 节点中使用
passed, content, reason = await guardrail_runner.validate_input(user_message)
if not passed:
    logger.warning("护栏拦截: %s", reason)
```

**添加自定义护栏**:

```python
from app.ai.guardrails import input_guardrail, GuardrailResult

@input_guardrail
async def my_custom_check(content: str) -> GuardrailResult:
    """自定义检查逻辑。"""
    if "禁止词" in content:
        return GuardrailResult(passed=False, reason="包含禁止词")
    return GuardrailResult(passed=True)

# 注册到执行器
from app.ai.guardrails import guardrail_runner
guardrail_runner.add_input_guardrail(my_custom_check)
```

### 2. Intent Classification 意图识别模式

**来源**: Flock `classifier_node.py`

**文件**: `app/ai/intent_classifier.py`

```python
from app.ai.intent_classifier import classify_intent

# 在意图分类节点中使用
result = await classify_intent(user_message, model_id)
# result.intent: "greeting" | "web_search" | "todo_management" | ...
# result.route_to: "supervisor" | "data_expert" | "todo_expert"
```

**添加新意图类型**:

```python
# 1. 在 IntentType 中添加
IntentType = Literal[
    # ... 现有类型
    "new_intent",  # ⬅️ 新增
]

# 2. 更新 INTENT_PROMPT 中的意图列表
# 3. 更新 route_to 规则
```

### 3. Prompt 渐进披露模式

**来源**: Anthropic Skills `references/` 目录

**文件**: `app/ai/prompts/prompt_loader.py`

```python
from app.ai.prompts.prompt_loader import load_reference, enrich_prompt

# 按需加载参考文档
sql_guide = load_reference("sql_guide")

# 为 Prompt 添加工具参考
enriched_prompt = enrich_prompt(base_prompt, tool_name="sql_inter")
```

**添加新参考文档**:

1. 在 `app/ai/prompts/references/` 创建 `new_guide.md`
2. 在 `prompt_loader.py` 的 `TOOL_REFERENCE_MAPPING` 中添加映射

```python
TOOL_REFERENCE_MAPPING = {
    # ...
    "new_tool": "new_guide",
}
```

### 4. 决策树 Prompt 模式

**来源**: OpenAI Swarm `triage_agent`

**文件**: `multi_agent_graph.py` 中的 `SUPERVISOR_PROMPT`

```python
SUPERVISOR_PROMPT = """
## 决策树

```
用户请求
    ├─ 简单问候？ → 直接回复
    ├─ 需要联网？ → tavily_search
    ├─ 知识库查询？ → knowledge_search
    └─ 待办管理？ → 委派 todo_expert
```

## 工具速查

| 工具 | 用途 | 典型请求 |
|------|------|----------|
| ... | ... | ... |
"""
```

**设计原则**:
- 使用 ASCII 可视化决策流程
- 提供工具速查表
- 保持核心指令精简

### 5. Parameter Extractor 参数提取模式

**来源**: Flock `parameter_extractor_node.py`

**文件**: `app/ai/parameter_extractor.py`

```python
from app.ai.parameter_extractor import extract_todo_params, TodoParams

# 从自然语言提取结构化参数
params = await extract_todo_params("明天下午3点开会，提醒我准备PPT")
# -> TodoParams(title="开会", due_date=datetime(...), reminder="准备PPT")
```

**支持的参数类型**:

| 模型 | 用途 | 字段 |
|------|------|------|
| `TodoParams` | 待办事项 | title, due_date, priority, category |
| `QueryParams` | 数据库查询 | table_name, columns, conditions, limit |
| `ChartParams` | 图表绘制 | chart_type, title, x_label, y_label |

### 6. LLM as Judge 输出评估模式

**来源**: OpenAI Agents SDK `llm_as_a_judge.py`

**文件**: `app/ai/llm_judge.py`

```python
from app.ai.llm_judge import evaluate_response, iterative_improvement

# 评估 Agent 回复
result = await evaluate_response(question, response)
# result.score: "pass" | "needs_improvement" | "fail"
# result.feedback: "具体的反馈和改进建议"

# 迭代优化模式
async def generate(q, fb):
    return await agent.ainvoke(f"{q}\n反馈: {fb}")

final_response, history = await iterative_improvement(question, generate)
```

**评估维度**:
- accuracy: 准确性
- completeness: 完整性
- clarity: 清晰度
- relevance: 相关性
