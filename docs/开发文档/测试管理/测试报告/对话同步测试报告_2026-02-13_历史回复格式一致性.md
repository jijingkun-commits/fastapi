# 对话同步测试报告_2026-02-13_历史回复格式一致性

> 创建日期：2026-02-13  
> 测试范围：聊天系统 / 对话同步 / 历史回放格式一致性  
> 对应需求：`docs/产品文档/聊天系统需求.md` §9（历史回复格式一致性）  
> 对应测试用例：`docs/开发文档/测试管理/聊天系统测试案例.md`（TC-SYNC-06）

---

## 1. Executive Summary

- 结论：PASS
- 本轮目标：修复历史消息回放时出现 `[{ 'type': 'text', 'text': '...' }]` 等结构串外露，确保“实时展示”和“历史展示”一致。
- 覆盖说明：包含单测 + API 测试 + 浏览器可视化回归（发送消息后刷新查看历史）。

---

## 2. 变更背景

本轮修复的核心是：

1. **写入侧根因修复**：后处理落库前对 `AIMessage.content` 做统一归一化，避免把 list/dict 直接 `str()` 写入数据库。
2. **读取侧兼容修复**：历史接口回放时对遗留结构串进行兼容解析与归一化，避免历史脏数据继续污染 UI。

对应代码提交：

- `9b27abc`：fix(chat): 统一归一化历史消息内容并修复回放格式异常

浏览器验证产物提交：

- `84df0ad`：test(e2e): 保存本地 playwright 浏览器联调产物
- `ecc8bf4`：test(e2e): 追加历史回放修复的浏览器验证快照

---

## 3. 测试环境

- 前端：`http://127.0.0.1:3000`
- 后端：`http://127.0.0.1:8000`
- 健康检查：`GET /api/v1/health` -> `{"status":"ok"}`
- 测试账号：`jjk` / 空密码（开发环境）

---

## 4. 执行记录

### 4.1 自动化测试（单测 + API）

执行命令：

```bash
python3 -m pytest -o addopts='' \
  tests/unit/test_chat_repo_serialization.py \
  tests/api/test_chat_api.py -q
```

执行结果：

- `15 passed`

覆盖要点：

- 落库路径：`save_conversation_from_messages()` 对 AI block list 归一化为文本。
- 历史接口：`GET /api/v1/chat/threads/{thread_id}/messages` 对遗留结构串输出可读正文。

### 4.2 浏览器可视化回归（Playwright CLI）

关键步骤：

1. 打开页面并登录进入 `/chat`
2. 发送消息：`测试历史格式修复 20260213`
3. 等待 AI 回复完成
4. 刷新页面（reload）
5. 验证：刷新后同一条 AI 回复仍为正常可读文本（无结构串外露）

对应证据文件：

- 页面快照：`.playwright-cli/page-2026-02-13T02-51-08-842Z.yml`
- 页面快照（交互/滚动状态）：`.playwright-cli/page-2026-02-13T02-51-32-243Z.yml`
- 视口截图：`.playwright-cli/page-2026-02-13T02-52-11-559Z.png`

---

## 5. Trace Matrix

| 用例ID | 场景 | 执行方式 | 结果 | 证据 |
|---|---|---|---|---|
| TC-SYNC-06 | 历史结构串兼容回放 | API + 单测 | PASS | `tests/api/test_chat_api.py` + `tests/unit/test_chat_repo_serialization.py` |
| TC-SYNC-02 | 刷新后消息完整加载 | 浏览器可视化 | PASS | `.playwright-cli/*.yml` + `.playwright-cli/*.png` |

---

## 6. Defect List

- 无新增缺陷。

备注：浏览器控制台存在部分 401 报错，发生在未登录/鉴权前的资源请求阶段，不影响本轮验证目标（历史内容渲染一致性）。

---

## 7. 结论与建议

- 本轮目标已达成：历史回放不再展示结构串，刷新后与实时展示保持一致。
- 建议后续优化：将前端启动阶段的 401 噪声日志收敛（例如在未登录状态减少 threads/models 拉取或降低日志级别），避免影响排障体验。
