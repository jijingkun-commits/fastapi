# 聊天系统测试案例

> **创建日期**: 2026-02-01
> **更新日期**: 2026-03-09
> **版本**: 2.3
> **覆盖范围**: 基础聊天 + 对话同步 + 多会话并发 + 异常处理 + 银行业务场景 + 多智能体编排 + 复合提问 + 统一汇总

## 0. 需求追溯与自动化覆盖

> 需求文档: `docs/产品文档/聊天系统需求.md`
> 设计文档: `docs/开发文档/架构设计/AI模块设计.md`

| 用例范围 | 关联需求 | 自动化覆盖 |
|---------|----------|------------|
| TC-CHAT-01~04 | US-CHAT-01~05 | `web/e2e/chat.spec.cjs`, `web/e2e/features/chat.feature.cjs`, `web/e2e/test_feedback_stop.spec.cjs` |
| TC-SYNC-01~08 | US-CHAT-04 | `web/e2e/chat_sync.spec.cjs`, `web/e2e/chat-thread-switch.spec.cjs` |
| TC-SYNC-06 | US-CHAT-04 | `tests/api/test_chat_api.py`, `tests/unit/test_chat_repo_serialization.py` |
| TC-MSC-01~09 | chat-multi-session-concurrency | `web/e2e/chat-multi-session-concurrency.spec.cjs`、`tests/api/test_chat_api.py`、`tests/unit/test_run_control_service.py`（`001~009` 已具备显式自动化证据） |
| TC-CHAT-ERR-01~04 | US-CHAT-01 | ⬜ 待补充 |
| TC-CHAT-BIZ-01~03 | US-CHAT-01~03 | ⬜ 待补充 |
| TC-CHAT-PERF-01~02 | US-CHAT-01 | ⬜ 待补充 |
| TC-OCF-01~09 | OpenClaw 迁移完善 AC-01~06 | `tests/api/test_chat_api.py -k cancel`、`tests/unit/test_multi_agent_tool_governance_runtime.py`、`tests/unit/test_multi_agent_skill_workflow.py`、`tests/unit/test_skill_service.py -k "version or binding"`、`tests/unit/test_chat_service_memory_flags.py`、`tests/unit/test_user_preference_memory_service.py`、`tests/unit/test_multi_intent_queue_flow.py`、`app/tests/test_handoff_detection.py`、`tests/unit/test_multi_agent_streaming_helpers.py` |

### 0.1 关键风险覆盖总览

| 风险ID | 风险描述 | 风险等级 | 触发条件 | 失败后果 | 当前覆盖 |
|---|---|---|---|---|---|
| R-CHAT-001 | mixed query 下精确 data task 被整句用户问题覆盖 | P0 | handoff 归一错误回退到用户原始问题 | `data_expert` 目标漂移，最终答非所问 | `app/tests/test_handoff_detection.py` |
| R-CHAT-002 | Tavily 原始 HTML / 站点噪声进入 handoff 或最终答复 | P0 | 外部搜索工具返回网页碎片或原始站点文本 | 用户看到脏文本，handoff frame 被污染 | `tests/unit/test_multi_agent_streaming_helpers.py` |
| R-CHAT-003 | `data_expert` 仅返回澄清文本却被误判为 success | P0 | 无结构化 data，仅返回澄清文案 | coverage 提前收口，系统误报已完成 | `tests/unit/test_multi_intent_queue_flow.py` |
| R-CHAT-004 | 多目标队列部分完成时提前统一收口 | P1 | 一个 goal 完成，另一个 goal 仍 pending | 最终答案漏答或误答 | `tests/unit/test_multi_intent_queue_flow.py` |
| R-CHAT-005 | 外部检索 no-result / error 文本原样透传给用户 | P1 | 第三方工具返回 no-result 或报错文本 | 用户看到原始错误语义，体验与契约失控 | ⬜ 待补充 |

### 0.2 当前覆盖缺口

| 风险ID | 当前缺口 | 原因 | 优先级 | 计划补齐方式 |
|---|---|---|---|---|
| R-CHAT-005 | no-result 用户语义尚无单独回归 | 当前已覆盖 raw noise，未覆盖无结果用户可读输出 | P1 | 新增 unit 回归，覆盖 no-result -> user-safe summary / pending fallback |
| R-CHAT-006 | SSE 断流恢复 + partial-result 收口缺组合覆盖 | 现有测试多为单项断言 | P1 | 新增 integration / e2e 组合回归 |
| R-CHAT-007 | 旧 smoke / 弱断言用例尚未显式标记替代关系 | 历史资产未建立低价值治理清单 | P2 | 在本文件与 `测试用例库.md` 补淘汰矩阵 |

## 1. 测试环境准备

参考 `docs/开发文档/测试管理/测试指南与环境配置.md` 完成环境与账号准备。

---

## 2. 基础对话测试

### TC-CHAT-01: 流式响应首 Token

| 属性 | 值 |
|-----|---|
| **前置条件** | 用户已登录，后端正常 |
| **输入** | "你好" |
| **预期行为** | 1. 2 秒内出现首字符<br>2. 过程中显示"正在输入"指示<br>3. 生成完成后指示消失 |
| **验证方法** | 记录发送时间与首字符出现时间差，应 < 2s |
| **数据库验证** | `SELECT * FROM t_message WHERE conversation_id = ? ORDER BY created_at DESC LIMIT 2` — 应有 user + assistant 两条记录 |

### TC-CHAT-02: 对话历史持久化

| 属性 | 值 |
|-----|---|
| **前置条件** | 已发送至少 3 条消息并收到回复 |
| **步骤** | 1. 记录当前消息数量和内容<br>2. 刷新页面（F5）<br>3. 等待页面完全加载 |
| **预期行为** | 1. 历史消息完整显示，无丢失<br>2. 消息顺序与刷新前一致<br>3. 用户消息和 AI 回复的角色标识正确 |
| **验证方法** | 对比刷新前后的消息列表内容、数量、顺序 |

### TC-CHAT-03: 多轮上下文保持

| 属性 | 值 |
|-----|---|
| **前置条件** | 同一对话线程 |
| **步骤** | 1. 发送"我下午有会议"<br>2. 等待 AI 回复<br>3. 发送"它几点开始？"<br>4. 等待 AI 回复<br>5. 发送"帮我总结一下刚才的对话" |
| **预期行为** | 1. 第 2 轮 AI 能理解"它"指代"会议"<br>2. 第 3 轮 AI 能概括前两轮的对话主题 |
| **边界情况** | 超过 10 轮后上下文是否仍准确（受 context window 限制） |

### TC-CHAT-04: 停止生成与反馈

| 属性 | 值 |
|-----|---|
| **前置条件** | 发送一个需要长回复的问题（如"详细介绍中国四大银行的发展历史"） |
| **步骤** | 1. 发送问题，等待 AI 开始流式输出<br>2. 在生成过程中点击"停止"按钮<br>3. 对已生成内容进行点赞<br>4. 对另一条回复进行点踩 |
| **预期行为** | 1. 点击停止后，流式输出立即中断<br>2. 已生成的部分内容被保留展示<br>3. 点赞/点踩图标状态变化正确<br>4. 反馈记录被保存到数据库 |
| **数据库验证** | `SELECT feedback FROM t_message WHERE id = ?` — 应有对应反馈值 |

---

## 3. 对话同步测试

### TC-SYNC-01: 前后端消息一致性

| 属性 | 值 |
|-----|---|
| **步骤** | 1. 发送消息"测试同步一致性"<br>2. 等待 AI 回复完成<br>3. 通过 API 查询后端存储的消息内容 |
| **预期行为** | 1. 前端展示的消息内容与后端 `t_message` 表中存储的一致<br>2. 消息的 role（user/assistant）标识一致<br>3. 消息时间戳相差不超过 1 秒 |
| **验证方法** | `GET /api/v1/conversations/{id}/messages` 与前端展示对比 |

### TC-SYNC-02: 刷新后消息完整加载

| 属性 | 值 |
|-----|---|
| **前置条件** | 当前对话有 5 条以上消息 |
| **步骤** | 1. 记录当前消息内容列表<br>2. 强制刷新页面（Ctrl+Shift+R）<br>3. 等待对话列表和消息加载完成 |
| **预期行为** | 1. 刷新后消息数量相同<br>2. 消息内容逐条匹配<br>3. Markdown 渲染和代码块格式一致 |

> **与 TC-CHAT-02 区别**: TC-CHAT-02 侧重持久化机制验证（消息是否保存），本用例侧重同步协议验证（前后端数据是否一致）。

### TC-SYNC-03: 快速连续发送

| 属性 | 值 |
|-----|---|
| **步骤** | 1. 在 1 秒内连续发送 3 条消息：<br>  - "第一条"<br>  - "第二条"<br>  - "第三条"<br>2. 等待所有回复完成 |
| **预期行为** | 1. 3 条消息均被正确接收，无丢失<br>2. 消息按发送顺序排列<br>3. 每条消息均有对应的 AI 回复（或后续消息排队等待） |
| **边界情况** | 若系统限制并发，应有友好提示而非静默丢弃 |

### TC-SYNC-04: 长文本响应完整性

| 属性 | 值 |
|-----|---|
| **输入** | "请列出 20 个常见的银行业务术语及其解释"（触发长回复） |
| **预期行为** | 1. 回复内容完整，列出全部 20 项（或模型实际输出的全部内容）<br>2. 末尾无截断（不以 `...` 或不完整句子结束）<br>3. Markdown 格式正常渲染 |
| **验证方法** | 对比前端显示字符数与后端存储的字符数 |

### TC-SYNC-05: 特殊字符处理

| 属性 | 值 |
|-----|---|
| **输入** | 依次测试以下特殊字符输入：<br>1. HTML 标签: `<script>alert('xss')</script>`<br>2. SQL 注入: `'; DROP TABLE t_message; --`<br>3. Emoji: `测试 🎉 消息`<br>4. 中文特殊符号: `【】《》「」` |
| **预期行为** | 1. HTML 标签被转义显示，不执行脚本<br>2. SQL 注入字符串作为普通文本展示<br>3. Emoji 正常显示<br>4. 中文特殊符号正常显示 |

### TC-SYNC-06: 历史结构串兼容回放

| 属性 | 值 |
|-----|---|
| **前置条件** | 历史消息中存在遗留结构串（如 `[{'type': 'text', 'text': '...'}]`） |
| **步骤** | 1. 调用 `GET /api/v1/chat/threads/{thread_id}/messages` 获取历史消息<br>2. 检查返回的 `content` 字段 |
| **预期行为** | 1. 接口返回可读正文文本，不暴露内部结构串<br>2. 非结构串普通文本保持原样输出 |
| **自动化覆盖** | `tests/api/test_chat_api.py::test_get_messages_normalizes_legacy_structured_content` + `tests/unit/test_chat_repo_serialization.py` |

### TC-SYNC-07: 首次点击“新建会话”应直接进入空会话

| 属性 | 值 |
|-----|---|
| **前置条件** | URL 带 `threadId`（已处于历史会话） |
| **步骤** | 1. 打开 `/chat?threadId=<existing>`<br>2. 点击右上角“新建会话”按钮 |
| **预期行为** | 1. 首次点击后 URL 的 `threadId` 被移除<br>2. 页面进入空会话态，不回填到“最近会话” |
| **自动化覆盖** | `web/e2e/chat-thread-switch.spec.cjs`（`TC-CHAT-THREAD-01`） |

### TC-SYNC-08: 历史列表左侧区域点击可直接切换会话

| 属性 | 值 |
|-----|---|
| **前置条件** | 历史列表中存在至少 2 个会话 |
| **步骤** | 1. 打开带侧栏参数的聊天页（`chatHistoryOpen=true`）<br>2. 点击目标会话条目的左侧图标区域 |
| **预期行为** | 1. 一次点击即可切换到目标会话<br>2. URL `threadId` 与目标会话一致 |
| **自动化覆盖** | `web/e2e/chat-thread-switch.spec.cjs`（`TC-CHAT-THREAD-02`） |

### TC-MSC-01~09: 多会话并发与 active run 恢复（2026-03-09）

| 用例 ID | 场景 | 预期结果 | 自动化覆盖 | 状态 |
|---|---|---|---|---|
| `MSC-CL-001` | A/B 会话并发提交 | 双会话完成且消息不串写；后台完成的线程显示 `unread` 蓝点，进入线程后清除为 `none` | `web/e2e/chat-multi-session-concurrency.spec.cjs` | ✅ |
| `MSC-CL-002` | 停止 B 不影响 A | B 进入 `stopping`，A 持续输出 | `web/e2e/chat-multi-session-concurrency.spec.cjs` | ✅ |
| `MSC-CL-003` | 刷新后恢复运行态 | 侧边栏旋转运行图标与 stop 能按 active run 恢复 | `web/e2e/chat-multi-session-concurrency.spec.cjs` | ✅ |
| `MSC-CL-004` | 同线程重复提交 | 返回 `409 active_run_exists` | `tests/api/test_chat_api.py::test_stream_preflight_returns_409_for_active_run_exists` | ✅ |
| `MSC-CL-005` | 用户并发超限 | 返回 `429 parallel_limit_exceeded`，且保留已有 3 个运行态 | `web/e2e/chat-multi-session-concurrency.spec.cjs` + `tests/unit/test_run_control_service.py::test_run_control_active_query_gate_rejects_parallel_limit` | ✅ |
| `MSC-CL-006` | cancel 缺失 `thread_id` | 返回 `400 thread_id_required` | `tests/api/test_chat_api.py::test_multi_session_contract_matrix_cancel_missing_thread_id_returns_400` | ✅ |
| `MSC-CL-007` | cancel `thread_id` 不匹配 | 返回 `400 thread_id_mismatch` | `tests/api/test_chat_api.py::test_multi_session_contract_matrix_cancel_thread_mismatch_returns_400` | ✅ |
| `MSC-CL-008` | 停留期间 active 自动同步 | `active_count>0` 时侧边栏显示旋转运行图标；active 消失后切为 `unread` 蓝点，进入线程后清为 `none` | `web/e2e/chat-multi-session-concurrency.spec.cjs` | ✅ |
| `MSC-CL-009` | 多 worker 一致性 | active 列表不漏报 | `tests/api/test_chat_api.py::test_multi_worker_active_runs_reads_directly_from_db` | ✅ |

---

## 4. 异常处理测试

### TC-CHAT-ERR-01: 网络中断恢复

| 属性 | 值 |
|-----|---|
| **步骤** | 1. 发送一条消息<br>2. 在 AI 生成过程中断开网络（DevTools → Network → Offline）<br>3. 等待 5 秒<br>4. 恢复网络连接 |
| **预期行为** | 1. 断网后显示网络错误提示<br>2. 恢复网络后，用户可重新发送消息<br>3. 之前的对话历史不丢失 |

### TC-CHAT-ERR-02: 后端服务异常

| 属性 | 值 |
|-----|---|
| **前置条件** | 后端服务不可用或 LLM API 返回错误 |
| **步骤** | 发送消息 |
| **预期行为** | 1. 前端显示友好错误提示（非白屏/堆栈）<br>2. 错误消息有明确指引（如"服务暂时不可用，请稍后重试"）<br>3. 输入框恢复可用状态，不卡死 |

### TC-CHAT-ERR-03: 认证过期

| 属性 | 值 |
|-----|---|
| **前置条件** | 用户 Token 已过期 |
| **步骤** | 1. 手动清除或篡改 localStorage 中的 token<br>2. 尝试发送消息 |
| **预期行为** | 1. 返回 401 错误<br>2. 自动跳转到登录页或显示重新登录提示<br>3. 不暴露敏感信息 |

### TC-CHAT-ERR-04: 空消息与超长消息

| 属性 | 值 |
|-----|---|
| **步骤** | 1. 尝试发送空消息（仅空格/换行）<br>2. 尝试发送超长消息（10000 字符以上） |
| **预期行为** | 1. 空消息：发送按钮不可点击或提示"请输入内容"<br>2. 超长消息：前端有字符限制提示，或后端返回明确错误 |

---

## 5. 银行业务场景测试

### TC-CHAT-BIZ-01: 银行业务咨询

| 属性 | 值 |
|-----|---|
| **角色** | 银行新员工 |
| **步骤** | 1. 发送"什么是对公存款和零售存款的区别？"<br>2. 等待 AI 回复<br>3. 追问"分行在统计时应该注意什么？" |
| **预期行为** | 1. AI 能准确解释对公/零售存款概念<br>2. 多轮追问中上下文保持连贯<br>3. 回复使用银行专业术语但通俗易懂 |

### TC-CHAT-BIZ-02: 跨 Agent 路由（银行场景）

| 属性 | 值 |
|-----|---|
| **角色** | 分行经理 |
| **步骤** | 1. 发送"帮我查一下上月各支行的存款余额"（应路由到问数助手）<br>2. 发送"创建一个待办：下周一提交分行月报"（应路由到待办助手）<br>3. 发送"AI 助手有哪些功能？"（应由通用 Agent 回答） |
| **预期行为** | 1. 数据查询请求被正确路由到问数助手<br>2. 待办创建请求被正确路由到待办助手<br>3. 通用问题由默认 Agent 回答<br>4. 各次路由之间上下文不互相干扰 |

### TC-CHAT-BIZ-03: 合规边界（敏感信息处理）

| 属性 | 值 |
|-----|---|
| **角色** | 客户经理 |
| **步骤** | 1. 发送包含客户身份证号的消息（如"客户张三，身份证号 110101199001011234，需要办理贷款"）<br>2. 发送包含银行卡号的消息 |
| **预期行为** | 1. AI 回复中不应明文重复敏感信息<br>2. 如果系统有脱敏机制，敏感信息应被部分遮蔽（如 `1101****1234`）<br>3. 对话历史中存储的消息应符合数据安全规范 |
| **合规要求** | 符合银行业个人信息保护相关规定 |

---

## 6. 性能测试

### TC-CHAT-PERF-01: 首 Token 响应时间

| 属性 | 值 |
|-----|---|
| **测试条件** | 网络正常，后端空闲状态 |
| **步骤** | 连续发送 10 条不同问题，记录每次首 Token 时间 |
| **预期结果** | 1. 平均首 Token 时间 < 2 秒<br>2. P95 首 Token 时间 < 5 秒<br>3. 无超时（> 30 秒）情况 |

### TC-CHAT-PERF-02: 长会话稳定性

| 属性 | 值 |
|-----|---|
| **测试条件** | 单一对话线程 |
| **步骤** | 在同一对话中连续进行 20 轮问答 |
| **预期结果** | 1. 第 20 轮回复质量不明显下降<br>2. 页面无卡顿或内存泄漏<br>3. 滚动和渲染正常 |

---

## 7. 测试执行清单

| 分类 | 用例 ID | 用例名称 | 优先级 | 状态 |
|------|---------|----------|--------|------|
| 基础对话 | TC-CHAT-01 | 流式响应首 Token | P0 | ✅ |
| 基础对话 | TC-CHAT-02 | 对话历史持久化 | P0 | ✅ |
| 基础对话 | TC-CHAT-03 | 多轮上下文保持 | P0 | ✅ |
| 基础对话 | TC-CHAT-04 | 停止生成与反馈 | P1 | ✅ |
| 对话同步 | TC-SYNC-01 | 前后端消息一致性 | P0 | ✅ |
| 对话同步 | TC-SYNC-02 | 刷新后消息完整加载 | P0 | ✅ |
| 对话同步 | TC-SYNC-03 | 快速连续发送 | P1 | ✅ |
| 对话同步 | TC-SYNC-04 | 长文本响应完整性 | P1 | ✅ |
| 对话同步 | TC-SYNC-05 | 特殊字符处理 | P1 | ✅ |
| 对话同步 | TC-SYNC-06 | 历史结构串兼容回放 | P0 | ✅ |
| 对话同步 | TC-SYNC-07 | 首次点击“新建会话”直接进入空会话 | P0 | ✅ |
| 对话同步 | TC-SYNC-08 | 历史列表左侧区域点击切换 | P0 | ✅ |
| 多会话并发 | MSC-CL-001 | A/B 会话并发提交 | P0 | ✅ |
| 多会话并发 | MSC-CL-002 | 停止 B 不影响 A | P0 | ✅ |
| 多会话并发 | MSC-CL-003 | 刷新后恢复运行态 | P0 | ✅ |
| 多会话并发 | MSC-CL-004 | 同线程重复提交 | P0 | ✅ |
| 多会话并发 | MSC-CL-005 | 用户并发超限 | P0 | ✅ |
| 多会话并发 | MSC-CL-006 | cancel 缺失 thread_id | P0 | ✅ |
| 多会话并发 | MSC-CL-007 | cancel thread_id 不匹配 | P0 | ✅ |
| 多会话并发 | MSC-CL-008 | 停留期间 active 自动同步 | P0 | ✅ |
| 多会话并发 | MSC-CL-009 | 多 worker 一致性 | P0 | ✅ |
| 多会话并发 | MSC-CL-010 | active 空窗保留本地 running | P0 | ✅ |
| 多会话并发 | MSC-CL-011 | 新线程立即入栏 | P0 | ✅ |
| 异常处理 | TC-CHAT-ERR-01 | 网络中断恢复 | P0 | ⬜ |
| 异常处理 | TC-CHAT-ERR-02 | 后端服务异常 | P0 | ⬜ |
| 异常处理 | TC-CHAT-ERR-03 | 认证过期 | P1 | ⬜ |
| 异常处理 | TC-CHAT-ERR-04 | 空消息与超长消息 | P1 | ⬜ |
| 银行场景 | TC-CHAT-BIZ-01 | 银行业务咨询 | P1 | ⬜ |
| 银行场景 | TC-CHAT-BIZ-02 | 跨 Agent 路由 | P0 | ⬜ |
| 银行场景 | TC-CHAT-BIZ-03 | 合规边界 | P1 | ⬜ |
| 性能测试 | TC-CHAT-PERF-01 | 首 Token 响应时间 | P1 | ⬜ |
| 性能测试 | TC-CHAT-PERF-02 | 长会话稳定性 | P2 | ⬜ |

---

## 8. OpenClaw 迁移完善专项（AC-01 ~ AC-06）

> 需求来源：`docs/内部参考/迭代需求/openclaw特性迁移完善_requirements.md`
> 实施计划：`docs/内部参考/迭代需求/openclaw特性迁移完善_implementation_plan.md`

### TC-OCF-01: run 取消幂等（AC-01）

| 属性 | 值 |
|-----|---|
| **前置条件** | `ENABLE_RUN_CONTROL=true`，创建可取消 run |
| **步骤** | 1. 调用 `/api/v1/chat/runs/{run_id}/cancel`<br>2. 对同一 `run_id` 再次调用 cancel |
| **预期行为** | 1. 两次请求均返回 `accepted=true`（或等价幂等语义）<br>2. `status` 保持可解释（如 `stopping/stopped`） |
| **自动化覆盖** | `tests/api/test_chat_api.py -k cancel` |

### TC-OCF-02: 多用户取消隔离（AC-01）

| 属性 | 值 |
|-----|---|
| **前置条件** | 用户 A / B 分别存在活跃 run |
| **步骤** | 1. 用户 A 对自己的 run 发起 cancel<br>2. 观察用户 B run 状态 |
| **预期行为** | 1. 用户 A run 被取消<br>2. 用户 B run 不受影响 |
| **自动化覆盖** | `tests/unit/test_run_control_service.py`（含多用户隔离断言） |

### TC-OCF-03: 工具治理策略生效（AC-02）

| 属性 | 值 |
|-----|---|
| **前置条件** | 启用 `tool_governance.enabled`，配置 `allow/deny` 策略 |
| **步骤** | 1. 构造包含可允许与需拒绝工具的工具集<br>2. 执行治理过滤 |
| **预期行为** | 1. `deny` 优先于 `allow`<br>2. `fail_mode` 异常兜底生效（`compat/allow/deny/minimal`） |
| **自动化覆盖** | `tests/unit/test_multi_agent_tool_governance_runtime.py` |

### TC-OCF-04: Skill 用户绑定版本命中（AC-03）

| 属性 | 值 |
|-----|---|
| **前置条件** | `ENABLE_SKILL_VERSIONING=true` 且 `ENABLE_USER_SKILL_BINDING=true` |
| **步骤** | 1. 绑定 `user_id + skill_id + version`<br>2. 发起带 `user_id` 的检索 |
| **预期行为** | 1. 命中用户绑定版本<br>2. 响应中可解释生效版本来源（binding/default） |
| **自动化覆盖** | `tests/unit/test_multi_agent_skill_workflow.py`、`tests/unit/test_skill_service.py -k "version or binding"`、`tests/api/test_skill_admin_api.py` |

### TC-OCF-05: Skill 迁移切流与回退（AC-03）

| 属性 | 值 |
|-----|---|
| **前置条件** | 新旧 Skill 表均可访问 |
| **步骤** | 1. 开启版本化开关并验证三层主读路径<br>2. 关闭版本化开关验证回退旧路径 |
| **预期行为** | 1. 新路径与回退路径均可返回结果<br>2. 回退不影响主流程可用性 |
| **自动化覆盖** | `tests/unit/test_skill_service.py -k "version or binding"` |

### TC-OCF-05b: Skill Progressive Loader 显式加载与回放 canonical（Phase A）

| 属性 | 值 |
|-----|---|
| **前置条件** | `ENABLE_PROGRESSIVE_SKILL_LOADING=true` 且 `SKILL_RUNTIME_MODE=catalog_tool` |
| **步骤** | 1. preprocess 构建 `skill_catalog_manifest / skill_catalog_context`<br>2. Supervisor 调用 `load_skills` 加载正文<br>3. 最终 AIMessage 落 `additional_kwargs.skill_runtime`<br>4. 下一轮从历史 AIMessage 恢复 `loaded_skill_registry` |
| **预期行为** | 1. 不再以 hybrid 检索结果作为聊天主路径<br>2. 回放 canonical 字段完整<br>3. `loaded_skill_context` 可按 registry 回源重建 |
| **自动化覆盖** | `app/tests/test_skill_catalog_manifest.py`、`app/tests/test_skill_loader_tool.py`、`app/tests/test_skill_runtime_replay.py`、`tests/unit/test_multi_agent_skill_workflow.py` |


### TC-OCF-06: 偏好记忆 recall 注入（AC-04）

| 属性 | 值 |
|-----|---|
| **前置条件** | `ENABLE_MEMORY_RECALL=true`，存在 `t_user_memory` 历史记录 |
| **步骤** | 调用聊天流式接口，传入有效 `user_id` |
| **预期行为** | 1. 会话上下文注入用户偏好摘要<br>2. 未提供 `user_id` 时不注入，避免越权 |
| **自动化覆盖** | `tests/unit/test_chat_service_memory_flags.py`、`tests/unit/test_user_preference_memory_service.py` |

### TC-OCF-07: 偏好记忆 flush 落库（AC-04）

| 属性 | 值 |
|-----|---|
| **前置条件** | `ENABLE_PRE_COMPACTION_FLUSH=true` |
| **步骤** | 发送包含显式偏好的用户消息，触发会话结束 |
| **预期行为** | 1. 命中偏好时写入 `t_user_memory`<br>2. 关闭 flush 开关时不写入 |
| **自动化覆盖** | `tests/unit/test_chat_service_memory_flags.py`、`tests/unit/test_user_preference_memory_service.py` |

### TC-OCF-08: 复合提问队列与统一汇总（AC-05）

| 属性 | 值 |
|-----|---|
| **关联需求** | `docs/产品文档/聊天系统需求.md` §5.2 边界与异常 |
| **关联设计** | `docs/开发文档/架构设计/AI模块设计.md` 中的 Data Handoff 规范化 / Tool Observation 归一 / Coverage Pending 收口 |
| **风险标签** | `handoff`, `semantic-contract`, `multi-intent`, `partial-result`, `sanitization` |
| **风险等级** | P0 |
| **前置条件** | 触发 multi-intent 路由，构造 `handoff_batch`，且 `Supervisor` 已为 data 目标生成精确 `task_description` |
| **输入 / 触发** | 类似“先查天气，再看 2025-06-30 贷款余额前 10 名客户”的 mixed query |
| **核心步骤** | 1. 执行队列串行 handoff<br>2. mixed query 下将精确 data task 透传给 `data_expert`<br>3. 当 `data_expert` 仅返回澄清文本时，继续执行 coverage 判定与统一汇总 |
| **主结果断言** | 1. `handoff_queue` 串行消费<br>2. 已生成的精确 data task 不被用户原始复合问题覆盖 |
| **失败语义断言** | 1. `data.query` 未产出结构化结果时保持 `pending`，不得误判为 success<br>2. Tavily 原始网页/HTML 噪声与 no-result/error 文本不得直接进入 handoff frame 或最终答复 |
| **状态 / 副作用断言** | `missing_goals` 持续记录未完成 goal；统一汇总阶段输出补齐提示而不是冒充已完成 |
| **自动化覆盖** | `tests/unit/test_multi_intent_queue_flow.py`、`app/tests/test_handoff_detection.py`、`tests/unit/test_multi_agent_streaming_helpers.py` |
| **历史缺陷来源** | mixed query handoff 漂移、第三方 observation 噪声污染、coverage success 条件过宽 |
| **退化信号** | 最终答复混入天气等无关上下文、出现原始网页噪声、或在核心 data 目标未完成时声称“已完成” |

### TC-OCF-09: 灰度开关回滚（AC-06）

| 属性 | 值 |
|-----|---|
| **前置条件** | OpenClaw 相关开关已开启并通过专项用例 |
| **步骤** | 依次关闭 `ENABLE_RUN_CONTROL`、`ENABLE_TOOL_GOVERNANCE`、`ENABLE_MEMORY_RECALL`、`ENABLE_PRE_COMPACTION_FLUSH`、`ENABLE_SKILL_VERSIONING` |
| **预期行为** | 1. 系统回退兼容路径，无阻塞<br>2. 主聊天流程可继续运行 |
| **自动化覆盖** | 由上述专项用例结合开关切换回归验证 |


## 9. 自动化覆盖缺口

| 风险ID | 当前缺口 | 原因 | 优先级 | 计划补齐方式 |
|---|---|---|---|---|
| R-CHAT-005 | 外部检索 no-result 用户语义缺显式回归 | 当前覆盖 raw noise，但未覆盖 no-result 用户可读输出 contract | P1 | 新增 unit 回归，验证 no-result -> user-safe summary / pending fallback |
| R-CHAT-006 | SSE 断流恢复 + partial-result 统一收口缺组合场景 | 现有覆盖偏单项，组合退化仍靠人工识别 | P1 | 新增 integration / e2e 组合回归 |
| R-CHAT-007 | 历史弱断言 smoke 未建立替代关系 | 老测试资产仍以“能跑”作为主要价值 | P2 | 与 `测试用例库.md` 联动建立低价值测试淘汰矩阵 |

## 10. 低价值 / 待淘汰用例

| 用例ID/脚本 | 问题类型 | 原因 | 替代方案 | 状态 |
|---|---|---|---|---|
| 旧聊天 smoke（仅断言响应非空的场景脚本） | 弱断言 | 只证明链路没崩，不验证 handoff / coverage / 外部工具语义 | 由 `TC-OCF-08` 及其回归入口替代 | 待清理 |
| 只测内部 helper 返回值的路由/编排测试 | 过度耦合实现细节 | 轻微重构就脆断，且无法证明最终业务 contract 正确 | 改为 contract 级测试，验证 `task_description` / `pending` / `missing_goals` / 最终摘要 | 待重写 |
