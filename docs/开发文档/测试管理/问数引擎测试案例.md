# 问数引擎测试案例

> **创建日期**: 2026-02-01  
> **版本**: 1.0  
> **覆盖范围**: Schema 同步 / SQL 生成 / 多维分析

## 0. 需求追溯与自动化覆盖

> 需求文档: `docs/产品文档/问数助手需求.md`

| 用例范围 | 关联需求 | 自动化覆盖 |
|---------|----------|------------|
| TC-AD-01~03 | US-AD-01~03 | `test_ask_data_flow.py` |
| TC-AD-02, TC-AD-03, TC-AD-04, TC-AD-05 | US-AD-02, US-AD-03 | `web/e2e/data-agent.spec.cjs` |

## 1. 测试环境准备

参考 `docs/开发文档/测试管理/测试指南与环境配置.md`，确保数据库与问数服务可用。

## 2. Schema 同步测试

### TC-AD-01: Schema 同步

| 属性 | 值 |
|-----|---|
| **前置条件** | 具备 DDL 或元数据 |
| **操作** | 执行 Schema 同步脚本 |
| **预期行为** | 元数据被写入元表，可查询 |

## 3. SQL 生成测试

### TC-AD-02: 基础 SQL 生成

| 属性 | 值 |
|-----|---|
| **输入** | “查询贷款余额” |
| **预期行为** | 生成包含聚合的 SQL |

### TC-AD-03: 多维分析

| 属性 | 值 |
|-----|---|
| **输入** | “按分行统计存款” |
| **预期行为** | 生成带 GROUP BY 的 SQL 并返回结果 |

### TC-AD-04: 意图解析与澄清（银行业务场景）

| 属性 | 值 |
|-----|---|
| **输入** | “查询贷款余额” |
| **预期行为** | 识别为业务数据查询（intent 含 free_query）、指标为贷款余额；路由到数据专家（assign_to_data_expert）；若未指定时间范围，返回澄清提示（如请指定时间范围、筛选条件等） |
| **业务口径** | 贷款余额为对公/零售核心指标，需时间区间与可选维度（分行、产品线等） |

### TC-AD-05: 安全拦截

| 属性 | 值 |
|-----|---|
| **输入** | 危险操作类自然语言（如“删除所有订单数据”） |
| **预期行为** | 不执行破坏性 SQL，响应为拒绝或安全说明 |
