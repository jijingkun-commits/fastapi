# 脚本链路证据注册表

> 用途：为 `/jjk-test` 的 `Scripted Flow Status` 提供唯一可追溯清单。  
> 适用范围：命中 `mandatory_evidence` 包含 `scripted_flow` 的任务。  
> 最后更新：2026-03-13

---

## 1. 报告对账字段（强制）

每次测试报告至少包含以下字段：

1. `Required Evidence`
2. `Actual Evidence`
3. `Scripted Flow Status`
4. `Historical Gap vs Current Gap`

若脚本链路缺失导致 `mandatory_evidence` 未满足，输出失败码：`TEST_DB_CHAIN_INCOMPLETE`。

---

## 2. 注册表

| Script ID | 脚本/测试入口 | 证据类别 | 主要风险标签 | 最低通过标准 |
|:--|:--|:--|:--|:--|
| SF-CHAT-001 | `tests/api/test_chat_api.py` | `chat_db` + `api` | `chat_db` | DB 写入读回断言通过，`exit_code=0` |
| SF-CHAT-002 | `web/e2e/chat_sync.spec.cjs` | `scripted_flow` + `chat_db` | `chat_db`, `scripted_flow` | SSE/历史回放链路通过，且状态一致 |
| SF-CHAT-003 | `scripts/verify/chat_stream_smoke.py` | `scripted_flow` + `chat_api` | `streaming`, `scripted_flow` | 登录成功，SSE 流可建立，能持续收到事件 |
| SF-TODO-001 | `tests/api/test_todo_api.py` | `chat_db` + `api` | `chat_db` | Todo CRUD 与持久化断言通过 |
| SF-TODO-002 | `scripts/verify/todo_shortcuts.py` | `scripted_flow` + `chat_db` | `todo_shortcut`, `scripted_flow` | 快捷指令链路按预期输出，关键 DB 结果可核对 |
| SF-TODO-003 | `scripts/verify/todo_complex_flow.py` | `scripted_flow` | `todo_multiround`, `scripted_flow` | 多轮复杂场景可连续执行，输出不中断 |
| SF-TODO-004 | `scripts/verify/todo_complex_scenario.py` | `scripted_flow` | `todo_multiround`, `scripted_flow` | 复杂多轮场景可完成到最终阶段，不出现硬错误 |
| SF-TODO-005 | `scripts/verify/todo_comprehensive_suite.py` | `scripted_flow` + `chat_db` | `todo_crud`, `scripted_flow` | 关键创建/重复检测链路通过，必要 DB 结果可核对 |
| SF-TODO-006 | `scripts/verify/todo_e2e_real.py` | `scripted_flow` + `chat_db` | `todo_real_llm`, `scripted_flow` | 真实 LLM + DB 路径能完成一次端到端创建验证 |
| SF-TODO-007 | `scripts/verify/todo_multiround.py` | `scripted_flow` | `todo_intent`, `guardrails`, `scripted_flow` | 意图识别、参数提取、护栏检查都可独立执行 |
| SF-DATA-001 | `tests/api/test_data_chat.py` | `data_db` + `api` | `data_db` | SQL 路由命中 + 结果断言通过 |
| SF-DATA-002 | `scripts/data/verify_data_db.py` | `scripted_flow` + `data_db` | `data_db`, `scripted_flow` | 路由/SQL/结果三段式证据齐全 |
| SF-DATA-003 | `scripts/verify/ask_data_flow.py` | `scripted_flow` + `data_db` | `askdata`, `scripted_flow` | DDL、元数据同步和 SQL 生成验证链路完成 |
| SF-DATA-004 | `scripts/verify/vanna_retrieval.py` | `scripted_flow` + `data_db` | `vanna`, `scripted_flow` | 可完成一次检索验证并输出候选结果 |
| SF-ENV-001 | `tests/verify_environment.py` | `env_gate` | `scripted_flow` | 环境前置门禁全部通过 |
| SF-INFRA-001 | `scripts/verify/minio_connection.py` | `scripted_flow` + `infra` | `minio`, `scripted_flow` | Bucket 列表、ensure_bucket 与预签名 URL 验证完成 |
| SF-SKILL-001 | `scripts/verify/skill_visibility.py` | `scripted_flow` + `chat_api` | `skill_runtime`, `scripted_flow` | 期望的技能状态消息能在流中出现 |

---

## 3. 维护规则

1. 新增脚本链路时必须先登记 Script ID，再纳入测试矩阵。
2. `Actual Evidence` 需引用结构化结果（如 `acceptance_results[*].kind/cmd/exit_code/summary`）。
3. 允许记录历史缺口，但不得将历史缺口冒充本轮已验证。
4. 命中 DB 风险任务时，脚本链路仅是补充证据，不能替代 DB 断言证据本身。
