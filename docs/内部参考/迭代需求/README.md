# 迭代需求目录（旧入口说明）

> 更新时间：2026-03-13
> 适用范围：`docs/内部参考/迭代需求/`

## 当前口径

- 这里不再是新的过程文档 canonical 根目录。
- 新的需求过程文档统一进入 `workdocs/需求/`。
- 新的设计过程文档统一进入 `workdocs/设计/`。
- 新的实施合同、验收合同、报告与机读校验统一进入 `workdocs/任务拆解/` 或 `workdocs/归档/`。

## 这里暂时保留什么

1. 这份说明页

## 这里不再新增什么

1. 新的 `requirements.md`
2. 新的 `implementation_plan.md`
3. 新的调试 / 审查 / 验收 / 重构报告
4. 新的机读 JSON 校验产物
5. 新的研究材料与模板

## 后续方向

- 当前正文与模板都已迁走，这个目录后续只保留旧入口说明页。
