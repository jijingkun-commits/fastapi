# WS-C03 C03 P3 最近会话回显与收口验证

> WS 编号: WS-C03  
> 对应卡片: `C03`  
> 类型: `parallel`  
> 对应 `feature_id`: `P1-04,P1-05`

## 0. 关联与来源

- 对应 `task_key`: `PP-20260301-CHAT-RUN-STOP`
- 来源主计划: `docs/内部参考/迭代需求/聊天断页续跑与强停止_implementation_plan.md`
- 来源并行计划: `docs/内部参考/任务拆解/2026-03-01_聊天断页续跑与强停止/parallel_plan.md`
- card_key: `PP-20260301-CHAT-RUN-STOP::WS-C03`
- PR 归属: `PR-03` / `codex/chat-run-stop-pr-03`
- PR 依赖: `['PR-02']`

## 1. 目标

- 新增最近会话查询与登录后自动定位 thread_id
- 失败重试与提示文案收口，保障可观测和可回退
- 补齐相关 API/E2E/文档校验，形成闭环证据

### 1.1 功能机制（必填）

- 触发条件: 前置依赖 `C02` 完成
- 输入: 上游卡片 done gate 证据 + 当前 feature 配置
- 输出: 卡片验收证据、回滚锚点、状态更新
- 状态流转（含异常分支）: `Backlog -> Doing -> Review -> Gate -> Done`，失败则回滚并标记 blocked
- 与上/下游 WS 的契约关系: 仅在 `depends_on` 满足后推进

### 1.2 代码锚点与样例（必填）

- 代码锚点（函数/类级）:
  - `app/repositories/chat_repo.py::get_latest_thread_by_user`
  - `app/api/v1/endpoints/chat_api.py::latest_thread`
  - `web/src/components/auth/LoginCard.tsx::onLogin`
  - `web/src/hooks/useSSEStream.ts::loadThreadMessages`
- 最小样例（可伪代码）:

```python
if checks_passed:
    mark_done(card_id)
else:
    rollback_to_anchor()
```

## 2. 文件边界

### 可修改（白名单）
- `app/repositories/chat_repo.py`
- `app/api/v1/endpoints/chat_api.py`
- `web/src/components/auth/LoginCard.tsx`
- `web/src/hooks/useSSEStream.ts`
- `tests/api/test_chat_api.py`
- `web/e2e/test_reopen_latest_thread.spec.cjs`
- `docs/SUMMARY.md`

### 禁止修改（黑名单）
- `docs/内部参考/迭代需求/openclaw迁移重建基线_implementation_plan.md`

## 3. 状态与契约

- 可写字段: `card.status`、`evidence_entry`、`gate_result`
- 只读字段: `card_id`、`feature_ids`、`depends_on`
- 外部契约: `planning_contract` 与 `vk_cards.json` 一致

## 4. 实施步骤

1. 拉取前置卡完成状态并执行本卡前置检查。
2. 运行 `acceptance_checks` 并记录输出。
3. 回填证据与回滚锚点结果。

### 4.1 串行门禁（serial 模式必填）

- 前置卡: `C02`
- 解锁条件: `depends_on` 对应卡片全部通过
- 本 WS 不得推进条件: 任一 `acceptance_checks` 失败

## 5. 测试与验收

- 最小测试集:
- `venv/bin/python -m pytest tests/api/test_chat_api.py -k latest_thread -q`
- `cd web && pnpm exec playwright test e2e/test_reopen_latest_thread.spec.cjs --project=chromium`
- 验收标准:
- 登录自动回显最近会话
- 失败重试与提示可观测

### 5.0 验收门禁映射（必填）

- 对应 implementation plan `done_gate`: `登录自动回显最近会话; 失败重试与提示可观测`
- 本 WS 负责的门禁子项: `C03:P1-04,P1-05`
- 证据回填位置（文档节）: `docs/内部参考/迭代需求/聊天断页续跑与强停止_implementation_plan.md#7`

## 6. 风险与回滚

- 主要风险: 前置卡未完成、字段契约漂移、测试结果不稳定
- 回滚点:
- 登录流程回退 router.push('/chat') 固定路径
- 下线 latest thread 查询入口，保留原线程列表获取路径
- 回滚开关/策略: 仅在本卡范围回滚，禁止跨卡改动

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表: 见白名单
- 是否修改了白名单外文件（是/否）: 否
- 测试命令与结果: 见 acceptance_checks
- 已知风险点: 串行链路断裂导致阻塞
- 回滚建议: 先回滚本卡，再评估下游
- 证据绑定检查（target_task_id == evidence_task_id）: 必填

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-C03
  feature_id: P1-04,P1-05
  card_key: PP-20260301-CHAT-RUN-STOP::WS-C03
  title: C03 P3 最近会话回显与收口验证
  type: parallel
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  lane: lane-chat-experience
  hard_depends_on: ['C02']
  soft_depends_on: []
  depends_on: ['C02']
  file_whitelist:
    - app/repositories/chat_repo.py
    - app/api/v1/endpoints/chat_api.py
    - web/src/components/auth/LoginCard.tsx
    - web/src/hooks/useSSEStream.ts
    - tests/api/test_chat_api.py
    - web/e2e/test_reopen_latest_thread.spec.cjs
    - docs/SUMMARY.md
  mechanism_summary:
    - 新增最近会话查询与登录后自动定位 thread_id
    - 失败重试与提示文案收口，保障可观测和可回退
    - 补齐相关 API/E2E/文档校验，形成闭环证据
  code_anchor_refs:
    - app/repositories/chat_repo.py::get_latest_thread_by_user
    - app/api/v1/endpoints/chat_api.py::latest_thread
    - web/src/components/auth/LoginCard.tsx::onLogin
    - web/src/hooks/useSSEStream.ts::loadThreadMessages
  acceptance_checks:
    - venv/bin/python -m pytest tests/api/test_chat_api.py -k latest_thread -q
    - cd web && pnpm exec playwright test e2e/test_reopen_latest_thread.spec.cjs --project=chromium
  rollback_anchors:
    - 登录流程回退 router.push('/chat') 固定路径
    - 下线 latest thread 查询入口，保留原线程列表获取路径
  evidence_entry: docs/内部参考/迭代需求/聊天断页续跑与强停止_implementation_plan.md#7
  check_cmd:
    - venv/bin/python -m pytest tests/api/test_chat_api.py -k latest_thread -q
    - cd web && pnpm exec playwright test e2e/test_reopen_latest_thread.spec.cjs --project=chromium
  done_gate:
    - 登录自动回显最近会话
    - 失败重试与提示可观测
  source_ws_file: docs/内部参考/任务拆解/2026-03-01_聊天断页续跑与强停止/workstreams/WS-C03_C03P3最近会话回显与收口验证.md
```
