# 工作包说明

> WS 编号: WS-02  
> 名称: 指标采集与健康聚合服务  
> 类型: parallel

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260213-ADMIN-OVERVIEW-COCKPIT`
- 来源主计划：`docs/内部参考/迭代需求/implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/parallel_plan.md`

---

## 1. 目标

- 本包目标：完成总览指标采集、分钟聚合、健康分计算与模块状态映射。
- 完成定义（DoD）：
  1. 输出统一 canonical 结构 `OverviewSnapshot`。
  2. 健康分与告警判定只在服务层计算，前端不重复实现。
  3. 聚合任务失败时可降级读取最近快照。

---

## 2. 文件边界

### 可修改（白名单）

- `app/services/admin_overview_service.py`
- `app/services/ops_snapshot_service.py`
- `app/services/__init__.py`
- `app/core/middlewares/correlation.py`（仅限采集打点扩展）
- `tests/unit/test_admin_overview_service.py`

### 禁止修改（黑名单）

- `app/models/**`
- `app/api/**`
- `web/src/**`

---

## 3. 状态与契约

- 可写字段：`OverviewSnapshot` 内部聚合与阈值映射逻辑。
- 只读字段：SSE envelope（`done/result/interrupt`）与前端组件字段名。
- 单写入权：`health_score/health_level` 仅 WS-02 可定义语义。

---

## 4. 实施步骤

1. 定义 `OverviewSnapshot` 内部对象与聚合入口函数。
2. 实现成功率、P95、5xx、QPS、成本、数据新鲜度、模块矩阵计算。
3. 增加降级策略（最近快照兜底 + 数据过期标记）。
4. 补齐核心单测。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python -m pytest -q tests/unit/test_admin_overview_service.py`
- 验收标准：
  1. 阈值映射与健康分计算稳定。
  2. 缺失数据场景返回 `unknown/expired` 而非误置 `0`。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：仅服务层与聚合逻辑。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：不涉及 UI 改动。

---

## 6. 风险与回滚

- 主要风险：阈值硬编码分散，后续难以维护。
- 回滚点：回退新增 service 文件，保留已有总览入口页。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-02
  card_key: PP-20260213-ADMIN-OVERVIEW-COCKPIT::WS-02
  title: 指标采集与健康聚合服务
  type: parallel
  lane: lane-backend-service
  hard_depends_on:
    - WS-00
    - WS-01
  soft_depends_on: []
  depends_on:
    - WS-00
    - WS-01
  file_whitelist:
    - app/services/admin_overview_service.py
    - app/services/ops_snapshot_service.py
    - app/core/middlewares/correlation.py
    - tests/unit/test_admin_overview_service.py
  readonly_scope:
    - app/models/
    - app/api/
    - web/src/
  owner_fields:
    - health_score
    - health_level
    - module_matrix
    - alerts
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit/test_admin_overview_service.py
  handoff_artifacts:
    - app/services/admin_overview_service.py
    - app/services/ops_snapshot_service.py
  dod:
    - OverviewSnapshot 聚合逻辑可用并覆盖异常降级路径
```
