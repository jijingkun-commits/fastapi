# 工作包说明

> WS 编号: WS-01  
> 名称: 总览观测快照模型与迁移  
> 类型: parallel

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260213-ADMIN-OVERVIEW-COCKPIT`
- 来源主计划：`docs/内部参考/迭代需求/implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/parallel_plan.md`

---

## 1. 目标

- 本包目标：新增总览观测快照模型与迁移，建立分钟级快照存储基线。
- 完成定义（DoD）：
  1. 新增 `t_ops_metric_snapshot_minute` 表结构、索引、唯一约束。
  2. 新增 SQLAlchemy 模型并纳入 `app/models/__init__.py`。
  3. 迁移脚本支持 downgrade。

---

## 2. 文件边界

### 可修改（白名单）

- `app/models/ops_metric_snapshot.py`
- `app/models/__init__.py`
- `alembic/versions/*ops_metric_snapshot*.py`
- `tests/unit/test_ops_metric_snapshot_model.py`

### 禁止修改（黑名单）

- `app/services/**`
- `app/api/**`
- `web/src/**`

---

## 3. 状态与契约

- 可写字段：快照表字段命名、类型、约束。
- 只读字段：健康分算法和 API 响应结构。
- 双库约束：仅写 `DATABASE_URL(chat_db)`；禁止对 `ANALYTICS_DATABASE_URL(data_db)` 执行写入迁移。

---

## 4. 实施步骤

1. 新建 ORM 模型 `OpsMetricSnapshotMinute`。
2. 生成 Alembic 迁移，添加索引与唯一键。
3. 补充模型级单测（字段与约束）。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python -m pytest -q tests/unit -k "ops_metric_snapshot or migration"`
- 验收标准：
  1. 迁移可执行且可回滚。
  2. 表结构满足实现方案字段口径。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：仅后端模型/迁移变更。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：无前端交互改动。

---

## 6. 风险与回滚

- 主要风险：字段类型选择不当导致成本/精度损失。
- 回滚点：执行 migration downgrade + 回退模型文件。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（/vk 机读，必填）

```yaml
card_export:
  id: WS-01
  card_key: PP-20260213-ADMIN-OVERVIEW-COCKPIT::WS-01
  title: 总览观测快照模型与迁移
  type: parallel
  lane: lane-backend-model
  hard_depends_on:
    - WS-00
  soft_depends_on: []
  depends_on:
    - WS-00
  file_whitelist:
    - app/models/ops_metric_snapshot.py
    - app/models/__init__.py
    - alembic/versions/
    - tests/unit/test_ops_metric_snapshot_model.py
  readonly_scope:
    - app/services/
    - app/api/
    - web/src/
  owner_fields:
    - snapshot_minute
    - health_score
    - health_level
    - budget_usage_pct
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit -k "ops_metric_snapshot or migration"
  handoff_artifacts:
    - app/models/ops_metric_snapshot.py
    - alembic/versions/
  dod:
    - t_ops_metric_snapshot_minute 模型与迁移落地并可回滚
```
