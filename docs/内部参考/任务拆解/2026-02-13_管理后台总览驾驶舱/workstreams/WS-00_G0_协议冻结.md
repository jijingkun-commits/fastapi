# 工作包说明

> WS 编号: WS-00  
> 名称: G0 协议冻结  
> 类型: foundation

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260213-ADMIN-OVERVIEW-COCKPIT`
- 来源主计划：`docs/内部参考/迭代需求/implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/parallel_plan.md`

---

## 1. 目标

- 本包目标：冻结总览 SSE 协议 `done/result/interrupt` 字段契约。
- 完成定义（DoD）：
  1. `parallel_plan.md` 的 G0 章节完整可读。
  2. 机读协议文件 `contracts/sse_events_v1.json` 可被后续 WS 复用。
  3. 所有并行 WS 均声明 `hard_depends_on: [WS-00]`。

---

## 2. 文件边界

### 可修改（白名单）

- `docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/parallel_plan.md`
- `docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/workstreams/WS-00_G0_协议冻结.md`
- `docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/contracts/sse_events_v1.json`

### 禁止修改（黑名单）

- `app/**`
- `web/**`

---

## 3. 状态与契约

- 可写字段：`sse.done`、`sse.result`、`sse.interrupt`。
- 只读字段：业务聚合状态与前端展示字段。
- 外部契约：`docs/开发文档/代码解读/SSE事件协议.md`。

---

## 4. 实施步骤

1. 冻结 `done/result/interrupt` 的 required/optional 字段。
2. 产出机读协议 `contracts/sse_events_v1.json`。
3. 回填并行计划中的契约 owner/consumer 关系。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python scripts/docs_guard.py --strict`
- 验收标准：
  1. 契约定义与并行计划一致。
  2. 后续 WS 不新增私有协议假设。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：仅协议冻结，不涉及 UI 交互。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：无前端变更。

---

## 6. 风险与回滚

- 主要风险：契约定义过宽导致后续实现解读不一致。
- 回滚点：回退 `parallel_plan.md` 与 `contracts/sse_events_v1.json`。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（/vk 机读，必填）

```yaml
card_export:
  id: WS-00
  card_key: PP-20260213-ADMIN-OVERVIEW-COCKPIT::WS-00
  title: G0 协议冻结
  type: foundation
  lane: lane-foundation
  hard_depends_on: []
  soft_depends_on: []
  depends_on: []
  file_whitelist:
    - docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/parallel_plan.md
    - docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/workstreams/WS-00_G0_协议冻结.md
    - docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/contracts/sse_events_v1.json
  readonly_scope:
    - app/
    - web/
  owner_fields:
    - sse.done
    - sse.result
    - sse.interrupt
  check_cmd:
    - venv/bin/python scripts/docs_guard.py --strict
  handoff_artifacts:
    - docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/contracts/sse_events_v1.json
    - docs/内部参考/任务拆解/2026-02-13_管理后台总览驾驶舱/parallel_plan.md
  dod:
    - done/result/interrupt 契约冻结并可机读消费
```
