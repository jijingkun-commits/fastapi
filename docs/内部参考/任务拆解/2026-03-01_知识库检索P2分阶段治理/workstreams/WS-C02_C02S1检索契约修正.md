# 工作包说明：WS-C02 C02 S1 检索契约修正

> WS 编号: WS-C02  
> 名称: C02 S1 检索契约修正  
> 类型: parallel  
> 对应 `feature_id`: P1-01, P1-02

## 0. 关联与来源

- 对应 `task_key`: PP-20260301-KB-RETRIEVAL-P2
- 对应 `task_id`: T-01, T-02
- 来源主计划: `docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md`
- 来源并行计划: `docs/内部参考/任务拆解/2026-03-01_知识库检索P2分阶段治理/parallel_plan.md`

## 1. 目标

- 本包目标:
- 显式区分 page_size 与 top_k 语义并配置化
- 补齐超时与错误降级路径，避免检索失败外抛
- 保持 dataset 选择逻辑兼容

## 2. 文件边界

### 可修改（白名单）
- app/ai/tools/ragflow_tool.py
- app/core/config.py
- tests/unit/test_ragflow_tool.py

### 禁止修改（黑名单）
- 白名单外文件

## 3. 状态与契约

- 可写字段: 当前卡片 `feature_ids` 对应实现字段
- 只读字段: 上一阶段已冻结契约与下游未解锁字段
- 外部契约: `depends_on=['C01']`

## 4. 实施步骤

1. 按 `mechanism_summary` 完成核心改造。
2. 绑定并执行 `acceptance_checks`。
3. 回填证据到 `evidence_entry`。

### 4.1 串行门禁

- 前置卡: C01
- 解锁条件: 当前卡 `acceptance_checks` 全部通过
- 本 WS 不得推进条件: 前置卡未通过或 Gate 阻断

## 5. 测试与验收

- 最小测试集:
  - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "payload or timeout"

### 5.0 验收门禁映射

- 对应 implementation plan `done_gate`:
- 参数契约修正完成且回归通过
- 证据回填位置: `docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md`

## 6. 风险与回滚

- 主要风险: 契约偏移或回归失败导致串行阻塞
- 回滚点:
- 关闭 RAGFLOW_PAGE_SIZE 配置并回退旧 payload
- 回退异常处理到当前稳定分支

## 7. card_export（机读）

```yaml
card_export:
  id: WS-C02
  feature_id: P1-01
  card_key: PP-20260301-KB-RETRIEVAL-P2::WS-C02
  title: C02 S1 检索契约修正
  type: parallel
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  hard_depends_on: ['C01']
  soft_depends_on: []
  depends_on: ['C01']
  file_whitelist:
    - app/ai/tools/ragflow_tool.py
    - app/core/config.py
    - tests/unit/test_ragflow_tool.py
  mechanism_summary:
    - 显式区分 page_size 与 top_k 语义并配置化
    - 补齐超时与错误降级路径，避免检索失败外抛
    - 保持 dataset 选择逻辑兼容
  code_anchor_refs:
    - app/ai/tools/ragflow_tool.py::_call_ragflow_retrieval
    - app/ai/tools/ragflow_tool.py::knowledge_search
    - app/core/config.py::RAGFLOW_*
  example_refs:
    - docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md#L1
  acceptance_checks:
    - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "payload or timeout"
  rollback_anchors:
    - 关闭 RAGFLOW_PAGE_SIZE 配置并回退旧 payload
    - 回退异常处理到当前稳定分支
  evidence_entry: docs/内部参考/迭代需求/知识库检索P2分阶段治理_implementation_plan.md#8
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit/test_ragflow_tool.py -k "payload or timeout"
  done_gate:
    - 参数契约修正完成且回归通过
  pr_id: PR-01
  pr_branch: codex/kb-retrieval-p2-pr-01
  pr_depends_on: []
  pr_subject: S1 参数契约化与稳健性改造
```
