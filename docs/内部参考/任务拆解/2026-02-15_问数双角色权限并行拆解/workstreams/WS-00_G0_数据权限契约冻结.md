# 工作包说明

> WS 编号: WS-00
> 名称: G0 数据权限契约冻结
> 类型: foundation

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260215-ASKDATA-DUAL-ROLE-PERMISSION`
- 来源主计划：`docs/内部参考/迭代需求/askdata_dual_role_permission_implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/parallel_plan.md`

---

## 1. 目标

- 本包目标：冻结问数权限双角色语义、默认行级策略与优先级规则。
- 完成定义（DoD）：
  1. 机读契约文件落地并通过评审。
  2. 并行计划中的 G0 字段与机读契约一致。

---

## 2. 文件边界

### 可修改（白名单）

- `docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/parallel_plan.md`
- `docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/contracts/data_permission_contract_v1.json`
- `docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/workstreams/WS-00_G0_数据权限契约冻结.md`

### 禁止修改（黑名单）

- `app/**`
- `web/**`

---

## 3. 状态与契约

- 可写字段：`identity_contract`、`permission_precedence`、`default_row_policy`。
- 只读字段：业务实现与接口细节。
- 外部契约：`contracts/data_permission_contract_v1.json`。

---

## 4. 实施步骤

1. 固化 `data_role` 枚举代码值。
2. 固化“admin 不默认放行”约束。
3. 固化默认 `dept_code` 行级策略。
4. 回填并行计划中的 G0 冻结区块。

---

## 5. 测试与验收

- 最小测试集：
  - `python3 -m json.tool docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/contracts/data_permission_contract_v1.json > /dev/null`
- 验收标准：
  1. 机读契约为合法 JSON。
  2. 并行计划与机读契约字段一致。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：仅文档契约冻结。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：不涉及浏览器交互。

---

## 6. 风险与回滚

- 主要风险：G0 契约定义不完整导致后续 WS 反复返工。
- 回滚点：回退 `contracts/data_permission_contract_v1.json` 与并行计划 G0 区块。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-00
  card_key: PP-20260215-ASKDATA-DUAL-ROLE-PERMISSION::WS-00
  title: G0 数据权限契约冻结
  type: foundation
  lane: lane-foundation
  hard_depends_on: []
  soft_depends_on: []
  depends_on: []
  file_whitelist:
    - docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/parallel_plan.md
    - docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/contracts/data_permission_contract_v1.json
  readonly_scope:
    - app/
    - web/
  owner_fields:
    - contract.identity_contract
    - contract.permission_precedence
    - contract.default_row_policy
  check_cmd:
    - python3 -m json.tool docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/contracts/data_permission_contract_v1.json > /dev/null
  handoff_artifacts:
    - docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/contracts/data_permission_contract_v1.json
  dod:
    - G0 数据权限契约冻结完成
```
