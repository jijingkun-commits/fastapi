# 工作包说明

> WS 编号: WS-01
> 名称: 用户模型与迁移改造
> 类型: parallel

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260215-ASKDATA-DUAL-ROLE-PERMISSION`
- 来源主计划：`docs/内部参考/迭代需求/askdata_dual_role_permission_implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-15_问数双角色权限并行拆解/parallel_plan.md`

---

## 1. 目标

- 本包目标：引入 `data_role` 字段并完成历史用户回填。
- 完成定义（DoD）：
  1. `t_user` 存在 `data_role` 字段，默认值 `staff`。
  2. 历史数据无 `data_role` 空值。
  3. 用户 schema/API 与新字段兼容。

---

## 2. 文件边界

### 可修改（白名单）

- `app/models/user.py`
- `app/schemas/user.py`
- `app/repositories/user_repo.py`
- `app/services/user_service.py`
- `alembic/env.py`
- `alembic/versions/`

### 禁止修改（黑名单）

- `app/services/permission_service.py`
- `app/ai/utils/sql_rewriter.py`
- `app/api/v1/endpoints/access_admin_api.py`

---

## 3. 状态与契约

- 可写字段：`user.data_role`、`user.schema.data_role`。
- 只读字段：`permission_precedence`、`default_row_policy`（由 WS-00 冻结）。
- 外部契约：`contracts/data_permission_contract_v1.json`。

---

## 4. 实施步骤

1. 在用户模型中新增 `data_role` 字段。
2. 新增 Alembic 迁移，补字段并回填历史数据。
3. 调整用户创建/列表 schema 支持 `data_role`。
4. 保持 `role` 字段作为系统角色兼容入口。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python -m pytest -q tests/unit -k "user and role"`
  - `venv/bin/python -m alembic upgrade head`
- 验收标准：
  1. 迁移成功执行。
  2. 用户接口不会因新增字段破坏兼容。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：后端模型与迁移，不涉及 UI 流程。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：不涉及浏览器交互。

---

## 6. 风险与回滚

- 主要风险：历史数据量大时迁移回填耗时。
- 回滚点：回滚 Alembic 版本与用户字段变更。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（/vk 机读，必填）

```yaml
card_export:
  id: WS-01
  card_key: PP-20260215-ASKDATA-DUAL-ROLE-PERMISSION::WS-01
  title: 用户模型与迁移改造
  type: parallel
  lane: lane-backend-model
  hard_depends_on:
    - WS-00
  soft_depends_on: []
  depends_on:
    - WS-00
  file_whitelist:
    - app/models/user.py
    - app/schemas/user.py
    - app/repositories/user_repo.py
    - app/services/user_service.py
    - alembic/env.py
    - alembic/versions/
  readonly_scope:
    - app/services/permission_service.py
    - app/ai/utils/sql_rewriter.py
  owner_fields:
    - user.data_role
    - user.schema.data_role
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit -k "user and role"
    - venv/bin/python -m alembic upgrade head
  handoff_artifacts:
    - alembic/versions/
  dod:
    - data_role 字段与迁移回填完成
```
