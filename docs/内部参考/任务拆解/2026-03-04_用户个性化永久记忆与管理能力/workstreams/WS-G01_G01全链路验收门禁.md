# G01 Gate 全链路验收门禁

> WS 编号: WS-G01  
> 类型: gate  
> 对应 feature_id: G-1

## 0. 关联与来源

- 对应 `task_key`: `PP-20260304-USER-MEMORY-LLM-ASYNC`
- 来源主计划：`docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-03-04_用户个性化永久记忆与管理能力/parallel_plan.md`

## 1. 目标

- 本包目标：校验全部实现卡 done_gate 与证据绑定
- 完成定义（DoD）：校验全部实现卡 done_gate 与证据绑定; 执行 docs_guard 作为最终文档门禁

### 1.1 功能机制

- 触发条件：当前卡被 `card_order` 激活。
- 输入：上游依赖卡状态与本卡代码锚点。
- 输出：本卡 `acceptance_checks` 全部通过并回填证据。
- 状态流转：Backlog -> Doing -> Review -> Gate。

### 1.2 代码锚点与样例

- 代码锚点：
  - `scripts/check_gate_contract_consistency.py::main`
  - `scripts/docs_guard.py::main`

```python
# 伪代码：执行本卡机制并输出可验证结果
result = execute_g01()
assert result.ok
```

## 2. 文件边界

### 可修改（白名单）
- `scripts/check_gate_contract_consistency.py`
- `scripts/docs_guard.py`

### 禁止修改（黑名单）
- 不在本卡代码锚点中的跨卡文件

## 3. 状态与契约

- 依赖卡：C09
- task_mode：`inspection-card`
- merge_required：`false`
- 外部契约：`planning_contract` 与 `vk_cards` 一致

## 4. 实施步骤

1. 拉取并确认依赖卡状态。
2. 完成机制点并补测试。
3. 执行验收命令并回填证据。

## 5. 测试与验收

- 验收命令：
- `python3 scripts/check_gate_contract_consistency.py --task-split-dir 2026-03-04_用户个性化永久记忆与管理能力`
- `python3 scripts/docs_guard.py --strict`

- 对应 implementation plan `done_gate`：`docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md` 中 `G01` 段。
- 证据回填位置：`docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md`

## 6. 风险与回滚

- 主要风险：跨卡依赖未满足导致误推进。
- 回滚点：
- 停止进入 cardrun 与主干合并

## 7. card_export（机读）

```yaml
card_export:
  id: WS-G01
  card_id: G01
  feature_ids: ["G-1"]
  task_mode: inspection-card
  merge_required: false
  execution_mode: serial
  hard_depends_on: ["C09"]
  acceptance_checks:
    - python3 scripts/check_gate_contract_consistency.py --task-split-dir 2026-03-04_用户个性化永久记忆与管理能力
    - python3 scripts/docs_guard.py --strict
  rollback_anchors:
    - 停止进入 cardrun 与主干合并
  evidence_entry: docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md
  pr_id: PR-G01
  pr_branch: codex/user-memory-async-pr-g01
  pr_depends_on: ["PR-09"]
  pr_subject: "Gate 全链路验收门禁"
```
