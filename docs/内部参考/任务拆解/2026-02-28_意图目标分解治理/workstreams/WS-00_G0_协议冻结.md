# WS-00 C00 预检门禁冻结

> WS 编号: WS-00  
> 对应卡片: `C00`（preflight，不默认落卡）  
> 类型: `foundation`

## 0. 冻结目标

1. 冻结 `execution_mode=serial` 与 `single_active_card=true`。
2. 冻结 `card_order=['C01', 'C02', 'C03', 'C04', 'C05', 'C06', 'C07', 'C08', 'G01', 'G02', 'G03']` 与 Gate 卡片化依赖。
3. 冻结 D+B 降级链口径：`tool_call -> json_object -> text_parse -> heuristic_fallback`。
4. 冻结自动执行作用域三元组：`project_id/task_split_dir/task_key`。

## 1. 预检清单

- [x] 主计划存在并包含 `planning_contract`：`docs/内部参考/迭代需求/意图目标分解治理_implementation_plan.md`
- [x] `gate_contract.mode=as_cards` 且 `gate_ids` 完整：`['G01', 'G02', 'G03']`
- [x] 作用域 project_id 已继承：`2ea99dca-a111-43bb-ae73-f836bafe0fb0`
- [x] `vk_cards.json` 必填字段完整（FAIL_FAST 校验）
- [x] set_active_task 已执行并回读一致

## 2. 协议文件

- 协议机读文件：`docs/内部参考/任务拆解/2026-02-28_意图目标分解治理/contracts/sse_events_v1.json`

## 3. 退出条件

1. `python3 scripts/set_active_task.py --task-split-dir 2026-02-28_意图目标分解治理 --project-id 2ea99dca-a111-43bb-ae73-f836bafe0fb0` 成功。
2. `docs/内部参考/任务拆解/_active_task.json` 与本轮 `task_key/task_split_dir/project_id` 一致。
