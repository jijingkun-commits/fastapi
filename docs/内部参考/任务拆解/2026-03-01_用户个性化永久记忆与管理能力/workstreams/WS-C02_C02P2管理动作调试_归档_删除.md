# WS-C02 C02 P2 管理动作（调试/归档/删除）

> WS 编号: WS-C02  
> 对应卡片: `C02`  
> 类型: `parallel`  
> 对应 `feature_id`: `P1-03,P2-01,P2-02`

## 0. 关联与来源

- 对应 `task_key`: `PP-20260301-USER-MEMORY-ADMIN`
- 来源主计划: `docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md`
- 来源并行计划: `docs/内部参考/任务拆解/2026-03-01_用户个性化永久记忆与管理能力/parallel_plan.md`

## 1. 目标

- 新增 search-debug 供管理员查看片段分数与引用
- 归档采用 active->archived 逻辑状态流转
- 删除接口保持安全边界，优先归档后硬删策略

### 1.1 功能机制（必填）

- 触发条件: 前置依赖 `C01` 完成
- 输入: 上游卡片 done gate 证据 + 当前 feature 配置
- 输出: 卡片验收证据、回滚锚点、审计结果
- 状态流转（含异常分支）: `Backlog -> Doing -> Review -> Gate -> Done`，失败则回滚并标记 blocked
- 与上/下游 WS 的契约关系: 仅在 `depends_on` 满足后推进

### 1.2 代码锚点与样例（必填）

- 代码锚点（函数/类级）:
  - `app/api/v1/endpoints/memory_admin_api.py::search_memory_debug`
  - `app/api/v1/endpoints/memory_admin_api.py::archive_memory`
  - `app/api/v1/endpoints/memory_admin_api.py::delete_memory`
  - `app/repositories/document_memory_repo.py::archive_document`
- 最小样例（可伪代码）:

```python
if checks_passed:
    mark_done(card_id)
else:
    rollback_to_anchor()
```

- 来源证据（output/专题文档）:
  - `docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md`

## 2. 文件边界

### 可修改（白名单）
- `app/api/v1/endpoints/memory_admin_api.py`
- `app/repositories/document_memory_repo.py`
- `app/services/memory_admin_service.py`
- `tests/api/test_memory_admin_api.py`

### 禁止修改（黑名单）
- `docs/内部参考/迭代需求/openclaw迁移重建基线_implementation_plan.md`

## 3. 状态与契约

- 可写字段: `card.status`、`evidence_entry`、`gate_result`
- 只读字段: `card_id`、`feature_ids`、`depends_on`
- 外部契约: `planning_contract` 与 `vk_cards.json` 一致

## 4. 实施步骤

1. 拉取前置卡完成状态并执行本卡前置检查。
2. 运行 `acceptance_checks` 并记录输出。
3. 回填证据与回滚锚点结果。

### 4.1 串行门禁（serial 模式必填）

- 前置卡: `C01`
- 解锁条件: `depends_on` 对应卡片全部通过
- 本 WS 不得推进条件: 任一 `acceptance_checks` 失败

## 5. 测试与验收

- 最小测试集:
- `cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k search_debug`
- `cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k archive`
- `cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k delete`
- 验收标准:
- search-debug/archive/delete 均通过接口回归
- 删除与归档幂等行为验证通过

### 5.0 验收门禁映射（必填）

- 对应 implementation plan `done_gate`: `search-debug/archive/delete 均通过接口回归; 删除与归档幂等行为验证通过`
- 本 WS 负责的门禁子项: `C02:P1-03,P2-01,P2-02`
- 证据回填位置（文档节）: `docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md#9`

## 6. 风险与回滚

- 主要风险: 前置卡未完成、字段契约漂移、测试结果不稳定
- 回滚点:
- 关闭 search-debug 路由
- 关闭 delete 路由仅保留 archive
- 回滚开关/策略: 仅在本卡范围回滚，禁止跨卡改动

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表: 见白名单
- 是否修改了白名单外文件（是/否）: 否
- 测试命令与结果: 见 acceptance_checks
- 已知风险点: 串行链路断裂导致阻塞
- 回滚建议: 先回滚本卡，再评估下游
- 证据绑定检查（target_task_id == evidence_task_id）: 必填

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-C02
  feature_id: P1-03,P2-01,P2-02
  card_key: PP-20260301-USER-MEMORY-ADMIN::WS-C02
  title: C02 P2 管理动作（调试/归档/删除）
  type: parallel
  task_mode: implementation-card
  merge_required: true
  execution_mode: serial
  lane: lane-implementation
  hard_depends_on: [C01]
  soft_depends_on: []
  depends_on: [C01]
  file_whitelist:
    - app/api/v1/endpoints/memory_admin_api.py
    - app/repositories/document_memory_repo.py
    - app/services/memory_admin_service.py
    - tests/api/test_memory_admin_api.py
  readonly_scope: []
  owner_fields: []
  mechanism_summary:
    - 新增 search-debug 供管理员查看片段分数与引用
    - 归档采用 active->archived 逻辑状态流转
    - 删除接口保持安全边界，优先归档后硬删策略
  code_anchor_refs:
    - app/api/v1/endpoints/memory_admin_api.py::search_memory_debug
    - app/api/v1/endpoints/memory_admin_api.py::archive_memory
    - app/api/v1/endpoints/memory_admin_api.py::delete_memory
    - app/repositories/document_memory_repo.py::archive_document
  example_refs:
    - docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md#4
  acceptance_checks:
    - cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k search_debug
    - cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k archive
    - cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k delete
  rollback_anchors:
    - 关闭 search-debug 路由
    - 关闭 delete 路由仅保留 archive
  evidence_entry: docs/内部参考/迭代需求/用户个性化永久记忆与管理能力_implementation_plan.md#9
  check_cmd:
    - cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k search_debug
    - cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k archive
    - cd /Users/jijingkun/bojxAI/fastapi && venv/bin/python -m pytest -q tests/api/test_memory_admin_api.py -k delete
  handoff_artifacts:
    - docs/内部参考/任务拆解/2026-03-01_用户个性化永久记忆与管理能力/contracts/sse_events_v1.json
  done_gate:
    - search-debug/archive/delete 均通过接口回归
    - 删除与归档幂等行为验证通过
```
