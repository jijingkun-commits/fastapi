# 任务拆解（旧入口说明）

> 这里不再是 task_split 的 canonical 根目录。

## 当前口径

- task_split canonical 根目录：`workdocs/任务拆解/`
- 机器契约：`workdocs/任务拆解/<task_split_dir>/contracts/`
- 过程报告：`workdocs/任务拆解/<task_split_dir>/reports/`
- 真实运行态：`.artifacts/states/task_splits/<task_split_dir>/<task_key>/`

## 这里保留什么

- 仅保留这份说明页，避免旧链接完全失去指向。
- 不再在本目录新增 task_split 机器 JSON、过程 JSON、workstreams 或 parallel_plan。
