# 工作包说明

> WS 编号: WS-03
> 名称: 编排接入与回归测试闭环
> 类型: parallel

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260216-TOOL-GOVERNANCE`
- 来源主计划：`docs/内部参考/迭代需求/implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-16_AI工具治理架构并行拆解/parallel_plan.md`

---

## 1. 目标

- 本包目标：完成多智能体编排接线，并闭合治理回归测试链路。
- 完成定义（DoD）：
  1. `multi_agent_graph` 工具获取路径接入 Registry + Policy。
  2. Hook 开关打开时可接入 WS-02 能力，关闭时保持兼容旧路径。
  3. `done/result/interrupt` required 字段保持稳定，新增字段仅可选扩展。

---

## 2. 文件边界

### 可修改（白名单）

- `app/ai/workflow/multi_agent_graph.py`
- `app/services/chat_service.py`
- `app/ai/events.py`
- `tests/integration/test_tool_policy_in_graph.py`
- `tests/integration/test_tool_lifecycle_events.py`
- `tests/unit/test_multi_agent_fallback.py`
- `tests/unit/test_chat_service_done_payload.py`

### 禁止修改（黑名单）

- `app/ai/tools/registry.py`
- `app/ai/tools/policy.py`
- `app/ai/tools/hooks.py`

---

## 3. 状态与契约

- 可写字段：`workflow.allowed_tools`、`decision_trace`、`chat_service.tool_event_adapter`。
- 只读字段：`sse_events_v1.json` 中 required 字段。
- 外部契约：`contracts/sse_events_v1.json`，`WS-02` 输出的 Hook 接口。

---

## 4. 实施步骤

1. 将 `_get_common_tools()`、`_get_supervisor_tools()` 改为治理层获取路径。
2. 在编排层接入策略降级语义与 fallback 保护。
3. 对齐 `chat_service` 与 `events` 的兼容消费，保持 `done/result/interrupt` 语义不变。
4. 补充图接线与事件兼容回归测试。
5. 回填 Gate 输入所需的测试证据路径。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python -m pytest -q tests/integration/test_tool_policy_in_graph.py`
  - `venv/bin/python -m pytest -q tests/integration/test_tool_lifecycle_events.py`
  - `venv/bin/python -m pytest -q tests/unit/test_multi_agent_fallback.py tests/unit/test_chat_service_done_payload.py`
- 验收标准：
  1. 开关关闭时行为与旧逻辑一致。
  2. 开关开启时工具集按策略可预测收敛。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：后端编排与协议兼容回归。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：不涉及前端交互变更。

---

## 6. 风险与回滚

- 主要风险：接入点改动导致工具路由退化或性能抖动。
- 回滚点：关闭治理开关并回退 `multi_agent_graph` 新接线。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（/vk 机读，必填）

```yaml
card_export:
  id: WS-03
  card_key: PP-20260216-TOOL-GOVERNANCE::WS-03
  title: 编排接入与回归测试闭环
  type: parallel
  lane: lane-backend-integration
  hard_depends_on:
    - WS-01
  soft_depends_on:
    - WS-02
  depends_on:
    - WS-01
    - WS-02
  file_whitelist:
    - app/ai/workflow/multi_agent_graph.py
    - app/services/chat_service.py
    - app/ai/events.py
    - tests/integration/test_tool_policy_in_graph.py
    - tests/integration/test_tool_lifecycle_events.py
    - tests/unit/test_multi_agent_fallback.py
    - tests/unit/test_chat_service_done_payload.py
  readonly_scope:
    - app/ai/tools/registry.py
    - app/ai/tools/policy.py
    - app/ai/tools/hooks.py
  owner_fields:
    - workflow.allowed_tools
    - workflow.decision_trace
    - events.compatibility
  check_cmd:
    - venv/bin/python -m pytest -q tests/integration/test_tool_policy_in_graph.py
    - venv/bin/python -m pytest -q tests/integration/test_tool_lifecycle_events.py
    - venv/bin/python -m pytest -q tests/unit/test_multi_agent_fallback.py tests/unit/test_chat_service_done_payload.py
  handoff_artifacts:
    - app/ai/workflow/multi_agent_graph.py
    - tests/integration/test_tool_policy_in_graph.py
  dod:
    - 编排接线完成且关键回归测试通过
```

