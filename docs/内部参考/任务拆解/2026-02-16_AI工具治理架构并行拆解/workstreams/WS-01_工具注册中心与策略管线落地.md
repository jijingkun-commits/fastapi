# 工作包说明

> WS 编号: WS-01
> 名称: 工具注册中心与策略管线落地
> 类型: parallel

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260216-TOOL-GOVERNANCE`
- 来源主计划：`docs/内部参考/迭代需求/ai_tools_governance_implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-16_AI工具治理架构并行拆解/parallel_plan.md`

---

## 1. 目标

- 本包目标：交付 `ToolRegistry + ToolPolicyPipeline + ToolPolicyStore`，并打通治理配置入口。
- 完成定义（DoD）：
  1. `registry/policy/policy_store` 模块可独立运行。
  2. 治理配置键完成 `settings.py + config_contract` 双入口收敛。
  3. 空策略兼容、`deny` 优先、`group/glob` 表达可验证。

---

## 2. 文件边界

### 可修改（白名单）

- `app/ai/tools/registry.py`
- `app/ai/tools/policy.py`
- `app/ai/tools/policy_store.py`
- `app/core/settings.py`
- `app/core/config_contract.py`
- `app/services/config_resolver.py`
- `tests/unit/test_tool_registry.py`
- `tests/unit/test_tool_policy.py`
- `tests/unit/test_tool_policy_store.py`

### 禁止修改（黑名单）

- `app/ai/tools/hooks.py`
- `app/services/tool_audit_service.py`
- `app/ai/workflow/multi_agent_graph.py`

---

## 3. 状态与契约

- 可写字段：`registry.tool_metadata`、`policy.rules`、`policy_store.cache`、`settings.tool_governance.*`。
- 只读字段：`done/result/interrupt` 契约（由 WS-00 冻结）。
- 外部契约：`contracts/sse_events_v1.json`、`chat_db.t_system_config` 键集合。

---

## 4. 实施步骤

1. 建立 Registry 元数据模型与分组查询能力。
2. 建立 Policy Pipeline（Global/Agent/Role/Scene）与 `deny` 优先规则。
3. 建立 DB 策略源读取与缓存刷新能力（经 `ConfigResolver`）。
4. 在配置层补齐治理开关、策略来源、缓存 TTL、fail_mode。
5. 补充单元测试并验证兼容行为。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python -m pytest -q tests/unit/test_tool_registry.py`
  - `venv/bin/python -m pytest -q tests/unit/test_tool_policy.py`
  - `venv/bin/python -m pytest -q tests/unit/test_tool_policy_store.py`
- 验收标准：
  1. 策略命中链路可追溯。
  2. DB 不可用时按 `settings` 降级，不中断主流程。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：后端治理底座改造，不涉及前端流程。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：不涉及浏览器交互。

---

## 6. 风险与回滚

- 主要风险：策略配置误配导致工具误杀。
- 回滚点：`TOOL_GOVERNANCE_ENABLED=false`，并回退 `registry/policy` 接线。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-01
  card_key: PP-20260216-TOOL-GOVERNANCE::WS-01
  title: 工具注册中心与策略管线落地
  type: parallel
  lane: lane-backend-core
  hard_depends_on:
    - WS-00
  soft_depends_on: []
  depends_on:
    - WS-00
  file_whitelist:
    - app/ai/tools/registry.py
    - app/ai/tools/policy.py
    - app/ai/tools/policy_store.py
    - app/core/settings.py
    - app/core/config_contract.py
    - app/services/config_resolver.py
    - tests/unit/test_tool_registry.py
    - tests/unit/test_tool_policy.py
    - tests/unit/test_tool_policy_store.py
  readonly_scope:
    - app/ai/workflow/
    - app/services/chat_service.py
  owner_fields:
    - registry.tool_metadata
    - policy.pipeline
    - policy_store.cache
    - settings.tool_governance
  check_cmd:
    - venv/bin/python -m pytest -q tests/unit/test_tool_registry.py
    - venv/bin/python -m pytest -q tests/unit/test_tool_policy.py
    - venv/bin/python -m pytest -q tests/unit/test_tool_policy_store.py
  handoff_artifacts:
    - app/ai/tools/registry.py
    - app/ai/tools/policy.py
    - app/ai/tools/policy_store.py
  dod:
    - Registry + Policy + PolicyStore 可运行并通过单测
```
