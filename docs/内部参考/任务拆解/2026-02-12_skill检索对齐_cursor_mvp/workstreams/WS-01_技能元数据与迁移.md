# 工作包说明

> WS 编号: WS-01  
> 名称: 技能元数据与迁移  
> 类型: Backend（可并行）

---

## 0. 关联文档

1. `docs/内部参考/迭代需求/requirements.md`
2. `docs/内部参考/迭代需求/implementation_plan.md`
3. `docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md`

---

## 1. 目标与完成定义

### 1.1 目标

1. 为 `t_agent_skills` 增补元数据治理字段。
2. 提供可回滚的迁移脚本与索引。
3. 保持旧数据可读写兼容。

### 1.2 DoD

1. 模型字段与迁移脚本一致。
2. 旧数据无需人工清洗即可迁移。
3. 迁移可逆且不破坏现有 embedding 检索。

---

## 2. 文件边界

### 2.1 可修改（白名单）

- `app/models/agent_skill.py`
- `alembic/versions/*`
- `tests/unit/test_*agent_skill*`

### 2.2 禁止修改（黑名单）

- `app/services/skill_service.py`
- `app/ai/workflow/*`
- `web/src/*`

---

## 3. 状态字段单写入权

- WS-01 可写：技能元数据字段定义与默认值语义。
- WS-01 只读：检索评分与注入字段语义。

---

## 4. 实施步骤

1. 扩展 `AgentSkill` 模型字段与注释。
2. 生成并校验 Alembic 迁移（含 downgrade）。
3. 补充模型/迁移单测。

---

## 5. 局部验收与最小验证命令

- `venv/bin/python -m pytest -q tests/unit -k "agent_skill"`

验收标准：

1. 模型字段定义与数据库一致。
2. 迁移/回滚可执行。

---

## 6. 风险与回滚

- 风险：历史数据 NULL 值导致运行时分支分裂。
- 回滚：回退迁移版本与模型字段定义。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## card_export

```yaml
id: WS-01
title: 技能元数据与迁移
type: parallel
lane: backend-model
depends_on: []
file_whitelist:
  - app/models/agent_skill.py
  - alembic/versions/
readonly_scope:
  - app/services/skill_service.py
  - app/ai/workflow/
owner_fields:
  - auto_enabled
  - priority
  - scope
  - trigger_phrases
  - conflicts_with
check_cmd: venv/bin/python -m pytest -q tests/unit -k "agent_skill"
dod: 元数据字段迁移完成并兼容旧数据
```

