# 工作包说明

> WS 编号: WS-03  
> 名称: 混合检索与注入策略  
> 类型: Backend/Workflow（可并行）

---

## 0. 关联文档

1. `docs/内部参考/迭代需求/requirements.md`
2. `docs/内部参考/迭代需求/implementation_plan.md`
3. `docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md`

---

## 1. 目标与完成定义

### 1.1 目标

1. 将技能检索升级为关键词+向量混合召回。
2. 引入融合排序、冲突裁决、作用域过滤。
3. 注入阶段改为章节/段落级懒加载并遵守预算。

### 1.2 DoD

1. 检索结果可解释（含 score 明细）。
2. 冲突技能不会同时注入。
3. 注入内容在 token budget 内。

---

## 2. 文件边界

### 2.1 可修改（白名单）

- `app/services/skill_service.py`
- `app/ai/workflow/multi_agent_graph.py`
- `tests/unit/test_*skill*`
- `app/tests/test_skill_retrieval_smoke.py`

### 2.2 禁止修改（黑名单）

- `app/models/*`
- `web/src/*`

---

## 3. 状态字段单写入权

- WS-03 可写（owner）：
  - `skill_candidates`
  - `selected_skill_ids`
  - `skill_context`
  - `skill_injection_meta`
- WS-03 只读：模型字段结构定义（WS-01 owner）。

---

## 4. 实施步骤

1. 增加混合召回实现与融合排序。
2. 增加 `auto_enabled/scope/conflicts` 预过滤与裁决。
3. 在 workflow 中落地懒加载与预算控制注入。

---

## 5. 局部验收与最小验证命令

- `venv/bin/python -m pytest -q app/tests/test_skill_retrieval_smoke.py`
- `venv/bin/python -m pytest -q tests/unit -k "skill and retrieval"`

验收标准：

1. 混合召回链路可跑通。
2. 排序与裁决结果稳定可解释。

---

## 6. 风险与回滚

- 风险：融合权重不当导致误召回。
- 回滚：通过配置开关回退到纯向量检索。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## card_export

```yaml
id: WS-03
title: 混合检索与注入策略
type: parallel
lane: backend-retrieval
depends_on:
  - WS-01
  - WS-02
file_whitelist:
  - app/services/skill_service.py
  - app/ai/workflow/multi_agent_graph.py
readonly_scope:
  - app/models/
owner_fields:
  - skill_candidates
  - selected_skill_ids
  - skill_context
check_cmd: venv/bin/python -m pytest -q app/tests/test_skill_retrieval_smoke.py
dod: hybrid召回、冲突裁决、懒加载注入生效
```

