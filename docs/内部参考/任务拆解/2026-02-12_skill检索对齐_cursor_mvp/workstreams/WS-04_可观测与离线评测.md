# 工作包说明

> WS 编号: WS-04  
> 名称: 可观测与离线评测  
> 类型: Observability/Test（可并行）

---

## 0. 关联文档

1. `docs/内部参考/迭代需求/requirements.md`
2. `docs/内部参考/迭代需求/implementation_plan.md`
3. `docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md`

---

## 1. 目标与完成定义

### 1.1 目标

1. 建立检索链路结构化日志。
2. 补齐离线评测样例与回归命令。
3. 提供对比基线数据，支持调参。

### 1.2 DoD

1. 检索日志字段完整且可关联 trace。
2. 离线评测脚本可执行并生成结果。
3. 回归报告可复用到后续迭代。

---

## 2. 文件边界

### 2.1 可修改（白名单）

- `tests/**`
- `scripts/**`
- `docs/内部参考/迭代需求/evaluation_report.md`

### 2.2 禁止修改（黑名单）

- `app/models/*`
- `app/ai/workflow/*`
- `web/src/*`

---

## 3. 状态字段单写入权

- WS-04 可写：观测日志结构定义与评测报告模板。
- WS-04 只读：检索与注入业务语义。

---

## 4. 实施步骤

1. 设计并落地检索日志字段规范。
2. 增加离线评测数据样例与执行脚本。
3. 生成评测报告并记录基线对比。

---

## 5. 局部验收与最小验证命令

- `venv/bin/python -m pytest -q tests -k "skill"`

验收标准：

1. 日志字段可追溯 query->candidates->final。
2. 离线评测可稳定复现。

---

## 5.1 浏览器测试触发评估

- 是否触发浏览器测试：否
- 触发依据：本 WS 为后端观测与测试资产，不涉及前端交互行为。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：无 UI 代码变更。

---

## 6. 风险与回滚

- 风险：评测集覆盖不足导致误判。
- 回滚：保留基线评测脚本并逐步扩展样本。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## card_export

```yaml
card_export:
  id: WS-04
  card_key: PP-20260213-SKILL-RETRIEVAL-MVP::WS-04
  title: 可观测与离线评测
  type: parallel
  lane: lane-observability
  hard_depends_on:
    - WS-00
    - WS-03
  soft_depends_on:
    - WS-01
    - WS-02
  depends_on:
    - WS-00
    - WS-03
  file_whitelist:
    - app/services/skill_service.py
    - tests/
    - scripts/
    - docs/内部参考/迭代需求/evaluation_report.md
  readonly_scope:
    - app/models/
    - app/ai/workflow/
  owner_fields:
    - retrieval_log.thread_id
    - retrieval_log.trace_id
    - retrieval_log.query_hash
    - retrieval_log.selected_skill_ids
  check_cmd:
    - venv/bin/python -m pytest -q tests -k "skill"
  handoff_artifacts:
    - docs/内部参考/迭代需求/evaluation_report.md
    - tests/
  dod:
    - 检索链路日志可追溯，离线评测可执行
```
