# 工作包说明

> WS 编号: WS-00  
> 名称: G0 协议冻结  
> 类型: Foundation（串行前置）

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260213-SKILL-RETRIEVAL-MVP`
- 来源主计划：`docs/内部参考/迭代需求/implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md`

---

## 1. 目标

- 本包目标：冻结 `done/result/interrupt` 契约 required/optional 与兼容策略。
- 执行方式：本包默认在 `/rwfj` 阶段生成并完成，不要求额外执行 `/imp-ws WS-00`。
- 完成定义（DoD）：
  - 并行层 WS 全部声明 `hard_depends_on: [WS-00]`。
  - `parallel_plan.md` 中 G0 冻结章节完整。
  - 机读协议文件可被下游 WS 引用。

---

## 2. 文件边界

### 2.1 可修改（白名单）

- `docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md`
- `docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/workstreams/WS-00_G0_协议冻结.md`
- `docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/contracts/sse_events_v1.json`

### 2.2 禁止修改（黑名单）

- `app/**`
- `web/**`

---

## 3. 状态与契约

- 可写字段：`sse.done`、`sse.result`、`sse.interrupt` 的文档与机读契约定义。
- 只读字段：业务实现状态字段（如 `skill_candidates`、`selected_skill_ids`、`skill_context`）。
- 外部契约：`docs/开发文档/代码解读/SSE事件协议.md`（权威来源）。

---

## 4. 实施步骤

1. 对齐主计划与 SSE 权威文档，冻结三类事件 required/optional。
2. 生成机读协议文件 `contracts/sse_events_v1.json`。
3. 在各 WS `card_export` 中补齐对 `WS-00` 的硬依赖。

---

## 5. 测试与验收

- 最小测试集：
  - `venv/bin/python scripts/docs_guard.py --strict`
- 验收标准：
  - G0 协议冻结内容与看板导出索引一致。
  - 所有 WS 的依赖图包含 `WS-00` 前置约束。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：本 WS 仅文档与协议冻结，不涉及前端交互。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因（如不触发则必填）：无 UI 改动。

---

## 6. 风险与回滚

- 主要风险：冻结字段与实现偏差导致后续 WS 假设不一致。
- 回滚点：回退 `parallel_plan.md` 与 `contracts/sse_events_v1.json` 到上一版。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（机读，必填）

```yaml
card_export:
  id: WS-00
  card_key: PP-20260213-SKILL-RETRIEVAL-MVP::WS-00
  title: G0 协议冻结
  type: foundation
  lane: lane-foundation
  hard_depends_on: []
  soft_depends_on: []
  depends_on: []
  file_whitelist:
    - docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md
    - docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/workstreams/WS-00_G0_协议冻结.md
    - docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/contracts/sse_events_v1.json
  readonly_scope:
    - app/
    - web/
  owner_fields:
    - sse.done
    - sse.result
    - sse.interrupt
  check_cmd:
    - venv/bin/python scripts/docs_guard.py --strict
  handoff_artifacts:
    - docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/contracts/sse_events_v1.json
    - docs/内部参考/任务拆解/2026-02-12_skill检索对齐_cursor_mvp/parallel_plan.md
  dod:
    - done/result/interrupt 契约冻结并可机读消费
```
