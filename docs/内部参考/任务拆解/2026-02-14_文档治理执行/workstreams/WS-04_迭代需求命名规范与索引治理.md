# 工作包说明

> WS 编号: WS-04
> 名称: 迭代需求命名规范与索引治理
> 类型: parallel

---

## 0. 关联与来源

- 对应 `task_key`: `PP-20260214-DOCS-GOVERNANCE`
- 来源主计划：`docs/内部参考/迭代需求/docs_governance_implementation_plan.md`
- 来源并行计划：`docs/内部参考/任务拆解/2026-02-14_文档治理执行/parallel_plan.md`

---

## 1. 目标

- 本包目标：在保持目录结构不变前提下，统一 `迭代需求` 命名规则与索引可达性，并补模板“示例占位”提示。
- 完成定义（DoD）：
  1. `开发工作流.md` 模板代码显式标注“示例占位”。
  2. `内部参考/迭代需求` 的命名规则有明确文档入口。
  3. `SUMMARY` 对迭代需求文档索引完整且命名口径一致。

---

## 2. 文件边界

### 可修改（白名单）

- `docs/开发文档/工作流/开发工作流.md`
- `docs/内部参考/迭代需求/README.md`（可新增）
- `docs/SUMMARY.md`
- `docs/文档梳理与重构方案_2026-02-13.md`

### 禁止修改（黑名单）

- `docs/开发文档/代码解读/*`
- `docs/开发文档/架构设计/*`
- `.cursor/commands/*.md`

---

## 3. 状态与契约

- 可写字段：命名规范说明、索引入口、模板占位标记。
- 只读字段：目录结构策略（不引入双层归档）。
- 外部契约：`naming_rule` 与 `placeholder_rule`。

---

## 4. 实施步骤

1. 在 `开发工作流.md` 模板段补“示例占位”提示。
2. 新增或更新 `迭代需求` 目录命名规范说明文档。
3. 回填 `SUMMARY` 的迭代需求入口，保证索引与命名一致。

---

## 5. 测试与验收

- 最小测试集：
  - `python3 scripts/docs_guard.py --strict`
  - `rg -n '示例占位' docs/开发文档/工作流/开发工作流.md`
- 验收标准：
  1. 索引覆盖率 100%。
  2. 命名规则与已拍板决策一致。

### 5.1 浏览器测试（触发式）

- 是否触发浏览器测试（是/否）：否
- 触发依据（命中项）：仅文档索引与模板注释治理。
- 执行命令：N/A
- 结果与证据路径：N/A
- 未执行原因：不涉及浏览器行为链路。

---

## 6. 风险与回滚

- 主要风险：索引更新遗漏导致 docs_guard 失败。
- 回滚点：回退 `SUMMARY` 与命名规范文档后重新执行门禁。

---

## 7. 协作者自检卡（提交必填）

- 实际修改文件列表：
- 是否修改了白名单外文件（是/否）：
- 测试命令与结果：
- 已知风险点：
- 回滚建议：

---

## 8. card_export（/vk 机读，必填）

```yaml
card_export:
  id: WS-04
  card_key: PP-20260214-DOCS-GOVERNANCE::WS-04
  title: 迭代需求命名规范与索引治理
  type: parallel
  lane: lane-docs-index
  hard_depends_on:
    - WS-00
  soft_depends_on:
    - WS-02
    - WS-03
  depends_on:
    - WS-00
  file_whitelist:
    - docs/开发文档/工作流/开发工作流.md
    - docs/内部参考/迭代需求/README.md
    - docs/SUMMARY.md
    - docs/文档梳理与重构方案_2026-02-13.md
  readonly_scope:
    - docs/开发文档/代码解读/
    - docs/开发文档/架构设计/
    - .cursor/commands/
  owner_fields:
    - docs.iteration_requirements.naming_rule
    - docs.summary.index_coverage
    - docs.template.placeholder_marker
  check_cmd:
    - python3 scripts/docs_guard.py --strict
    - rg -n '示例占位' docs/开发文档/工作流/开发工作流.md
  handoff_artifacts:
    - docs/SUMMARY.md
    - docs/内部参考/迭代需求/README.md
  dod:
    - 命名规范与索引规则落地且门禁通过
```
