# 安装部署

本文档介绍如何在本地开发环境或生产环境中部署项目。

## 系统要求

| 组件 | 版本要求 |
|------|----------|
| Python | ≥ 3.11 |
| Node.js | ≥ 18 (前端) |
| pnpm | ≥ 10.5.1 (前端包管理) |
| PostgreSQL | **≥ 16** (需安装 `pgvector` 扩展用于问数功能) |
| MinIO | 最新版 |

---

## 方式一：Docker Compose（推荐）

最快速的启动方式，自动配置所有依赖服务。

### 1. 克隆项目

```bash
git clone <repo-url>
cd fastapi
```

### 2. 配置环境变量

```bash
cp .env.example .env.dev
# 编辑 .env.dev，配置必要参数（参考 [配置说明](配置说明.md) 章节）
```

### 3. 启动基础设施

```bash
# 仅启动 PostgreSQL + MinIO（开发模式）
docker compose up -d

# 查看服务状态
docker compose ps
```

服务启动后：
- **PostgreSQL**: `localhost:5432`
- **MinIO API**: `localhost:19000`
- **MinIO Console**: `localhost:19001` (用户名: admin, 密码: 12345678)

### 4. 启动后端

```bash
# 安装 Python 依赖
pip install -e ".[dev]"

# 运行后端服务
uvicorn app.main:app --reload --port 8000
```

### 5. 启动前端

```bash
cd web
pnpm install
pnpm dev
```

前端访问地址：`http://localhost:3000`

---

## 方式二：完整容器部署

启动包含应用服务的完整容器集群。

```bash
# 启动所有服务（基础设施 + 应用）
docker compose --profile app up -d
```

服务列表：
- **后端 API**: `http://localhost:8000`
- **前端 Web**: `http://localhost:3000`
- **PostgreSQL**: `localhost:5432`
- **MinIO**: `localhost:19000`

---

## 方式三：本地开发（无 Docker）

### 1. 安装 PostgreSQL

```bash
# macOS
brew install postgresql@15
brew services start postgresql@15

# 创建数据库
createdb chat_db
```

### 2. 初始化数据库表

```bash
psql -d chat_db -f install/sql/init_postgres.sql
psql -d chat_db -f install/sql/003_llm_config.sql
psql -d chat_db -f install/sql/004_create_todo_table.sql
```

### 3. 安装 MinIO

```bash
# macOS
brew install minio/stable/minio
minio server ~/minio-data --console-address ":9001"
```

### 4. 初始化 MinIO Bucket

```bash
cd install/scripts
python init_minio_buckets.py
```

### 5. 配置环境变量

```bash
cp .env.example .env.dev
# 根据实际情况修改 DATABASE_URL、MINIO_ENDPOINT 等
```

### 6. 启动服务

```bash
# 后端
pip install -e ".[dev]"
uvicorn app.main:app --reload

# 前端（新终端）
cd web && pnpm install && pnpm dev
```

---

## 生产部署注意事项

### 环境变量

生产环境需要使用 `.env.prod` 配置：

```bash
# 必须修改的配置
JWT_SECRET=<生成一个强随机字符串>
DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname
CORS_ALLOW_ORIGINS=https://your-domain.com
```

### 安全配置

- 禁用 `INIT_DB_ON_STARTUP`
- 设置强随机 `JWT_SECRET`
- 配置 CORS 白名单
- 使用 HTTPS

### 日志配置

```bash
LOG_LEVEL=INFO
LOG_FILE=logs/assistant.log
```

---

## 常见问题

### PostgreSQL 连接失败

确认数据库服务已启动：
```bash
docker compose ps  # Docker 模式
pg_isready         # 本地模式
```

### MinIO 无法访问

检查端口是否被占用：
```bash
lsof -i :19000
```

### 前端无法连接后端

确认后端 CORS 配置：
```bash
# .env.dev
CORS_ALLOW_ORIGINS=http://localhost:3000
```

---

## AI Skills RAG 配置与同步

本项目集成了 **Agent Skills RAG** 机制，允许 AI 在运行时动态检索业务技能。

### 1. 数据库准备

确保已安装 `pgvector` 扩展并执行了最新的数据库迁移：

```bash
# 激活虚拟环境
python scripts/init_tables_ci.py  # 初始化表结构（含 t_agent_skills）

# 或者手动执行 Alembic
alembic revision --autogenerate -m "add_t_agent_skills"
alembic upgrade head
```

### 2. 配置 Embedding 模型

AI 技能检索依赖向量化模型。请确保在数据库 (`t_llm_models`) 中配置了 **Embedding 模型**：

- **Provider**: `zhipu` (推荐) 或 `openai`
- **Model Type**: `embedding`
- **Model Name**: `embedding-3` (智谱，1024维)

### 3. 初始化/更新技能库

当添加或修改 `app/data/skills/` 目录下的 `SKILL.md` 文件后，需运行同步脚本：

```bash
# 全量导入/更新（自动检测变更）
python scripts/import_skills.py

# 强制重新导入所有技能
python scripts/import_skills.py --force

# 指定技能目录（可选）
python scripts/import_skills.py --source /path/to/custom_skills
```

> **提示**: 应用启动时也会尝试自动增量同步，但在生产环境中建议使用脚本手动触发以确保可控。

### 4. 验证安装

在聊天窗口输入指令验证：
> "帮我记录一个待办"

检查后端日志，若出现以下内容则说明 RAG 检索正常：
```text
[INFO] 预处理节点: 检索到 1 个相关技能: ['todo-intent']
```
