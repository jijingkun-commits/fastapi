# 后端架构

本文档详细介绍后端的分层架构、核心模块和设计模式。

## 目录结构

```
app/
├── api/                    # API 层
│   └── v1/
│       ├── endpoints/      # 端点实现
│       └── router.py       # 路由注册
├── core/                   # 核心基础设施
│   ├── config.py           # 配置管理
│   ├── security.py         # 认证安全
│   ├── logging.py          # 日志配置
│   └── handlers.py         # 异常处理器
├── db/                     # 数据库层
│   ├── session.py          # 会话管理
│   └── postgres_checkpoint.py  # LangGraph Checkpointer
├── models/                 # ORM 模型
├── repositories/           # 数据访问层
├── schemas/                # Pydantic 模型
├── services/               # 业务逻辑层
└── ai/                     # AI 模块
```

---

## 分层设计

### 1. API 层 (`app/api/`)

**职责**：HTTP 入口、参数校验、路由定义

```python
# 典型端点结构
@router.post("/chat/stream")
async def chat_stream(
    request: ChatRequest,
    user: User = Depends(get_current_user)
):
    return EventSourceResponse(
        chat_service.sse_stream(request, user.id)
    )
```

**关键点**：
- 使用 `Depends` 注入认证、数据库会话
- 返回 Pydantic Schema 自动序列化
- SSE 流使用 `EventSourceResponse`

### 2. Service 层 (`app/services/`)

**职责**：业务编排、跨模块协调

| 服务 | 文件 | 职责 |
|------|------|------|
| `ChatService` | `chat_service.py` | 管理聊天流程、SSE 事件生成 |
| `TodoService` | `todo_service.py` | 待办业务逻辑 |
| `LLMConfigService` | `llm_config_service.py` | LLM 模型配置管理 |
| `SystemConfigService` | `system_config_service.py` | 系统配置管理 |

**设计原则**：
- Service 调用 Repository，不直接操作 ORM Session
- Service 可调用其他 Service，但避免循环依赖

### 3. Repository 层 (`app/repositories/`)

**职责**：数据访问、CRUD 操作

```python
class TodoRepository:
    def __init__(self, db: Session):
        self.db = db
    
    def create(self, user_id: int, data: dict) -> Todo:
        todo = Todo(user_id=user_id, **data)
        self.db.add(todo)
        self.db.commit()
        return todo
    
    def find_by_id(self, todo_id: int) -> Optional[Todo]:
        return self.db.query(Todo).filter(Todo.id == todo_id).first()
```

**设计原则**：
- 每个 Repository 对应一个聚合根
- 返回 ORM 模型对象，不返回字典

### 4. Model 层 (`app/models/`)

**职责**：ORM 定义、表结构

使用 SQLAlchemy 2.0 `Mapped` 语法：

```python
class Todo(Base):
    __tablename__ = "t_todo"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    user_id: Mapped[int] = mapped_column(Integer, nullable=False)
    title: Mapped[str] = mapped_column(String(500), nullable=False)
    status: Mapped[str] = mapped_column(String(50), default="pending")
```

---

## 依赖注入

使用 FastAPI 的 `Depends` 实现依赖注入：

```python
# 获取数据库会话
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

# 获取当前用户
async def get_current_user(
    token: str = Depends(oauth2_scheme),
    db: Session = Depends(get_db)
) -> User:
    return verify_token(token, db)
```

---

## 异常处理

集中在 `app/core/handlers.py`：

| 异常类型 | 处理方式 |
|----------|----------|
| `HTTPException` | 返回对应状态码和消息 |
| `RequestValidationError` | 422 + 详细字段错误 |
| 未知异常 | 500 + 日志记录 |

---

## 日志系统

配置在 `app/core/logging.py`：

- **控制台**：彩色输出（开发模式）
- **文件**：`logs/assistant.log`，按日轮转
- **级别**：开发 DEBUG，生产 INFO

---

## 认证机制

使用 JWT Token：

1. 登录 → 签发 Token
2. 请求 → Header 携带 `Authorization: Bearer <token>`
3. 验证 → `get_current_user` 依赖解析并验证
