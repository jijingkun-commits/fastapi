---
description: 命令与 Skill 写法规则，避免重复全局规则、门禁化空话与上下文膨胀
alwaysApply: true
---

# 命令与 Skill 写法规则

## 1. 先定职责

1. **命令是提示词，不是门禁表**：`.cursor/commands/*.md` 与镜像 `SKILL.md` 主要负责告诉模型“你是谁、先读什么、产出什么、怎么写”，不要写成流程审计清单。
2. **命令只写阶段特有信息**：全局规则放 `AGENTS.md`、Layer2 通用规则、`PLANS.md`；命令只保留当前阶段真的特有的输入、产物、结构和交接关系。
3. **同一件事只说一次**：优先原位改写，不做平行追加；若某条规则已在别处存在，命令里只保留阶段特有信息或直接引用来源。

## 2. 单入口与镜像

1. **作者态单入口**：`.cursor/commands/*.md` 是作者态真理源，编辑命令时只改这里。
2. **运行态单入口**：Codex 运行时默认消费 `.agents/skills/*/SKILL.md`；普通执行任务不要默认双读 `.cursor/commands/*.md` 与 `SKILL.md`。
3. **镜像只跟随真理源**：`.agents/skills/*/SKILL.md` 只做镜像，不在镜像侧手写额外规则；改完命令后统一跑 `python3 scripts/sync_rules_to_cc.py`。

## 3. 上下文要瘦，但不能瘦错地方

1. **少门禁词，多产出指引**：少写“强制/失败码/阻断/FAIL_FAST”，多写“怎么写出高质量产物、推荐结构、好例子、坏例子”。
2. **先给结构，再给例子**：涉及文档、计划、报告、验收输出时，优先给字段结构、最小骨架和 1 个可照抄示例，不堆抽象原则。
3. **先写读什么，再写怎么产出**：把当前阶段真正要读的真理源写清楚，同时显式缩掉归档、报告、截图、运行态噪音目录。
4. **不要为了瘦身删消费锚点**：可以删口号、删重复、删空原则，但不能静默删除会被下游脚本、测试或下一阶段命令消费的字段、标记、路径和章节名。

## 4. 瘦身时必须保留的消费锚点

1. 若字段已被下游消费，只能改解释，不能随意改名或删掉。
2. 典型规划锚点：`risk_tags`、`mandatory_evidence`、`acceptance_cmds[*]`。
3. 典型测试锚点：`Required Evidence`、`Actual Evidence`、`Scripted Flow Status`、`Historical Gap vs Current Gap`。
4. 典型工具契约锚点：`APPLY_PATCH_TOOL_UNAVAILABLE_FALLBACK`。
5. 典型原则：瘦身删的是重复表述，不是交接 contract。

## 5. 按命令类型套骨架

1. **执行/编排类命令**：优先写 `输入前置 -> 关键校验 -> 执行流程 -> 证据回填 -> 下一步`。
2. **分析/审查类命令**：优先写 `适用场景 -> 先读什么 -> 先报什么 -> 怎么判断 -> 产物骨架 -> 下一步`。
3. **requirements/design/plan/test/verify** 这类文档型命令，重点是把产物写具体，不是把态度写严厉。

## 6. 什么叫真正高质量

1. 说清要改什么模块、为什么这么改、删除哪些旧代码。
2. 设计和需求默认给流程图、字段骨架、删除清单、交接信息。
3. 少写“保持一致”“确保完整”这种空原则，多写路径、字段、命令、证据和示例。
4. 下游必须能直接承接上游，不靠再次猜测或二次发明字段。

## 7. 防回退写法

1. 命中过往真实问题时，先补一个最小合同测试，再改规则或命令。
2. 测试只冻住“以后不能再丢的锚点”，不要把整篇提示词逐字锁死。
3. 新增规则前先问一句：这条到底是全局规则、阶段规则，还是一次性说明；放错层比没写更容易制造重复。

## 8. 反模式速记

1. **反模式**：把 `AGENTS.md` 的全局要求再抄进每个命令。
   **正确写法**：命令只写阶段特有信息。
2. **反模式**：为了“更精简”把 `risk_tags`、`mandatory_evidence` 之类字段一起删了。
   **正确写法**：删重复描述，保留消费锚点。
3. **反模式**：命令正文只有失败码和阻断词，没有高质量产物结构。
   **正确写法**：先给骨架、字段、示例，再谈门禁。
4. **反模式**：同时手改 `.cursor/commands/*.md` 和 `.agents/skills/*/SKILL.md`。
   **正确写法**：只改命令真理源，镜像靠同步脚本生成。
