---
description: 命令与 Skill 写法规则，避免重复全局规则、门禁化空话与上下文膨胀
alwaysApply: true
---

# 命令与 Skill 写法规则

1. **命令是提示词，不是门禁表**：`.cursor/commands/*.md` 与镜像 `SKILL.md` 主要负责告诉模型“你是谁、先读什么、产出什么、怎么写”，不要写成流程审计清单。
2. **全局规则不重复下沉**：已在 `AGENTS.md`、Layer2 通用规则、`PLANS.md` 说明过的共性约束，不要再重复塞进单个命令或 Skill。
3. **同一件事只说一次**：优先原位改写，不做平行追加；若某条规则已在别处存在，命令里只保留阶段特有信息或直接引用来源。
4. **少门禁词，多产出指引**：少写“强制/失败码/阻断/FAIL_FAST”，多写“怎么写出高质量产物、推荐结构、好例子、坏例子”。
5. **先给结构，再给例子**：涉及文档、计划、报告、验收输出时，优先给字段结构、最小骨架和 1 个可照抄示例，不堆抽象原则。
6. **镜像只跟随真理源**：`.cursor/commands/*.md` 是命令真理源；`.agents/skills/*/SKILL.md` 只做镜像，不在镜像侧手写额外规则。
