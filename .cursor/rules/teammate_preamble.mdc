---
description: Teammate 硬约束（CC Team 模式专用）
alwaysApply: false
---

# Teammate 硬约束（CC Team 模式专用）

本文件只定义 **team 模式增量约束**；通用技术细则统一遵循 Layer2，避免重复维护。

## 规则入口（先读后做）
- 核心原则与技术栈：`.cursor/rules/core.mdc`
- MCP 路由与降级口径：`.cursor/rules/mcp-routing.mdc`
- 双数据库：`.cursor/rules/dual-database.mdc`
- LangGraph：`.cursor/rules/langgraph.mdc`
- 文档同步：`.cursor/rules/doc_sync.mdc`
- 语言风格：`.cursor/rules/python_style.mdc`、`.cursor/rules/typescript_style.mdc`

## Team 模式附加要求（仅增量）
1. 命中高风险文件时，修改前必须自检并在交付中说明影响范围与验证结果：
   - `app/ai/prompts/agent_prompts.py`
   - `app/ai/state.py`
   - `*_graph.py` 中的 `preprocess/postprocess` 节点
2. 发生工具降级或阻塞时，必须显式给出：`原因`、`替代工具`、`验证结果`。
