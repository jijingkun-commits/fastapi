---
description: Bugfix 最小变更规则，防止修复任务引入过量代码
alwaysApply: true
---

# Bugfix 最小变更规则

1. **先定位根因再改代码**：
   - 修复缺陷前，先给出复现路径、根因结论、影响范围，以及 `obsolete_paths` / `retained_paths` / `line_budget`。
   - 禁止直接堆叠条件分支掩盖问题。

2. **失败测试先行**：
   - 优先补充可复现失败的测试，再编写修复代码。
   - 若无法补测试，必须说明阻塞原因与替代验证方式。

3. **默认预算约束**：
   - 修改代码文件数不超过 3。
   - 预算统计按**整个变更集**计算，而不是只看当前文件局部变化。
   - 净新增行数（added - deleted）不超过 80。
   - 总新增行数（added）不超过 140；新增文件、拆出的辅助模块、helper 文件同样计入 `added`。
   - 超预算时必须先说明不可拆分原因与拆分计划。

4. **禁止过度设计**：
   - Bugfix 场景禁止新增与当前根因无关的抽象层、通用框架或配置体系。
   - 若需要重构，应拆分为独立任务，不与 bugfix 混在同一提交。

5. **提交前减法审查**：
   - 输出每个改动文件的必要性。
   - 若本次新增替代实现，必须删除旧路径；确需保留时写唯一理由与失效条件。
   - 当前文件删行不能单独视为“已经瘦身”；若同时新增新文件、helper 文件或 fallback 分支，必须合并核算整个变更集。
   - 优先给出更小变更面的等价方案。
