---
description: 核心原则，每次对话必须遵守
alwaysApply: true
---

# 核心原则

1. **中文主导**: 思考过程和输出永远使用中文，说人话。
2. **审慎修改**: 修改前逐行审查源代码；不确定时先查文档/测试/调用链，仍无法判断再一次性提问
3. **工程质量优先**: 优先结构清晰、命名一致、可读、可测试、可维护的方案
4. **精简优先**: 能用现有模块就不新建；必要时先做适度重构以降低复杂度与后续维护成本，尽量降低代码行数，不要每次都引入大量新代码。
5. **纯净注释**: NEVER 在注释中使用 emoji，NEVER 添加"修复/优化"过程注释
6. **架构优先**: 独立思考，勇于指出现有项目的问题和错误，不要顺着用户的思路走；无论是需求开发、缺陷修复还是性能调整，都先确认模块边界、状态契约与职责归属；跨层改动必须先梳理端到端数据流与状态生命周期，避免策略散落到多个节点造成隐性耦合
7. **拒绝补丁**: 禁止以临时条件分支、硬编码或重复逻辑掩盖问题；必须先定位根因，并在正确层级进行系统性修复，必要时先做适度重构以降低复杂度与后续维护成本
8. **文档同步**: 涉及架构/API/表结构/配置变更时，先更新文档再改代码
9. **智能体优先**: 本项目为智能体项目，涉及智能体流程（如 `app/ai/**`）的功能实现时，优先采用流程编排、策略配置、工具抽象与状态管理方案；禁止优先通过硬编码业务分支或固定决策路径实现功能
10. **OpenClaw 对标优先**: 长期参考项目为 `/Users/jijingkun/bojxAI/bot/openclaw`，涉及智能体架构、流程编排、工具抽象、状态管理与关键实现方案时，优先对齐其设计理念与实现方式；若与本项目约束冲突，需先说明差异再给出落地方案
11. **文档收敛**: 对 `docs/plans/*-design.md` 与 `docs/内部参考/迭代需求/*_requirements.md`、`*_implementation_plan.md` 只保留最终方案；禁止方案 A/B/C 或 1/2/3 对比表。若需说明取舍，仅允许在“决策权衡”中简述放弃原因，不做评分。
12. **开关默认开启**: 新功能/新链路配置开关默认值应为开启（`true`）；除非用户明确要求灰度，禁止采用“默认关闭 + 分阶段放量”作为默认口径。回退路径统一写为“关闭开关或触发降级策略”。
13. **预上线重构优先架构**: 目前项目未上线，若确认需要重构，优先消除结构性问题与维护成本；不要以”保持线上平稳/历史兼容”为首要约束保留结构性债务。仍需保证关键路径可验证，并给出统一回退口径（关闭开关或触发降级策略）。
14. **讨论优先联网核验**: 讨论/方案阶段涉及以下场景时，必须先使用联网搜索或 GitHub 搜索核验事实，禁止仅凭记忆推断：
    - 第三方库版本、API 变更、发布日期、官方声明
    - 开源项目实现方案、Issue 讨论、PR 状态
    - 技术选型对比、最佳实践、社区共识
    - 安全漏洞、兼容性问题、已知 Bug
    - 若 MCP 工具不可用，必须明确标注”基于本地知识推断，需人工复核”并说明降级原因

15. **语义判定边界**: 编排层（如 `app/services/**`、`app/api/**`、router/controller）禁止通过关键词词表、正则词表、substring 命中或 `*_HINTS/*_KEYWORDS/*_TRIGGERS` 常量执行自然语言语义识别；语义判定必须归属于 `intent/policy/resolver` 层，并以结构化 contract（如 `decision/reason_code/slot_key/needs_clarification`）对外输出；若现有判定置信度不足，应返回拒绝/澄清 contract，而不是把语义 fallback 下沉到编排层
16. **Lean 硬门禁**: 命中热点目录（默认 `app/ai/workflow/**/*.py`、`app/services/**/*.py`、`scripts/**/*.py`）时，超阈值文件必须进入 shrink-only 模式：继续修改该文件时默认不得净增长；超阈值文件默认禁止新增私有 helper（`def _*` / `async def _*`）与新增嵌套函数；若确属新增能力，应优先外移到新模块/新职责边界，而不是继续向热点文件追加内部函数，执行细则以 `docs/工程规范/lean-guard.md` 与 `scripts/ci/check_lean_budget.py` 为准
17. **瘦身合同前置**: `bugfix/refactor` 编码前必须声明 `change_mode`、`obsolete_paths`、`retained_paths`、`single_entry_owner` 与 `line_budget`；若新实现已覆盖旧职责，却继续保留旧函数、旧分支或旧文件且无唯一保留理由，应 `FAIL_FAST`，不得以“先留着以后删”过关
18. **命令执行拆分**: 执行 shell / Git / 测试 / 验证命令时，必须遵循“单步单目标”原则：基础观测、期望比对、解释器解析、测试执行、统计汇总分别执行；禁止把多个阶段拼成一条长 `&&` / `;` / 管道链。任一步失败只允许重跑当前步，长任务启动后只允许轮询/读取结果，输出被截断时优先拆短当前步，不得为看尾部结果整串重跑
# 技术栈

- **后端**: FastAPI + LangGraph + SQLAlchemy 2.0
- **前端**: Next.js 15 + React 19 + TypeScript
- **AI**: 涉及 LangChain/LangGraph 时用 `context7` MCP 查询最新 API
