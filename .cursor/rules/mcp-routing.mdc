---
description: MCP 工具路由优先规则（可降级，避免工具缺失导致阻塞）
alwaysApply: true
---

# MCP 工具路由（优先 + 可降级）

以下场景应优先使用对应 MCP 工具；当 MCP 未配置、不可用或权限受限时，允许降级到 CLI/脚本，并在结果中标注降级原因与替代命令。

| 场景 | 优先 MCP 工具 | 降级方式（MCP 不可用时） |
|------|-------------------|-------------|
| 查询 chat_db 数据/表结构 | `mcp__postgres__query` | `psql`（需在输出说明 SQL 与目标库） |
| 查询 data_db 数据/表结构 | `mcp__postgres-data-db__query` | `psql`（需在输出说明 SQL 与目标库） |
| LangChain/LangGraph/第三方库 API 用法不确定时 | `context7`（先 resolve-library-id 再 query-docs） | 官方文档 + 版本说明（禁止纯记忆猜测） |
| 讨论/方案阶段需要外部事实核验（版本、发布日期、官方声明） | `web.search_query`（优先官方来源） | 明确标注“基于本地知识推断”并提示人工复核 |
| GitHub 操作（PR/Issue/代码搜索） | GitHub MCP（若已配置） | `gh` CLI 或 Web 操作记录 |
| E2E 测试/浏览器交互/截图 | `playwright` 系列工具 | 本地 Playwright CLI / 浏览器手动复现 |
| 任务管理/看板操作 | `vibe_kanban` 系列工具 | 项目内任务文档 + 明确状态变更记录 |
| 对象存储操作 | `minio` 系列工具 | `mc` CLI / SDK 脚本（需记录桶与对象路径） |

## 触发规则（强制）

1. **数据库查询**：涉及数据库查询时，先判断目标库（chat_db vs data_db），优先调用对应 MCP。
2. **第三方库 API**：涉及第三方库 API 且不是 100% 确定用法时，优先用 context7 查文档再写代码。
3. **GitHub 操作**：涉及 GitHub 操作时，先检测是否存在 GitHub MCP；不存在则直接降级到 `gh` CLI，不阻塞任务。
4. **浏览器测试**：需要浏览器测试时优先用 Playwright MCP；若不可用，可改为 Playwright CLI。
5. **降级透明化**：发生降级时，回复中必须包含：`原因`、`替代工具`、`验证结果`。
6. **讨论阶段强制联网核验**（新增硬约束）：
   - 凡结论依赖外部最新信息（版本、发布日期、官方声明、API 变更），**必须先联网检索**，禁止仅凭记忆推断。
   - 涉及开源项目实现、Issue 讨论、PR 状态、代码示例时，**必须同步进行 GitHub 检索**。
   - 技术选型、最佳实践、安全漏洞、兼容性问题等场景，**优先使用 `WebSearch` 查询官方来源**。
   - 若 MCP 工具不可用，必须在回复中明确标注"基于本地知识推断，需人工复核"并说明降级原因。

## 项目开发讨论场景主动搜索触发（强制）

以下场景在讨论/方案阶段**必须先搜索再回答**，禁止仅凭记忆或本地知识推断：

### A. 架构设计与模式（必搜）
**触发词**：架构、设计模式、最佳实践、业界做法、主流方案、参考实现
**搜索策略**：
- 优先搜索：`site:github.com [技术栈] [模式名] architecture OR pattern`
- 次选搜索：`[技术栈] [场景] best practices 2024 OR 2025`
- 必须包含：至少 2 个开源项目实现案例或官方文档

**示例触发**：
- "LangGraph 多智能体架构最佳实践"
- "FastAPI 项目结构业界做法"
- "这个设计合理吗？有没有更好的方案"

### B. 重构与优化（必搜）
**触发词**：重构、优化、改进、简化、性能提升、代码质量
**搜索策略**：
- 优先搜索：`[具体问题] refactoring patterns OR optimization`
- 次选搜索：`site:stackoverflow.com [技术栈] [问题] performance`
- 必须包含：性能对比数据或社区共识

**示例触发**：
- "这段代码有没有优化空间"
- "如何重构这个模块降低复杂度"
- "性能瓶颈在哪，业界怎么解决"

### C. 技术选型与对比（必搜）
**触发词**：选型、对比、vs、哪个更好、区别、优缺点、替代方案
**搜索策略**：
- 优先搜索：`[方案A] vs [方案B] comparison 2024`
- 次选搜索：`[技术栈] [场景] alternatives OR comparison`
- 必须包含：至少 2 个来源的对比分析（官方文档、技术博客、GitHub discussions）

**示例触发**：
- "Vanna vs LangChain SQL Agent 哪个更适合"
- "用 Redis 还是 PostgreSQL 做缓存"
- "有没有更轻量的替代方案"

### D. 合理性与风险评估（必搜）
**触发词**：合理吗、有问题吗、风险、隐患、坑、注意事项、生产环境
**搜索策略**：
- 优先搜索：`[方案] production issues OR gotchas OR pitfalls`
- 次选搜索：`site:github.com [方案] issues label:bug OR label:discussion`
- 必须包含：已知问题、社区反馈、生产案例

**示例触发**：
- "这个方案有什么风险"
- "生产环境这样做有没有坑"
- "这个设计合理吗，会不会有隐患"

### E. 最新特性与兼容性（必搜）
**触发词**：最新、支持、兼容、废弃、推荐、官方、稳定版
**搜索策略**：
- 优先搜索：`[库名] [版本] release notes OR changelog`
- 次选搜索：`[库名] [特性] support OR compatibility`
- 必须包含：官方文档或 GitHub Release 页面

**示例触发**：
- "LangGraph 0.2.x 支持流式输出吗"
- "FastAPI 最新版推荐用哪个 ORM"
- "这个 API 在新版本还能用吗"

### F. 错误排查与已知问题（优先搜索）
**触发词**：报错、异常、Bug、不工作、失败、超时、崩溃
**搜索策略**：
- 优先搜索：`[错误信息] site:github.com OR site:stackoverflow.com`
- 次选搜索：`[库名] [错误类型] known issues OR workaround`
- 必须包含：Issue 讨论或解决方案

**示例触发**：
- "ModuleNotFoundError 怎么解决"
- "为什么 LangGraph checkpointer 一直报错"
- "这个异常是已知问题吗"

### G. 社区共识与争议（必搜）
**触发词**：争议、讨论、社区、主流、普遍、大家都、业界
**搜索策略**：
- 优先搜索：`[话题] site:reddit.com OR site:news.ycombinator.com`
- 次选搜索：`[话题] community consensus OR discussion`
- 必须包含：多个来源的观点对比

**示例触发**：
- "社区对这个方案怎么看"
- "业界普遍用什么方案"
- "这个做法有争议吗"

### H. 工程方法论与流程（必搜）
**触发词**：方法论、流程、规范、标准、工作流、开发模式、团队协作、代码审查、CI/CD、测试策略、发布流程、分支策略、版本管理
**搜索策略**：
- 优先搜索：`[方法论/流程] best practices 2024 OR 2025 site:github.com OR site:martinfowler.com`
- 次选搜索：`[场景] workflow OR methodology OR process engineering`
- 必须包含：至少 1 个知名团队/公司的实践案例或权威文章

**示例触发**：
- "Git 分支策略最佳实践"
- "代码审查流程业界怎么做"
- "CI/CD 流程应该怎么设计"
- "测试金字塔模型适用吗"
- "Monorepo vs Multirepo 哪个更好"
- "团队协作用什么工作流"
- "发布流程有什么规范"
- "版本号怎么管理比较合理"

### I. 设计模式与编程范式（必搜）
**触发词**：设计模式、SOLID、DRY、KISS、单一职责、依赖注入、工厂模式、策略模式、观察者模式、函数式编程、面向对象、领域驱动设计、DDD、CQRS、事件驱动
**搜索策略**：
- 优先搜索：`[模式名] [技术栈] implementation examples OR use cases`
- 次选搜索：`[模式名] when to use OR anti-patterns site:refactoring.guru OR site:martinfowler.com`
- 必须包含：模式适用场景、反模式警示、代码示例

**示例触发**：
- "这里应该用什么设计模式"
- "依赖注入在 Python 中怎么实现"
- "领域驱动设计适合这个项目吗"
- "事件驱动架构有什么坑"
- "CQRS 模式的最佳实践"
- "函数式编程在这个场景合适吗"

### J. 代码质量与可维护性（必搜）
**触发词**：代码质量、可维护性、可读性、技术债、代码坏味道、重复代码、圈复杂度、耦合度、内聚性、代码规范、Lint、格式化
**搜索策略**：
- 优先搜索：`[技术栈] code quality best practices OR clean code`
- 次选搜索：`[问题] code smell OR refactoring OR maintainability`
- 必须包含：量化指标（如圈复杂度阈值）或工具推荐

**示例触发**：
- "这段代码有什么坏味道"
- "如何提高代码可维护性"
- "圈复杂度多少算高"
- "Python 代码规范用什么工具"
- "技术债怎么量化和管理"
- "代码审查应该关注哪些点"

## 搜索质量要求（强制）

1. **多源验证**：至少 2 个独立来源（官方文档 + GitHub/Stack Overflow/技术博客）
2. **时效性标注**：必须标注信息发布时间（优先 2024-2025 年内容）
3. **来源可信度**：优先级 `官方文档 > GitHub > Stack Overflow > 技术博客 > 个人博客`
4. **结果透明化**：回复中必须包含：
   - 搜索关键词
   - 信息来源链接（至少 2 个）
   - 信息发布时间
   - 可信度评估（高/中/低）
5. **冲突处理**：若多个来源信息冲突，必须列出差异并说明推荐理由

## 搜索失败降级（强制）

若搜索工具不可用或无有效结果，必须：
1. 明确标注"⚠️ 基于本地知识推断（未联网核验），需人工复核"
2. 说明降级原因（工具不可用/搜索无结果/网络问题）
3. 给出替代验证方式（手动搜索关键词、查阅官方文档链接）
4. 标注知识截止日期与可能过时的风险
