"""
语义层：Semantic Model 定义文件

目前阶段仅包含指标定义（Metrics），基于 t_dmp_ind_info 表。
业务明细表（Orders/Users 等）尚未接入，待后续补充。
"""

version: "1.0"

# ==========================================
# 1. 数据模型定义 (Models)
# ==========================================
models:
  # 暂无业务表，保留空列表或注释
  []

# ==========================================
# 2. 关系定义 (Relationships)
# ==========================================
relationships:
  []

# ==========================================
# 3. 指标定义 (Metrics)
# ==========================================
# 指标元数据已存储在数据库表 t_dmp_ind_info 中。
# 此处可保留少量核心指标作为示例，或留空完全依赖数据库检索。
metrics:
  []

# ==========================================
# 4. 维度定义 (Dimensions)
# ==========================================
dimensions:
  []
